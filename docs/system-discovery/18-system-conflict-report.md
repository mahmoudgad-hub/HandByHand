# 18 — SYSTEM CONFLICT REPORT

**٣١ بندًا.** كلٌّ منها مُثبت بموضعٍ في الكود. **ولم يُصلَح شيء** — هذه مرحلة جرد.

**`NEW`** = لم أجده مسجَّلًا في `pm/02-backlog.md` ولا `docs/00-roadmap.md` ولا
`docs/business-analysis/*` ولا `tests/test-cases/04-findings.md`.
**`KNOWN`** = مسجَّل، والمرجع مذكور.

| الصنف | العدد |
|---|---|
| 🔴 CRITICAL | **٥** |
| 🟠 HIGH | **١١** |
| 🟡 MEDIUM | **١٠** |
| ⚪ LOW | **٥** |

---

# 🔴 CRITICAL

## C-01 · رصيد الباقة لا يُخصَم أبدًا — `NEW`

| البند | التفصيل |
|---|---|
| **النوع** | Business logic غير موصولة · فقدان سلامة بيانات |
| **الدليل** | `hbh.consume_package_session` معرَّفة في `0008_billing.up.sql:473`، وممنوحة لـ`hbh_app` في السطر `693`، **ولا منادٍ لها في أي مكان**: صفر في `api/`، صفر في `web/`، صفر في أي مشغّل، وصفر داخل أي دالّة أخرى (`grep` على `db/` يرجّع ملفَّ تعريفها وحده + ملفّ `down`) |
| **و`close_session`** | قرأتُ جسمها كاملًا (`0005_scheduling.up.sql:772`): تحدّث `therapy_sessions`، وتُكمل الموعد، **ولا تلمس الباقة** |
| **العاقبة** | `child_packages.sessions_used` لا يتحرّك · `package_ledger` **يبقى فارغًا للأبد** · `child_packages.EXHAUSTED` **حالةٌ لا تُبلَغ** · و`expire_packages` تعلّم `EXPIRED` بالتاريخ فقط. **أسرةٌ تشتري ١٢ جلسة تبقى لها ١٢ إلى الأبد.** |
| **والأخطر** | التصميم يقول صراحةً: «كل حركة تخلّف قيدًا مضاف-فقط يحمل الرصيد بعدها. **أسرةٌ تسأل أين ذهبت جلساتي الاثنتا عشرة تُجاب من الدفتر**» — **والدفتر لا يُكتب فيه شيء** |
| **ولماذا لم يمسكه اختبار** | `tests/db/p6_verify.sql` **ينادي الدالّة مباشرةً** ويثبت أن حسابها صحيح (٢ ← ١ ← ٠ ← `HB051`). **وهو يثبت الدالّة ولا يثبت أن شيئًا يناديها** — وهي نفس عائلة `expire_packages` بالحرف: «كُتبت واختُبرت وكانت صحيحة **ولا شيء يناديها**» |
| **ملاحظة على وثيقة قائمة** | `docs/business-analysis/03-application-flows.md:408` يقول «**خصم:** `consume_package_session` عند إغلاق الجلسة» — **وهو وصفُ ما يجب أن يكون لا ما هو.** والباكلوج يسجّل أنها «لا تسأل عن الدفع» (`BL-37 ج`) **ولا يسجّل أنها لا تُنادى** |
| **السؤال العالق قبل أي إصلاح** | `OQ-02` — متى يُخصَم؟ وهل `ABORTED` يُخصَم؟ **قرار عمل لا قرار تقني** |

## C-02 · منحة `REPORT.WRITE` تضيع على قاعدة معاد بناؤها — `NEW`

| البند | التفصيل |
|---|---|
| **النوع** | خرقُ قاعدةٍ مكتوبة · ميزة تصير غير قابلة للوصول بصمت |
| **الدليل** | `db/migrations/0091_report_authoring.up.sql:79-87` — `INSERT INTO hbh.role_permissions … FROM hbh.roles r JOIN (VALUES ('CENTER_ADMIN','REPORT.WRITE'), ('THERAPIST','REPORT.WRITE')) …` |
| **القاعدة المنقوضة** | `CLAUDE.md` و`db/seed/0002_rbac.sql` (سبع مرّات): «**المهاجرة تُنشئ البنية. وأي `INSERT` يقرأ جدولًا تملؤه البذور مكانه ملفّ البذور.** `scripts/db.sh` يطبّق **كل** المهاجرات أوّلًا ثم البذور، فوقت تشغيل المهاجرة تكون `hbh.roles` **فارغة** على قاعدة معاد بناؤها. الـ`INSERT … SELECT` لا يطابق شيئًا، ولا يُدرج شيئًا، **ويُبلغ عن النجاح**» |
| **مؤكَّد** | `scripts/db.sh:194` يلفّ الهجرات · `:224` يلفّ البذور — **بهذا الترتيب** |
| **العاقبة** | على قاعدة معاد بناؤها: `REPORT.WRITE` يوجد في `hbh.permissions` **ولا دور يملكه**. فـ`create_report` و`update_report` ترفعان `HB032` **لكل أحد**، ومسار الكونسول `/children/:id/reports/new` يُحجب بحارسه، **وميزة صياغة التقارير (F-32) تختفي** — والقاعدة تبدو سليمة تمامًا |
| **ولا شيء في السكيما يغطّيه** | `db/seed/0002_rbac.sql` **لا يحتوي `REPORT.WRITE`** — فُحص |
| **ما يمسكه** | `tests/db/p14_verify.sql:343` يفحص `has_permission('REPORT.WRITE')` — **فسيفشل على رَبنة نظيفة**، وهذا جيّد. **لكن لم تُشغَّل جولة كاملة منذ 2026-09-03** |
| **الإصلاح** | نقل المنحة إلى `db/seed/0002_rbac.sql` (الصلاحية نفسها تبقى في `0091` — إدراجها لا يقرأ شيئًا) |

## C-03 · `origin_source` / `registration_source` بلا كاتب للأمام — `NEW`

| البند | التفصيل |
|---|---|
| **النوع** | سكيما تشيخ من لحظة إضافتها |
| **الدليل** | `grep -rn "registration_source\|record_completeness\|origin_source" db/ api/ web/` **لا يرجّع شيئًا خارج `0129`** |
| **ما يفعله `0129`** | يضيف الأعمدة · يضيف قيودها · **يبنيها بأثر رجعي** · يضيف `trg_origin_immutable` |
| **ما لا يفعله** | `convert_enrolment` لا تختم `ENROLMENT_REQUEST` · و`POST /children` و`POST /guardians` لا يختمان `CENTER` |
| **العاقبة** | **كل صفّ يُنشأ من 2026-09-12 يحمل `NULL`.** والعمود المبنيّ بأثر رجعي ليجيب «من أي باب دخل هذا السجلّ» **يجيب عن الماضي وحده** |
| **والأثر التراكمي** | المشغّل يسمح بـ`NULL → قيمة` **مرّة واحدة** — فالتصحيح اليدوي ممكن، لكن مع كل يوم يكبر عدد الصفوف التي تحتاجه |
| **`ONLINE_CONSULTATION`** | قيمةٌ في `CHECK` **بلا كاتب ولا جدول `consultation_requests`** — راجع F-36 |
| **السؤال** | `OQ-05` — هل الأصل مطلوب لسجلّات جديدة، ومن يختمه؟ |

## C-04 · `HB240` يحمل قاعدتين مختلفتين — `NEW`

| البند | التفصيل |
|---|---|
| **النوع** | تصادم رمز رفض · ترجمة HTTP خاطئة |
| **الدليل** | `0100_available_slots.up.sql:76` و`0127_online_slots.up.sql:84` يرفعان `HB240` بمعنى «**لا يجوز لك قراءة دفتر الحجز**» · و`0129_origin_of_a_record.up.sql:204` يرفعه بمعنى «**الأصل لا يُغيَّر بعد تسجيله**» |
| **ورأس `0129`** | يقول حرفيًّا `Error classes added here: HB240` — **أي أن مؤلّفه اعتقده حرًّا** |
| **الترجمة** | `api/internal/http/ops_handlers.go:975` → `http.StatusForbidden` — **صحيحة للأولى وخاطئة للثانية**: رفضُ عدم-قابلية-التغيير يصل الشاشة كـ«لا صلاحية» |
| **والأثر على اللوج** | المشغّل يقرأ **محاولةَ وصولٍ غير مصرَّح بها** حيث لم تكن هناك واحدة |
| **والمشروع كتب هذا الدرس بنفسه** | رأس `0052`: «`HB061` مأخوذ بالفعل… **ورقمٌ مُعاد استعماله أسوأ من رقم مفقود: القراءة الثانية واثقةٌ وخاطئة**» — ووقع نفسه بعد **٧٧ هجرة** |
| **لماذا نجا من الحارس** | `scripts/api.sh code-drift` يقرأ `pg_proc` ويقارنه بالجدول، فيجيب «`HB240` معروف» — **وهو معروف بالتعريف الخطأ**. والحارس يفحص **الوجود لا التفرّد** |
| **العلاج المقترح** | رمزٌ جديد لقاعدة الأصل (`OQ-26` يقرّر أيّهما ينتقل) + توسيع `code-drift` ليفشل على `SQLSTATE` يُرفَع بمعنيين |

## C-05 · صيغة الجوال في الواجهة تحجب ميزةً مقبولةً في القاعدة — `NEW`

| البند | التفصيل |
|---|---|
| **النوع** | **نفس قاعدة العمل منفَّذة في مكانين بتنفيذين مختلفين** — وهي بالضبط الحالة التي وُضع هذا الجرد لكشفها |
| **الدليل في القاعدة** | `sys_params.MOBILE_PATTERN = '^\+?[0-9٠-٩][0-9٠-٩\s().-]{5,24}$'` — يقبل `+` والأرقام العربية والمسافات والأقواس والنقاط والشرطات (`0113` سمّى ذلك «يقبل الكتابة الجارية») · و`0112_international_mobiles` (٣٣ كيلوبايت) + `country_dial_codes` + `canonical_mobile` |
| **الدليل في الواجهة** | `web/shared/src/config/app-config.ts:95` → `phonePattern: '^01[0-9]{9}$'` · و`web/portal/src/app/features/apply/apply.ts:180` و`:251` → `/^01[0-9]{9}$/` **مكتوبًا حرفيًّا مرّتين** |
| **العاقبة** | **مجموعة القبول `p15` عنوانها: «أسرةٌ خارج مصر يمكن تسجيلها والوصول إليها — *ذلك هو الميزة*»، وهي خضراء.** والنموذج **يرفض الرقم الدولي قبل أن يبلغ الخدمة** — بلا أي رسالة تشرح السبب الحقيقي |
| **وثلاث نسخ لا نسختان** | `app-config.ts` + سطران داخل `apply.ts` — **والقيمة في الأخيرين ليست مقروءةً من التهيئة أصلًا** |
| **والطبقة الأضعف هي التي تقرّر** | القاعدة ٤ بالحرف، مطبَّقةً على قاعدة صيغة بدل صلاحية |
| **العلاج** | قراءة النمط من `GET /api/v1/settings/params` (المسار موجود ومصادَق) · وحذف النسختين المضمَّنتين · **وحارس لينت يمنع رقمًا مصريًّا مضمَّنًا في `web/`** |

---

# 🟠 HIGH

## C-06 · رفع أصول الموقع يكتب داخل شجرة git وداخل مجلّد مزامنة — `NEW`

| البند | التفصيل |
|---|---|
| **الدليل** | `deploy/compose/docker-compose.yml:131` → `SITE_ASSETS_DIR: /srv/site-assets` · و`:144` → `- ../../site/assets:/srv/site-assets` |
| **و`site/assets` متعقَّب في git** | `git ls-files site/assets` يرجّع ٩ ملفّات منها `5e76…mp4` |
| **والشجرة تحت OneDrive** | المشروع تحت «سطح المكتب» المُعاد توجيهه |
| **العاقبة** | كل صورة فريق أو فيلم تعريف يُرفَع من شاشة `/site/team` **يهبط في شجرة العمل** → يُودَع في git → **يبقى في التاريخ للأبد** · **ويُزامَن إلى حساب سحابي شخصي** |
| **والحالة الأسوأ** | **صورةٌ رُفعت ولم تُنشَر بعد** (فموافقة صاحبها قد لا تكون مسجَّلة — `trg_site_consent_stamp` يحكم النشر لا الرفع) **تدخل التاريخ على أي حال** |
| **ونفس العائلة دُفع ثمنها مرّتين** | النسخ الاحتياطية كانت في `backups/` داخل المشروع، **فكل dump فيه السجلّ السريري الكامل كان يُرفَع تلقائيًّا** — و`backup.sh` صار **يقف** عند أي مجلّد مزامنة. و`.gitignore` يستبعد `/gallery/` و`/whatsapp/` و`/Image/` بتعليقٍ يقول: «**ودخولها تاريخ git مرّة واحدة يعني بقاءها فيه، فالاستبعاد يسبق `git init`**» |
| **والمخفِّف** | هذه أصول تسويقية بريدها الإنترنت المفتوح · و`STAFF_DOCS_DIR` **سليم**: حجم دوكر مسمًّى (`staffdocs`) خارج الشجرة، وذلك قرارٌ مكتوبٌ بسببه في نفس الملفّ |
| **العلاج** | وجهة رفعٍ خارج الشجرة (مثل `~/hbh-site-assets`) و`site-export.sh` ينسخ منها إلى `site/assets` عند النشر — **فيدخل التاريخَ المنشورُ وحده** |

## C-07 · `guardians.mobile` بلا قيد تفرّد — `NEW`

| البند | التفصيل |
|---|---|
| **الدليل** | الفهرس الفريد الوحيد على `guardians` هو `uix_guardians_user ON (user_id) WHERE user_id IS NOT NULL` (`0002:253`). **لا شيء على `mobile`** |
| **مقابل** | `uix_users_mobile_active ON hbh.users (mobile)` للصفوف الحيّة (`0098`) |
| **العاقبة** | `POST /api/v1/guardians` مرّتين بنفس الرقم ينتج **صفّين ولا شيء يعترض**. `convert_enrolment` **تعيد الاستعمال** فلا تنتجه، **والإدخال المباشر ينتجه** |
| **والكشف متأخّر** | التصادم يظهر عند `grant_portal_access` بـ`HB204` (`0126`) — **وحينها يوجد صفّان وأحدهما مربوطٌ بأطفال** |
| **ولماذا لم يُضَف القيد** | لأن السؤال **سؤال عمل**: هل يجوز لجوّال واحد أن يخدم أمًّا وأبًا يتشاركان هاتفًا؟ `OQ-43` |
| **وإن كان الجواب نعم** | فالتكرار الحقيقي **غير قابل للكشف بالجوال**، ويحتاج معيارًا آخر (اسم + رقم قومي) |

## C-08 · `guardians.user_id` قابل للكتابة من مسار CRUD عامّ — `NEW`

| البند | التفصيل |
|---|---|
| **الدليل** | `api/internal/store/crud.go:282-283` — `user_id` في `insertCols` **و**`updateCols` لمورد `guardians` |
| **والسياسة لا تقيّد عمودًا** | `p_guardians_edit` (`0012:217`) تفحص المركز و`GUARDIAN.MANAGE` **ولا شيء على مستوى العمود** |
| **العاقبة** | من يملك `GUARDIAN.MANAGE` (**والاستقبال يملكها**) يستطيع `PATCH /api/v1/guardians/{id}` ليعيد توجيه صفّ وليّ أمر إلى **حساب وليّ أمر آخر في نفس المركز** — فيصير دخول ذلك الحساب يحلّ إلى هذا الصفّ، **ويرى أطفاله** |
| **والمخفِّف** | `uix_guardians_user` يمنع توجيه صفّين إلى حساب واحد · والفعل مسجَّل في `audit_log` · والفاعل موظّف مصرَّح له |
| **لكن** | **الطريق المصمَّم هو `grant_portal_access` وحده**، وهذا بابٌ ثانٍ بجواره **بلا قاعدة تحكمه** — وهو نفس نمط «بابٌ ثانٍ بجوار الباب الذي عليه كل الفحوص» الذي منعته حزمة `meeting` بقصد |
| **وللسكيما سابقةٌ في العلاج** | `sys_params.code` **مستثنًى من منحة الأعمدة تمامًا، فلا صلاحية تبلغه** (`0051`) — نفس الأداة تنفع هنا |

## C-09 · طفلٌ واحد بطلبين يصير طفلين — `NEW`

| البند | التفصيل |
|---|---|
| **الدليل** | `convert_enrolment` (`0125`) — السطر ٤٩: `INSERT INTO hbh.children …` **بلا أي بحث** · مقابل السطر ٥٥: `SELECT g.guardian_id … WHERE mobile = …` **للوليّ** |
| **العاقبة** | أسرةٌ أرسلت طلبًا من الموقع ثم اتّصلت هاتفيًّا (فأُدخل طلب ثانٍ) — **وحُوِّل الطلبان** — تحصل على **طفلين مربوطين بنفس وليّ الأمر**: مواعيده على أحدهما وخطته على الآخر |
| **الحماية الوحيدة** | أن يلاحظ موظّف الاستقبال ويسم الثاني `DUPLICATE` **قبل** التحويل. والحالة موجودة في الآلة، **ولا شيء يقترحها ولا يحذّر** |
| **والرقم القومي لا يكفي** | `uix_children_national` **فهرس جزئي** `WHERE national_id IS NOT NULL` — **وهو صحيحٌ ومقصود** (كثير من الأطفال الصغار في مصر بلا رقم قومي)، لكنه لا يمسك الحالة الشائعة |
| **السؤال** | `OQ-48` — بأي معيار يُقال إنهما طفل واحد، ومن يقرّر؟ |

## C-10 · أرشفة وليّ الأمر لا تمسّ حسابه — `NEW`

| البند | التفصيل |
|---|---|
| **الدليل** | `store.SoftDelete` (`crud.go:893`) = `UPDATE %s SET active_flg = false, deleted_at = now() …` — جملةٌ واحدة على جدولٍ واحد |
| **مقابل** | `trg_revoke_sessions_on_offboard` (`0105`) موجودٌ **لأرشفة `users` وحدها** |
| **العاقبة** | أسرةٌ مؤرشَفة تبقى قادرة على الدخول: صفّها في `users` ما زال `ACTIVE` وجلساته صالحة حتى `SESSION_TTL_MINUTES` |
| **والعكس أيضًا** | أرشفة المستخدم لا تؤرشف صفّ وليّ الأمر — **عمليّتان منفصلتان بلا رابط** |
| **السؤال** | `OQ-44` |

## C-11 · `meetings` بلا آلة حالة ولا جدول تاريخ — `NEW`

| البند | التفصيل |
|---|---|
| **النوع** | خرقُ القاعدة ٥ على أحدث جدول حالة في السكيما |
| **الدليل** | `CREATE TRIGGER` في `0120`–`0124` = `trg_meetings_touch` و`trg_appointments_meeting` **فقط**. لا `legal_meeting_transition`، ولا `trg_meeting_status`، ولا `meeting_status_history` |
| **القاعدة** | «**الحالات آلات حالة مقيَّدة بجدول تاريخ — لا تحديث حرّ لعمود حالة، ولا من مدير المركز**» |
| **العاقبة** | `UPDATE hbh.meetings SET status = 'READY'` على لقاء `CLOSED` يمرّ — و`ck_meetings_closed` يمنعه **فقط** لأنه يربط `CLOSED` بـ`closed_at`، وهي حماية جانبية لا آلة |
| **وقيمتان بلا كاتب** | `PENDING` و`FAILED` (`0120:158`) — **حالتان لا يمكن الوصول إليهما** |

## C-12 · `ck_meetings_provider` يسمح بمزوّد لا تعرفه الخدمة — `NEW`

| البند | التفصيل |
|---|---|
| **الدليل** | `0120:33` → `CHECK (provider IN ('JITSI_JAAS', 'JITSI_PUBLIC', 'DAILY'))` · و`api/internal/meeting/meeting.go:45-47` يعرّف `ProviderJaaS` و`ProviderPublic` **وحدهما**. و`grep -rn DAILY api/` يرجّع صفرًا |
| **العاقبة** | صفّ لقاءٍ بـ`provider = 'DAILY'` **يُقبَل في القاعدة ويستحيل توقيعه في Go** — ورفضٌ لا رمز له |
| **الأثر اليوم** | صفر: `trg_appointment_meeting` هو الكاتب الوحيد ولا يضعها. **لكنها قدرةٌ معلَّقة تقرأ كخيار قائم في أي تخطيط** |

## C-13 · حارسا مسارٍ يذكران صلاحيةً غير التي تطلبها القاعدة — `NEW`

| المسار | حارس الشاشة | ما تطلبه القاعدة |
|---|---|---|
| `/settings` | `CATALOG.MANAGE` | **`SETTINGS.MANAGE`** (`set_center_param` · `clear_center_param` · `guard_center_settings`) |
| `/satisfaction` | `CATALOG.MANAGE` | **`NPS.MANAGE`** (`nps_summary` · إدارة الاستبيان) |

**الأثر اليوم صفر** لأن `CENTER_ADMIN` يملك الثلاثة وحده. **والخطر أن حارسًا يذكر
رمزًا غير الذي تفحصه القاعدة هو جوابٌ ثانٍ عن نفس السؤال** — فيوم يُمنَح
`CATALOG.MANAGE` لدور آخر (وهو أوّل ما يُطلَب: «من يحدّث الأسعار»)، تُفتَح شاشتا
الإعدادات والاستبيان لمن لا يستطيع فعل شيء فيهما.

## C-14 · خمسة أنواع إشعار معرَّفة ولا يرسلها شيء — `NEW`

`SESSION_STARTED` · `STAFF_APPOINTMENT_CANCELLED` · `STAFF_ENROLMENT_NEW` ·
`STAFF_REQUEST_NEW` · `STAFF_PLAN_APPROVED` — راجع 10 للدليل.
**وأربعةٌ منها هي كلّ ما يُفترض أن يبلّغ الموظّف بعملٍ جديد وصل**، فميزة `0089`
(«الوصول إلى زميل») **مبنيّة ثلثًا**: طلبُ التحاقٍ جديد وطلبُ وليِّ أمر جديد
**لا يُشعران أحدًا**.

## C-15 · الصيانة مُجدَّلة بطريقين بلا قفل — `NEW`

`deploy/server/cron-install.sh` يثبّت `cron` على المضيف · و
`deploy/compose/docker-compose.yml:170` يشغّل حاوية `hbh-maintenance` في حلقة.
**وإن عملا معًا، تتزاحم تشغيلتان** — و`run_maintenance` بلا قفل استشاري.
> والدرس المسجَّل في المشروع: «**وهذا يكشف التزاحم ولا يمنعه. المنع قفلٌ، وهو
> قرار المالك.**» — والكشف هنا **غير موصول** أيضًا (C-17).
`OQ-42` يقرّر أيّهما الحقيقي.

## C-16 · ثلاثة عروض صحّة بلا قارئ — `KNOWN` (`HBH-008`)

`v_backup_health` · `v_maintenance_health` · `v_sms_health` — **صفر مستهلك** في
`api/` و`web/`. و`/api/v1/ops/health` يقرأ `v_api_health` **وحده**
(`store/opslog.go:93`).
**فمُجدولٌ يموت، ونسخةٌ احتياطية تتأخّر، وصندوق صادر يتوقّف — كلّها تظهر في مكان
لا يفتحه أحد.** والقاعدة في `docker-compose.yml` تقول: «`run_maintenance` تسجّل
كل تشغيلة، **فمُجدولٌ يموت يظهر في `v_maintenance_health` بدل أن يُلاحَظ بعد
شهر**» — وهو صحيحٌ في القاعدة وغير محقَّق في المنتج.

---

# 🟡 MEDIUM

## C-17 · حالات الكيانات الأساسية بلا آلة — `NEW`

`users.status` · `children.status` · `therapists.status` · `plan_goals.status` ·
`cameras.status` · `child_packages.status` — **`CHECK` وحده**.
فـ`GRADUATED → ACTIVE` و`RESIGNED → ACTIVE` و`MET → OPEN` كلّها مسموحة بلا سببٍ
مسجَّل. **انطباقٌ ناقص للقاعدة ٥ على الكيانات نفسها.** راجع `OQ-16` · `OQ-17`.

## C-18 · «مقيَّدة بجدول تاريخ» تنطبق على ٢ من ١٢ — `NEW`

جدولا تاريخٍ فقط: `appointment_status_history` · `session_status_history`.
وبقيّة الآلات (الخطة · الفاتورة · الطلب · الالتحاق · التقييم · الانتظار) تعتمد
`audit_log` العامّ — **وهو ليس نفس الشيء**: يحمل الصفّ كاملًا لا
`from_status → to_status` باسمه، **ولا يحمل `reason`**.
**والفرق عمليّ:** «لماذا أُلغي هذا الموعد» يُجاب من جدول التاريخ، و«لماذا رُفض
هذا الطلب» **يُستخرَج من jsonb**.
> والقاعدة نصًّا تقول «مقيَّدة بجدول تاريخ». **فإمّا تُوسَّع الجداول، وإمّا يُصحَّح
> نصّ القاعدة** — لأن قاعدةً تُقرأ أوسع مما تُفرَض تُبنى عليها افتراضات.

## C-19 · تعليق مسارٍ ينقض الكود الذي يحمله — `NEW`

`web/ops/src/app/app.routes.ts:350-351` يقول:
> «NOT user administration: users, roles and permissions are five tables with
> **no endpoint of any kind**. Requested.»

و`features/access/access.ts` ينادي **تسعة مسارات** منها (`GET/POST /users` ·
`PATCH` · `DELETE` · `PUT .../roles` · `POST .../password-setup` ·
`POST .../documents` · `GET/POST .../photo` · `PUT /roles/{code}/permissions`).
> **وهذا هو بالضبط نمط العيب الذي صُحِّح في رأس `CLAUDE.md`:** «نصّ هذه الفقرة كان
> يقول *النظام الحالي هو الإنتاج* حتى بعد القرار… **أي جلسة تقرأ من أعلى كانت
> تصطدم بالجملة القديمة أوّلًا وتتصرّف بها**.»

## C-20 · حارس `/access` أوسع من أفعاله — `NEW`

الحارس `PORTAL.VIEW` (يملكه **كل** دور) · والأفعال تطلب `USER.MANAGE` و
`STAFF.PII`. الشاشة تحسب `canManage` وتخفي الأزرار، **والقائمة تُطلَب على أي
حال** فتُجاب بفراغ أو رفض.
وتجمع الشاشة سطرين مختلفَي الحساسية: **إدارة الحسابات** و**ملفّات الموظّفين
الشخصية**. `OQ-19`.

## C-21 · هجرتان بلا `.down.sql` — `NEW`

`0122_enrolment_mobile_limit` · `0126_portal_mobile_conflict` — **الوحيدتان من
١٢٩**. وكلتاهما تُصلح ثغرة (حدُّ الجوال المطفأ · تصادم الجوال عند منح الحساب)،
فقد يكون الغياب مقصودًا. **وغير مسجَّل بسببه** — وهو ما تطلبه بقيّة السكيما.
`OQ-23`.

## C-22 · `v_case_timeline` · `v_audit_trail` · `v_guardian_portal_status` · `v_attachment_index` بلا قارئ — `KNOWN` جزئيًّا

`v_case_timeline` مسجَّل (`HBH-045`). **والثلاثة الباقية غير مسجَّلة**، و
`v_guardian_portal_status` هو **حرفيًّا** العرض الذي يجيب سؤال الحلقة المقطوعة في
F-07: «هل هذه الأسرة تملك حسابًا؟» — **القاعدة تعرف الجواب ولا شاشة تسأله.**

## C-23 · قاعدة اعتماد الخطة أُسقطت ولم تُستبدَل — `KNOWN` (`HBH-044`)

`0088` أضاف `HB210` · و`0090` أسقطها لأنها رفضت انتقالًا كان يعمل · والرمز
**متقاعد**. فـ`DRAFT → ACTIVE` اليوم **بلا اشتراط معتمِد**. `OQ-27`.

## C-24 · قاعدة في ملفّ لا يعمل — `NEW`

`deploy/server/hbh.nginx.conf` يحمل في رأسه «بلوك الموقع بلا `proxy_pass`، ولا
تُضِف مسار API» — **وهو ليس ما يعمل في النشر الحالي** (`site.native.mjs` و
`web.native.mjs` هما العاملان).
> **وهذا هو الدرس المسجَّل حرفيًّا:** «القاعدة كانت مكتوبة في ملفّ nginx **الذي لا
> يعمل**، لا في الخادم الذي يعمل. **القاعدة تُكتب حيث تُنفَّذ.**»
> وقد نُقلت إلى `site.native.mjs` (قائمة السماح) — **ونسخة nginx باقية تحمل
> القاعدة القديمة**، فقارئها الأوّل يظنّها نافذة.

## C-25 · `p00` حمراء وجولة القبول الكاملة لم تُعَد — `KNOWN` (روادماب)

`docs/00-roadmap.md:85` (آخر تحديث **2026-09-10**): «`P00 — المعايير العامّة`
🔴 **حمراء 2026-09-10** — كانت ٢٢ فحصًا · ٠ فشل حتى 09-03. **تنمو مع السكيما،
والسكيما سبقتها**».
و`:104`: «الإجمالي وقت القبول (2026-09-03): ١٦٧٥ فحصًا · ٠ فشل — **ولم يُعَد
تشغيلها كاملةً منذ ذلك اليوم**. الرقم تاريخيّ، لا حالة.»
**ومنذ ذلك اليوم نزلت ٩٩ هجرة** (`0030`→`0129`).

## C-26 · `web/ops` بلا اختبار وحدة · و`ng test` يخرج بصفر بلا متصفّح — `KNOWN` (`HBH-046` · `HBH-047`)

---

# ⚪ LOW

## C-27 · روادماب متأخّر بـ٤٩ هجرة — `NEW` (والبند السابق مسجَّل كـ`HBH-028` وقد أُنجز)

`docs/00-roadmap.md` آخر تحديثه **2026-09-10** ويصف الكتل حتى `0080`.
**والشجرة عند `0129`.** والوثيقة نفسها تحمل السبب في رأسها: «بقيت هذه الوثيقة
سبعة أيام تقول *٢٩ هجرة* والشجرة عند ٨٠، فمرّت هجرتان غير مسجَّلتين وجدولان
يخرقان القاعدة ٣ **دون أن يراهما أحد**. **وثيقة حالة بلا موعد تحديث هي وثيقة
تشيخ بصمت.**»
**والموعد موجود الآن** («مراجعة نهاية كل سبرنت، وعند كل قبول مرحلة») — والفجوة
٤٩ هجرة تعني أن سبرنتًا لم يُراجَع بعد.

## C-28 · رمزان موثَّقان لم يُرفَعا قطّ — `NEW`

`HB062` (رأس `0009`: «توكن البثّ مجهول أو منتهٍ أو مسحوب») و`HB070` (رأس `0010`:
«حجز مغلق مشوَّه») — **لا `RAISE` يستعملهما**.
و`0052` يسمّي `HB062` «الفجوة» ويقرّر **ألّا** يملأها. **ورمزٌ في رأس هجرة بلا
وجود في أي دالّة وعدٌ لم يُنفَّذ** — وخطره أن كاتبًا لاحقًا يقرؤه فيحسبه مأخوذًا
أو حرًّا. **وهو الوجه الآخر لـC-04.**

## C-29 · `useFixtures: true` افتراضًا وتعليقه متقادم — `NEW`

`web/shared/src/config/app-config.ts:17` يقول «True **while the API is not built
yet**» — **والـAPI مبنيّ منذ 2026-09-03**، والافتراض في `DEFAULT_HBH_CONFIG` ما
زال `true`.
**والأثر صفر** لأن التطبيقين يكتبانه `false` صراحةً (`portal/app.config.ts:38` ·
`ops/app.config.ts:25`). **والخطر أن افتراضًا يُقرأ كحالةٍ حاضرة**، وأي تطبيق
ثالث يأخذ الافتراض يعمل على بيانات ثابتة **ويبدو سليمًا**.

## C-30 · `therapist-qualifications` له CRUD كامل ولا شاشة — `NEW`

مورد في `store.Resources()` بستّة مسارات، **وليس في أي `Spec` ولا شاشة**.
`OQ-22`.

## C-31 · حالة الطلب معرَّفة في مكانين — `NEW`

`ck_req_status CHECK (status IN ('NEW','ACCEPTED','REJECTED'))` **و**
`lookup_values` تحت `REQUEST_STATUS` بنفس الثلاث.
**والثاني لا يقيّد شيئًا** — مصدرٌ ثانٍ لمفردات لا يُفرَض. راجع 19 · D-06.

---

## جدول القيادة — مرتَّبًا بالخطورة

| # | البند | الصنف | جديد؟ | الميزة | السؤال العالق |
|---|---|---|---|---|---|
| C-01 | رصيد الباقة لا يُخصَم | 🔴 | NEW | F-35 · F-25 | `OQ-02` |
| C-02 | `REPORT.WRITE` تضيع على رَبنة | 🔴 | NEW | F-32 | — |
| C-03 | الأصل بلا كاتب للأمام | 🔴 | NEW | F-09 · F-12 | `OQ-05` |
| C-04 | `HB240` بقاعدتين | 🔴 | NEW | F-19 · F-10 | `OQ-26` |
| C-05 | صيغة الجوال تحجب ميزة مقبولة | 🔴 | NEW | F-01 · F-08 | — |
| C-06 | الرفع يكتب في git + OneDrive | 🟠 | NEW | F-42 | — |
| C-07 | `guardians.mobile` بلا تفرّد | 🟠 | NEW | F-12 | `OQ-43` |
| C-08 | `guardians.user_id` قابل للكتابة | 🟠 | NEW | F-07 · F-12 | — |
| C-09 | طفلان لطفل واحد | 🟠 | NEW | F-09 | `OQ-48` |
| C-10 | أرشفة وليّ الأمر لا تمسّ حسابه | 🟠 | NEW | F-12 | `OQ-44` |
| C-11 | `meetings` بلا آلة ولا تاريخ | 🟠 | NEW | F-36 | — |
| C-12 | `DAILY` مزوّد مجهول | 🟠 | NEW | F-36 | — |
| C-13 | حارسان بصلاحية خاطئة | 🟠 | NEW | F-41 · F-43 | — |
| C-14 | ٥ أنواع إشعار ميّتة | 🟠 | NEW | F-37 | `OQ-30` |
| C-15 | مُجدولان بلا قفل | 🟠 | NEW | F-44 | `OQ-42` |
| C-16 | ٣ عروض صحّة بلا قارئ | 🟠 | KNOWN | F-44 | — |
| C-17 | حالات أساسية بلا آلة | 🟡 | NEW | F-10 · F-16 | `OQ-16`·`OQ-17` |
| C-18 | «جدول تاريخ» ٢ من ١٢ | 🟡 | NEW | القاعدة ٥ | — |
| C-19 | تعليق ينقض كوده | 🟡 | NEW | F-05 | — |
| C-20 | حارس `/access` أوسع من أفعاله | 🟡 | NEW | F-05 | `OQ-19` |
| C-21 | هجرتان بلا `down` | 🟡 | NEW | — | `OQ-23` |
| C-22 | ٤ عروض بلا قارئ | 🟡 | جزئيًّا | F-07 · F-44 | `OQ-25` |
| C-23 | قاعدة اعتماد الخطة أُسقطت | 🟡 | KNOWN | F-28 | `OQ-27` |
| C-24 | قاعدة في ملفّ لا يعمل | 🟡 | NEW | النشر | — |
| C-25 | `p00` حمراء · الجولة لم تُعَد | 🟡 | KNOWN | كل شيء | — |
| C-26 | `ops` بلا اختبار · `ng test` كاذب | 🟡 | KNOWN | الواجهة | — |
| C-27 | روادماب متأخّر ٤٩ هجرة | ⚪ | NEW | الحوكمة | — |
| C-28 | `HB062` · `HB070` وعدان | ⚪ | NEW | — | — |
| C-29 | `useFixtures` افتراضًا | ⚪ | NEW | الواجهة | — |
| C-30 | مورد بلا شاشة | ⚪ | NEW | F-16 | `OQ-22` |
| C-31 | حالة الطلب في مكانين | ⚪ | NEW | F-40 | — |

---

## ما لم أجده — نتائج سلبية تستحقّ التسجيل

| ما فُحص | النتيجة |
|---|---|
| مسارات API مكرَّرة تؤدّي نفس الوظيفة | **لا شيء** — الاصطلاح متّسق تمامًا |
| جداول مكرَّرة أو legacy في السكيما | **لا شيء** — ولم يُحذف جدول في أي هجرة `up` |
| أرقام هجرات مكرَّرة | **صفر** — و`db.sh` يرفض ويقف |
| هجرات لا تسجّل رقمها | **صفر** — (كانت اثنتان ورُقِّعتا) |
| قاعدة عمل في Go بلا نظير في القاعدة | **لا شيء** — القاعدة ٢ محترَمة |
| أعمدة بلا منطقة زمنية | **صفر** — والقبول يفشل على واحدة |
| عرضٌ بلا `security_invoker` | **صفر** |
| `SECURITY DEFINER` بلا `search_path` مثبَّت | **صفر** |
| اعتماديّات دائرية بين الميزات | **لا شيء** |
| شاشات مكرَّرة داخل تطبيق واحد | **لا شيء** — و`/appointments`/`/my-day` مقصودتان وموثَّقتان |
| ثغرة حقن SQL | **لا شيء** — كل مصطلح بارامتر مربوط |

**هذه ليست حاشية.** نظامٌ بـ١٢٩ هجرة و٢٩٦ مسارًا بلا مسار مكرَّر ولا جدول مهجور
**نتيجةُ انضباطٍ مقصود**، وهي ما يجعل البنود أعلاه قابلةً للإصلاح فردًا فردًا بدل
أن تكون أعراضًا لفوضى.
