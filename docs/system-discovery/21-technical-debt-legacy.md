# 21 — TECHNICAL DEBT & LEGACY

---

## ١ · الإرث — ما بقي من النظام القديم

| البند | الحجم | الحالة |
|---|---|---|
| **`html/`** | ٢٠ ملفّ HTML + `README-APEX.md` بـ**٤٩ كيلوبايت** + `hbh-ops.js` + ٤ ملفّات CSS | 🔴 **إرث أوراكل/APEX** — والمحرّك متوقّف بقرار `D-26` منذ 2026-09-03 |
| **`html/app/`** | نموذج بصريّ · **١٢ شاشة قابلة للنقر** · يُعرَض على العميل | 🟡 **له استعمال قائم** |
| **`html/apex-region/`** | أجزاء APEX | 🔴 لا استعمال |
| **`html/html/app/`** | **مجلّد متداخل بنفس الاسم** | 🔴 على الأرجح نسخةٌ غير مقصودة |
| **`website/`** | نسخة تصميم · README يقول إن كل المحتوى Placeholder | 🟠 مرجع تصميم — راجع 19 · D-07 |
| **`docs/04-ops-api-needs.md` · `04-p10-contract.md` · `05-p11-contract.md`** | عقود مرحلية | 🟠 `INDEX.md` يصنّفها «**مرحلية ومنتهية — مرشَّحة للأرشفة**» |
| **`docs/INDEX.pdf` · `OWNERS.pdf` + ٧ ملفّات PDF في `business-analysis`** | ~٤ ميجابايت | 🟡 مخرجاتٌ مولَّدة داخل الشجرة |

**الحكم:** `html/README-APEX.md` وحده ٤٩ كيلوبايت من وثائق نظامٍ **لا يعمل**.
> **وقاعدةٌ مكتوبة في مكانٍ لا يُنفَّذ لا تمنع شيئًا** — وهي نفس عائلة C-24
> (`hbh.nginx.conf`). والأخطر أن **من يقرأ `html/` أوّلًا يبني على معمارٍ متوقّف**.
`OQ-63`.

## ٢ · دينُ الحوكمة

| # | البند | الدليل |
|---|---|---|
| T-01 | **`p00` حمراء** | `docs/00-roadmap.md:85` (2026-09-10): «كانت ٢٢ فحصًا · ٠ فشل حتى 09-03. **تنمو مع السكيما، والسكيما سبقتها**» |
| T-02 | **جولة القبول الكاملة لم تُعَد منذ 2026-09-03** | «١٦٧٥ فحصًا · ٠ فشل — **ولم يُعَد تشغيلها كاملةً منذ ذلك اليوم**. الرقم تاريخيّ، لا حالة.» **و٩٩ هجرة نزلت منذها** |
| T-03 | **٩٩ هجرة بلا مرحلة قبولٍ مرقَّمة** | الروادماب يسجّل ستّ كتل حتى `0080`، **والشجرة عند `0129`** |
| T-04 | **الروادماب متأخّر ٤٩ هجرة** | آخر تحديث 2026-09-10 · يصف حتى `0080` |
| T-05 | **عقد الـAPI غير مقابَل** | `INDEX.md` يصنّف `02-api-contract.md` ⬜ «لم يُقابَل بـ١٠٧ مسارًا» — **والعدد اليوم ١١٨ صريحًا** |
| T-06 | **باكلوجان** | `INDEX.md:20` يقول «~~يوجد باكلوجان~~ — ✅ **حُسِم 2026-09-10**»: `pm/02` هو المرجع بقرار المالك، و`business-analysis/02` صار سجلَّ ما ينتظر ردّه |
| T-07 | **مجموعتا حالات اختبار** | `tests/test-cases/` (**٢٣٥+ حالة**) مقابل `business-analysis/05-test-cases.csv` (**١٩١ حالة**) — `HBH-034` |
| T-08 | **ثلاثة جروداتٍ للشاشات** | `figma/screens.md` (٤٤) · `html/app` (١٢) · الحيّ (٥٦) — `HBH-036` |

> **والملاحظة الإدارية أهمّ من البنود، وهي مكتوبة في الباكلوج نفسه:**
> «كل كتلة بُنيت جيّدًا، **ولا واحدة منها لها اختبار قبول مرقَّم ولا سطر في هذه
> الوثيقة قبل اليوم. ما لا يُسجَّل لا يُراجَع** — ولهذا صارت `p00` تكتشف بعد شهر ما
> كان يُكتشف في نفس اليوم.»

## ٣ · دينُ الاختبار

| # | البند | الأثر |
|---|---|---|
| T-09 | **`web/ops` بلا اختبار وحدة واحد** (`HBH-047`) | أكبر تطبيق في المشروع (٢٨ شاشة · `ops_handlers.go` بـ٧٦ كيلوبايت) |
| T-10 | **`ng test` يخرج بصفر بلا متصفّح** (`HBH-046`) | `Cannot start ChromeHeadless — running as root without --no-sandbox`. **صفر اختبار نُفِّذ والأمر يقول نجح.** والعلاج `CHROME_BIN` يشير إلى غلاف يمرّر `--no-sandbox` |
| T-11 | **اختبارات البوّابة تعمل من داخل حاوية واحدة بعينها** | `node_modules` على القرص مبنيّ لويندوز، فـ`esbuild` يموت في أي حاوية لينكس أخرى |
| T-12 | **لا اختبار قبولٍ يمشي الطريق من الصفر** (`HBH-015`) | **وهو سبب نجاة الحلقات المقطوعة من ١٦٧٥ فحصًا** |
| T-13 | **CI بوظيفة واحدة** (`HBH-006` يطلب ثلاثًا) | `deploy/ci/run.sh` — «بوّابة واحدة لا تستطيع أن تبلّغ نجاحًا عن عمل لم تفعله» |

## ٤ · دينُ السكيما

| # | البند | الحكم |
|---|---|---|
| T-14 | **هجرتان بلا `.down.sql`** (`0122` · `0126`) | 🟡 قد يكون مقصودًا · **وغير مسجَّل** (18 · C-21) |
| T-15 | **`0091` يُدرج منحةً تقرأ جدولًا تملؤه البذور** | 🔴 18 · C-02 |
| T-16 | **`meetings` بلا آلة ولا تاريخ** | 🟠 18 · C-11 |
| T-17 | **٦ أعمدة حالة بلا آلة** | 🟡 18 · C-17 |
| T-18 | **جدولا تاريخ من ١٢ آلة** | 🟡 18 · C-18 |
| T-19 | **رمزان موثَّقان لم يُرفَعا** (`HB062` · `HB070`) | ⚪ 18 · C-28 |
| T-20 | **`HB240` بقاعدتين** | 🔴 18 · C-04 |
| T-21 | **أعمدة `0129` بلا كاتب ولا قارئ** | 🔴 18 · C-03 |
| T-22 | **٧ عروض بلا قارئ** | 🟠 18 · C-16 · C-22 |
| T-23 | **٥ أنواع إشعار ميّتة** | 🟠 18 · C-14 |
| T-24 | **`hbh.branches` مبذور بفرع · بلا شاشة ولا مسار** | 🟡 `OQ-01` · `OQ-24` |
| T-25 | **`storage_kind = 'OBJECT'` عمودٌ معدٌّ بلا منفّذ** | ⚪ خيارٌ مستقبليّ موثَّق |
| T-26 | **`'DAILY'` مزوّد في `CHECK` بلا تنفيذ** | 🟠 18 · C-12 |

**والأثر التراكمي للهجرات:** `0079`–`0084` **ستّ هجرات لجدولين** — `0079`/`0080`
أنشأتا `family_messages`/`family_message_reads` **بأسلوب مخالف للسكيما كلّها** (بلا
رأس · بلا قواعد مرقّمة · `PRIMARY KEY` مضمَّنًا · `uuid` حيث لا يستعمله أحد) وبلا
أعمدة تدقيق ولا حذف ناعم — **خرقٌ للقاعدة ٣ لا مجرّد فشل فحص** — ثم لزم
`0081` (الاصطلاحات) · `0082` (التدقيق) · `0084` (الحذف الناعم) لترقيعهما.
> **وكانتا مطبَّقتين على القاعدة ولا تسجّلان رقمهما في الدفتر، ولا `IF NOT EXISTS`
> فيهما** — فالتشغيلة التالية كانت ستصطدم بـ`CREATE TABLE` على جدول قائم **وتُسقط
> `migrate` كلّه**: معطَّلًا لا صامتًا.
> **والدرس الإداري أهمّ من البند:** «خرجتُ من *قرأتُ القاعدة* إلى *أعرف ما في
> الملفّ* بلا فحص بينهما. **ما لم أشغّله أو أقرأه بعيني ليس معلومة، مهما كان
> معقولًا.**»

## ٥ · دينُ الواجهة

| # | البند |
|---|---|
| T-27 | **صيغة الجوال بثلاث نسخ مضمَّنة** — 19 · D-09 · 18 · C-05 |
| T-28 | **`useFixtures: true` افتراضًا وتعليقه متقادم** — 18 · C-29 |
| T-29 | **`fixture-portal-api.ts` · `fixture-auth-api.ts`** — طبقة بيانات ثابتة كاملة لا تُستعمل في أي بناء. **مفيدة للاختبار · ودينٌ لو تباعدت عن العقد** |
| T-30 | **`dev-accounts.ts` / `dev-accounts.prod.ts`** في `web/ops` — اختصارات دخولٍ للتطوير، والإنتاجيّ فارغ. **يحتاج فحصَ نشرٍ يؤكّد أن المنشور هو الفارغ** |
| T-31 | **١١ ملاحظة واجهة مفتوحة** (`HBH-035`) |
| T-32 | **`web/ops/src/assets/i18n;W`** — مجلّد باسمٍ فيه فاصلة منقوطة، **فارغٌ وغير متعقَّب في git** (فُحص). من قصّ مسارٍ فاشل على الأرجح — ⚪ **أثره صفر ومرشَّح للحذف** |

## ٦ · دينُ النشر

| # | البند |
|---|---|
| T-33 | **`hbh.nginx.conf` يحمل قاعدةً ولا يعمل** — 18 · C-24 |
| T-34 | **مُجدولان للصيانة بلا قفل** — 18 · C-15 |
| T-35 | **رفع أصول الموقع يكتب في شجرة git وداخل مجلّد مزامنة** — 18 · C-06 |
| T-36 | **TLS غير مثبَّت على المسارات الثلاثة** (`HBH-004`) |
| T-37 | **`TRUST_PROXY=true` غير مضبوط** (`HBH-005`) — فـIP العميل في السجلّ هو IP النفق |
| T-38 | **لا استعادة فعلية مجدولة** (`HBH-009`) — **ونسخةٌ لم تُستعَد قطّ ليست نسخة** |
| T-39 | **حادثة نشرٍ غير مغلقة بأدلّة** (`HBH-037` 🔴) |

## ٧ · مجلّدات وملفّات مرشَّحة للتنظيف

| المسار | لماذا | قبل الحذف |
|---|---|---|
| `web/ops/src/assets/i18n;W` | اسمٌ فيه `;` | تأكيد أنه ليس مستعملًا |
| `html/html/` | مجلّد متداخل مكرَّر | كذلك |
| `html/apex-region/` | APEX متوقّف | قرار المالك |
| `docs/04-ops-api-needs.md` · `04-p10-contract.md` · `05-p11-contract.md` | مرحلية منتهية | أرشفة لا حذف |
| `web/dist/` · `web/.angular/` · `web/portal/.angular/` | مخرجات بناء | **مستبعَدة في `.gitignore` بالفعل** ✅ |
| `.claude/worktrees/adoring-antonelli-085781/` | شجرة عمل لجلسة أخرى | **لا تُلمس** — قد تكون جلسةٌ تعمل منها الآن |

> ⚠️ **ولا يُحذف شيء من هذه القائمة في هذه المرحلة.** والقاعدة المسجَّلة:
> **«وقبل حذف صفّ لا تعرفه، اسأل سجلّ التدقيق»** — وهي تسري على الملفّات:
> `.claude/worktrees/` شجرةٌ قد تكون جلسةٌ أخرى تبني منها **الآن**.

## ٨ · ما ليس دينًا — وقد يُقرأ كذلك

| البند | لماذا ليس دينًا |
|---|---|
| **٩٩ رمز رفض** | كلٌّ يسمّي قاعدةً · و`business_refusal_test.go` يثبت الخاصّية |
| **١٩٥ سياسة RLS على ٨٧ جدولًا** | القاعدة ٤ · ولا بديل لها |
| **١٢٩ هجرة** | سجلّ قرارات، وكل رأسٍ يحمل سببه وثمنه |
| **~١٧٠ دالّة** | القاعدة ٢ — البديل قواعدٌ في Go |
| **رؤوس هجرات بمئات السطور** | «كل سطر هنا كلّف دورة تشغيل» — **هذه أثمن وثيقة في المشروع** |
| **`convention_exemptions` بـ٣٢ صفًّا** | **الاستثناء مسجَّل بسببه، وهو ما يجعله استثناءً لا خرقًا** |
| **`web/shared` بـ١٨ ملفًّا** | إعادة استعمال حقيقية |
| **مجموعات اختبار بـ١٧ ألف سطر** | و«الحكم يُطبع دائمًا، ثم — وبعده فقط — يُرفع الاستثناء» |

---

## ٩ · الدين مرتَّبًا بالمخاطرة

| الرتبة | البند | لماذا أوّلًا |
|---|---|---|
| ١ | **T-02 · الجولة الكاملة لم تُعَد** | **كل حكمٍ على النظام اليوم مبنيّ على قياسٍ عمره تسعة أيام و٩٩ هجرة** |
| ٢ | **T-01 · `p00` حمراء** | **الحارس الوحيد المتبقّي على ٩٩ هجرة نزلت بلا مرحلة** |
| ٣ | **T-15 · `0091`** | ميزةٌ كاملة تختفي على رَبنة، **والقاعدة تبدو سليمة** |
| ٤ | **T-20 · `HB240`** | رفضٌ يصل الشاشة بالكلمة الخطأ، **واللوج يقرأ محاولةً أمنيّة لا وجود لها** |
| ٥ | **T-35 · الرفع في شجرة git** | **بيانات لا تُسحَب من تاريخ git** |
| ٦ | **T-36 · TLS** | كل شيء آخر مبنيٌّ على أن النقل آمن |
| ٧ | **T-38 · لا استعادة مجدولة** | **نسخةٌ لم تُستعَد ليست نسخة** |
| ٨ | **T-09 · `ops` بلا اختبار** | ٢٨ شاشة و٧٦ كيلوبايت من المعالِجات |
| ٩ | **T-04 · الروادماب متأخّر** | **«وثيقة حالة بلا موعد تحديث هي وثيقة تشيخ بصمت»** — والموعد موجود والفجوة ٤٩ هجرة |
| ١٠ | **T-05 · العقد غير مقابَل** | من يبني على العقد يبني على ١٠٧ مسارًا من ١١٨ |

---

## OPEN QUESTIONS

| # | السؤال | الأثر |
|---|---|---|
| **OQ-63** | `html/` — يُؤرشَف خارج الشجرة (مع `README-APEX.md`)، أم يبقى؟ والنموذج البصري `html/app/` **له استعمال قائم** فيُفصَل | 21 · 19 · D-07 |
| **OQ-64** | ملفّات PDF المولَّدة داخل الشجرة (~٤ ميجابايت) — تبقى متعقَّبة أم تُبنى عند الطلب؟ | 21 |
| **OQ-65** | متى تُعاد جولة القبول الكاملة، ومن يملك القرار بأنها شرطٌ قبل أي ميزة جديدة؟ | T-02 · 22 |
