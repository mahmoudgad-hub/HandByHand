# UX-PROBLEMS — مراجعة الوضع الحالي

**2026-09-14 · تحليل فقط، لا تنفيذ معتمد.** القسم الأعلى هو المرجع الحالي. التحليل السابق محفوظ في آخر الملف للأرشفة ولا يمثل العمل المتبقي.

**العدد الحالي: 49 شاشة تطبيق = 31 للمركز + 18 للبوابة. المستهدف: 47 = 31 + 16، بدمجين مقترحين في البوابة.** نعد كل route يرسم شاشة بما فيه الدخول والرفض والطباعة ونمطا إنشاء/تحرير التقرير؛ لا نعد shell أو redirects أو wildcard. لذلك هذا عدد مسارات شاشات وليس عدد المكونات أو بنود القائمة. الموقع العام site يضيف صفحتين، فيصير الإجمالي الموسع 51 → 49. website/index.html وhtml/ وfigma/ مراجع منفصلة لا يثبت وجودها أنها تشغيل فعلي.

**صفر حالات مستخدمة كصفحات انتظار مستقلة.** /live أداة مشاهدة قائمة، لا حالة متنكرة في شاشة. /my-day كان فلترًا بمسار وأصبح redirect بالفعل. لا صفحات انتظار ولي أمر/إسناد أخصائي في routes الحالية.

**حدود الإثبات:** جرد ساكن لكل routes ومصادر النماذج والأفعال وAPI، وتحليل للمنطق في Go والمهاجرات المتاحة، مع مشاهدة حية للدخول ولوحة المدير والمهام. لم تُختبر كل شاشة وكل دور عمليًا، ولم تُنفذ كتابة أعمال أو إرسال رسائل. تعذرت قراءة 0005_scheduling.up.sql و0006_plans_and_notes.up.sql بصلاحيات البيئة؛ لا ندعي اكتمال تدقيق SQL. تشغيل DB الحي قد يختلف عن ملفات الهجرة. تفاصيل الأدلة في [UX-AUDIT-SOURCE-INDEX.md](UX-AUDIT-SOURCE-INDEX.md)، والمسارات المحسوبة في [UX-AUDIT-ROUTES.json](UX-AUDIT-ROUTES.json).

## أكبر عشر مشكلات قائمة

الأولوية تقدير أثر المنتج. الأدلة الساكنة ليست تجربة فشل نفذت على بيانات حية.

| ID | Category/Priority | Problem | Evidence | Impact | Recommendation | Source |
| --- | --- | --- | --- | --- | --- | --- |
| R01 | P1 — Flow/Contract | مرحلة التقييم لا تكتمل بالعقد الحالي | 0110 تشترط assessment_at عند ASSESSMENT_BOOKED؛ EnrolmentUpdate يقبل status/note_ar فقط، وDayApi لا يرسل التاريخ | قد يرفض الانتقال لطلب لم يسجل تاريخه؛ اختيار الحالة لا ينشئ Appointment | توصية backend منفصلة لربط التاريخ والموعد؛ لا تضف حقلًا غير مدعوم أو نجاحًا وهميًا | db/migrations/0110_enrolment_assessment_sms.up.sql:73; api/internal/store/enrolment.go:259 |
| R02 | P1 — Task | العدد في المهام جزئي دون بيان كاف | list يقرأ أول 100؛ children/caseload أول 200؛ total يُسقط، والقالب يحذر من failed فقط | مهام تسقط؛ غياب caseload من العينة قد يصنع مهمة إسناد كاذبة | paging كامل أو incomplete صريح؛ لا استنتاج نفي إسناد من بيانات جزئية | web/ops/src/app/core/tasks/task-catalog.ts:106; features/tasks/tasks.html |
| R03 | P1 — Detail | ملف الأسرة قد يخفي طلبات قديمة | loadParts يجلب أول 100 enrolments/requests ثم يفلتر في الذاكرة | أسرة لها طلبات تبدو بلا طلبات | قراءة صفحات كاملة أو توضيح الجزئية؛ قارئ أسرة مخصص توصية لاحقة | web/ops/src/app/features/guardian/guardian-detail.ts:181 |
| R04 | P1 — Task | الجلسة المفتوحة أمس تختفي من طابور الإغلاق | SESSION_CLOSE يرسل date:today وIN_PROGRESS | عمل متأخر يغيب مع تغير اليوم | نطاق from/to معلوم ومعلن مع paging؛ لا ادعاء شمول all-time | web/ops/src/app/core/tasks/task-catalog.ts |
| R05 | P2 — Action | مهمة الإسناد تفقد سياق الطفل | CTA إلى therapists?tab=caseload بلا child_id | بحث وإدخال جديدان واحتمال اختيار طفل آخر | افتح ملف الطفل وحوار الإسناد القائم مسبوق التعبئة | web/ops/src/app/core/tasks/task-catalog.ts; features/child/child-profile.ts |
| R06 | P1 — Navigation | الرابط العميق للموعد محدود باليوم وأول صفحة | drawer بلا row/child يقرأ أول 100 موعد اليوم | reload لرابط موعد قديم قد يبدو not found | احتفظ بنطاق تاريخ وسياق آمن وابحث مع pagination؛ لا تعد غياب العينة 404 مثبتًا | web/ops/src/app/features/drawer/record-drawer-host.ts |
| R07 | P1 — Flow | قبول تغيير الموعد لا يغير الموعد | decide_request يعدل الطلب فقط بقصد | التباس بين قبول الطلب وتنفيذ الجدولة | نص صريح ورابط حجز سياقي؛ لا mutation تلقائية أو حالة تجارية جديدة | db/migrations/0099_center_ownership_guards.up.sql:178 |
| R08 | P2 — Navigation | خدمات الأسرة غير متاحة دائمًا من شريط الهاتف | خمسة tabs؛ reports/billing في webNav العريض، communications route مستقل | ضعف اكتشاف التقارير والفواتير والرسائل | دمج التقارير في المتابعة والرسائل في الطلبات مع قائمة المزيد | web/portal/src/app/layout/shell/shell.ts; app.routes.ts |
| R09 | P2 — Form | الخطط والأهداف تحرر بأرقام مراجع | ResourceSpec يوضح أن أسماء العرض لا تحل إدخال IDs في المحرر | عبء فهم الطفل والخطة والهدف والقياس يدويًا | منتقيات أسماء مترابطة داخل سياق الطفل، بنفس CRUD وIDs | web/ops/src/app/core/resource/resource-spec.ts:42 |
| R10 | P2 — Naming/Evidence | الوثائق وبعض النصوص ثابتة ومتقادمة | التحليل القديم يقول لا Tasks ولا تفاصيل؛ login يقول 19 صلاحية؛ تعليقات route غير محدثة | تكليف تنفيذ مكرر وافتراضات أدوار خاطئة | هذه المراجعة مرجع؛ حقوق /me هي الحقيقة لا رقم ثابت | docs/UX-IMPLEMENTATION-STATUS.md; web/ops/src/app/app.routes.ts |

## Navigation / Screen / Status / Task

Navigation: R06/R08. Screen: R03/R09. Status: R01/R07. Task: R02/R04/R05. صفر صفحات انتظار مستقلة للحذف؛ القيم الحالية بالفعل فلاتر أو أعمدة مراحل.

## ما عولج بالفعل

فصل Notifications/Tasks، تجميع Sidebar، دمج my-day/services/site-team، تفاصيل الطلب وولي الأمر، أفعال ملف الطفل، drawers الموعد والفاتورة، وحراس users/settings/satisfaction. لا تعاد كمهام تنفيذ.

## توصيات خارج نطاق الواجهة

مراجعة R01 بالتفصيل في [UX-ENROLMENT-JOURNEY-REVIEW.md](UX-ENROLMENT-JOURNEY-REVIEW.md): التاريخ الناقص ليس المشكلة الوحيدة؛ إضافته لا تنشئ Appointment. يجب تنسيق الإصلاح مع OD-01 وOD-39 المسجلين بالفعل. تشغيل trigger المقابلة يضع SMS في الطابور ولا يثبت تسليمه، ولا يعيد إرسالًا لمجرد تعديل التاريخ دون تغير الحالة.

R01 يحتاج عقد backend متسقًا. Assessment وPaymentPlan/Installment موجودة في DB بلا routes تشغيلية مقابلة في server.go؛ لا شاشات وهمية. قراءة موعد منفرد ومصدر مهام جامع تحت RLS توصيتان لاحقتان. لا نمنح BILLING.MANAGE للاستقبال؛ BILLING.VIEW لا يكفي لمهمة تحصيل. أعداد الأنواع في الوثائق القديمة لا تعكس التنفيذ الحالي.


<details>
<summary>أرشيف التحليل السابق — غير معتمد كوصف للحالة الحالية أو تكليف تنفيذ</summary>

# UX Problems — Hand By Hand (new)

**المرحلة ٤.** مشاكل بنية التجربة المكتشفة من الجرد (`UX-SCREEN-INVENTORY.md`) وخريطة التدفّق (`UX-BUSINESS-FLOW-MAP.md`). كل مشكلة مرقَّمة وموصوفة **بالدليل من الكود**، ومصنَّفة، ومعها الحلّ المقترح الذي يُفصَّل في وثائق الهدف. **لا يُعدَّل كود في هذه المرحلة.**

**الشدّة:** 🔴 يمنع المستخدم من معرفة ما عليه فعله أو يكسر التدفّق · 🟠 يكلّف قفزات وتخمينًا · 🟡 اتّساق وتسمية.

---

## أ · Navigation Problems — القائمة والمسارات

| # | المشكلة | الدليل | الشدّة | الهدف |
|---|---|---|---|---|
| N-01 | **قائمة مسطّحة من 22 بندًا** بلا تجميع بالنطاق؛ الترتيب لا يعكس دورة العمل (الوارد → لوحة التحكم → الأطفال → المواعيد → يومي → الجلسات → الأخصائيون → خدمات الأخصائيين → الغرف → الخطط → التقارير → الفواتير → الطلبات → المحادثات → الالتحاق → الكتالوج → الرضا → الموقع → الفريق → السجلّ → الصلاحيات → الإعدادات) | `web/ops/src/app/layout/shell/nav.ts` (22 مدخلًا، صلاحية لكل مدخل، **صفر شارات**) | 🔴 | 8 مجموعات بالنطاق (`UX-TARGET-INFORMATION-ARCHITECTURE.md`) |
| N-02 | **لا عدّاد ولا شارة في القائمة** — المستخدم الواقف على «المواعيد» لا يعرف أن طلبًا جديدًا وصل. العدّ موجود في الداشبورد فقط | `nav.ts` بلا `badge`؛ العدّ في `dashboard-tiles.ts` عبر `limit=1 → total` | 🔴 | شارة «المهامّ» + شارة «الإشعارات» في القائمة |
| N-03 | **مدخلا قائمة لشاشة واحدة**: `/appointments` و`/my-day` نفس `APPOINTMENTS_SPEC` بفلتر `mine=1` وصلاحية مسار مختلفة | `app.routes.ts` (التعليق يشرح السبب: `SESSION.START` لا يحمل `APPOINTMENT.BOOK`) | 🟠 | مسار واحد `/appointments` حارسه أيّ من الصلاحيتين؛ «مواعيدي» فلتر افتراضي لصاحب `therapistId` |
| N-04 | **تبويبات صارت مسارات ومداخل قائمة**: `/therapist-services` (تبويب في الأخصائيين)، `/site/team` (تبويب في الموقع)، `/satisfaction` (نتائج الاستبيانات المعرَّفة في `/catalog`) | `app.routes.ts` التعليق على `therapist-services`: صار مسارًا لأن `navKey` أضاء البند الخطأ (`HBH-047`) | 🟠 | تبويبات داخل شاشتها؛ مدخل قائمة واحد لكل كيان |
| N-05 | **بندان متجاوران بنفس الأيقونة والصلاحية**: «طلبات أولياء الأمور» و«المحادثات» كلاهما `ic-chat` + `REQUEST.MANAGE` | `nav.ts` | 🟡 | مجموعة «التواصل» فيها: الطلبات · المحادثات · الإشعارات — بأيقونات مختلفة |
| N-06 | **مسارات لا تتبع النطاق**: `/access` اسمها «الصلاحيات والشاشات» وهي إدارة المستخدمين والموظّفين؛ `/catalog` تحمل استبيانات الرضا؛ `/children` تحمل أولياء الأمور؛ `/rooms` و`/site` و`/ops-log` و`/settings` أربعة مداخل إعدادات مبعثرة | `resource-spec.ts` (`NPS_SURVEYS_SPEC` تحت `/catalog`، `GUARDIANS_SPEC` أوّل تبويب في `/children`) | 🟠 | مجموعة «الإعدادات» تضمّ الكتالوج والغرف والموقع والرضا والنظام؛ «العملاء» تضمّ أولياء الأمور والمستفيدين |
| N-07 | **حارس مسار يسمّي صلاحية غير التي تفحصها القاعدة**: `/settings` بـ`CATALOG.MANAGE` والقاعدة `SETTINGS.MANAGE`؛ `/satisfaction` بـ`CATALOG.MANAGE` والقاعدة `NPS.MANAGE`؛ `/access` بـ`PORTAL.VIEW` (الجميع) وكتاباتها `USER.MANAGE` | `app.routes.ts` · `docs/system-discovery/04-role-permission-matrix.md` C-13 · `OQ-19` | 🟠 | الحارس يسمّي صلاحية القاعدة نفسها (تغيير `data.permission` فقط — لا منطق) |
| N-08 | **رابط ميّت في الوارد**: إشعار `INVOICE` يوجّه إلى `/invoices` ولا مسار كهذا → `**` → `/dashboard` بصمت | `core/api/inbox-api.ts:112` | 🔴 | `/billing?invoice={id}` ثم تفصيل الفاتورة حين يُبنى |
| N-09 | **شاشات لا يصل إليها إلّا الرابط أو العنوان**: ملفّ الأخصائي لصاحبه (لا «ملفّي»)، كرت الطفل، البثّ؛ وشاشات لا يدخلها إلّا المنيو (`/therapist-services`, `/access`, `/ops-log`, `/settings`, `/site`) | جرد الروابط §7 في `ux_ops` | 🟡 | «ملفّي» في قائمة الأخصائي؛ الباقي إعدادات |
| N-10 | **البوّابة على الهاتف تُخفي 6 شاشات**: `/reports` `/billing` `/requests` `/notifications` `/communications` `/therapists/:id` ليست على شريط التبويب؛ الجرس **على الشاشة العريضة فقط** | `web/portal/src/app/layout/shell/shell.html` (5 تبويبات هاتف) | 🔴 | تبويب خامس «المزيد» + جرس في رأس الهاتف |
| N-11 | **رابط `<a href="/communications">` خام** في «طلباتي» = إعادة تحميل التطبيق كاملًا | `features/requests/requests.html` | 🟡 | `routerLink` |
| N-12 | **`/welcome` منتقي طفل خارج الغلاف** — أسرة بطفل واحد تمرّ به كل دخول؛ و«تبديل» في الرئيسية يعيد إليه | `app.routes.ts` (البوّابة) · `welcome.ts` | 🟠 | مبدّل طفل في رأس الغلاف؛ `/welcome` يبقى لأوّل دخول/عدّة أطفال |

## ب · Screen Problems — الشاشات نفسها

| # | المشكلة | الدليل | الشدّة | الهدف |
|---|---|---|---|---|
| S-01 | **طلب الالتحاق بلا شاشة تفصيل** — الكيان المحوري لأوّل خطوة في الرحلة يُدار من صفّ جدول + حوار «تغيير حالة». لا مكان للتاريخ، لا للتواصل، لا لموعد التقييم، لا لشارات «طفل قائم/وليّ أمر قائم/المصدر» المخطَّطة (`10` §7.3) | `day-spec.ts ENROLMENTS_SPEC` (أفعال: status · convert) | 🔴 | `/enrolments/:id` Detail Page (`UX-DETAIL-PAGES.md`) |
| S-02 | **وليّ الأمر بلا مسار ولا تفصيل** — master-detail مخصّص مضمَّن داخل التبويب الأوّل من «الأطفال» | `resource-screen.ts` (الحالة الخاصّة `guardians`) | 🔴 | `/guardians` + `/guardians/:id` |
| S-03 | **ملفّ الطفل للقراءة بلا أفعال**: حجز موعد، فاتورة، بيع باقة، إسناد أخصائي، خطّة — كلّها على شاشات أخرى. الاستقبال مع أسرة على الهاتف يفتح 3–5 شاشات ويفلتر كلًّا بيده (وهذا هو السبب المكتوب في تعليق المسار لإنشاء الملفّ أصلًا) | `features/child/child-profile.ts` («READ ONLY by design» إلّا نشر ملاحظة وموافقة البثّ) | 🔴 | CTA أساسي + قائمة أفعال في رأس الملفّ تفتح **نفس حوارات** `DayScreen` مسبوقة التعبئة بالطفل |
| S-04 | **الخطط مفهرسة بالكيان لا بالطفل**: 4 تبويبات (خطط · أهداف · قياسات · برنامج منزلي) بأربعة منتقيات رقمية متتالية على شاشة يومية للأخصائي (`BL-32`) | `PLANS_SPEC` · `GOALS_SPEC` (`ref` بأرقام) | 🟠 | تبويب «الخطّة» في ملفّ الطفل (خطّة ← أهدافها ← قياساتها ← برنامجها)؛ تبقى `/plans` قائمة عبر الأطفال |
| S-05 | **الموعد بلا تفصيل ولا تاريخ**: `appointment_status_history` مبنيّ بلا قارئ؛ التعديل حوار واحد | `day-spec.ts` (فعل `status` فقط) | 🟠 | لوحة جانبية (drawer) للموعد: التاريخ + الأفعال + رابط الطفل |
| S-06 | **الفاتورة بلا تفصيل**: سطورها ودفعاتها في حوارات؛ **حذف السطر موجود في الـAPI ولا شاشة تناديه** | `day-api.ts removeInvoiceLine` غير مستدعى | 🟠 | لوحة جانبية للفاتورة (سطور · دفعات · أقساط 0142) |
| S-07 | **عمل واحد على شاشتين**: الأخصائي يبدأ الجلسة من «يومي» ويغلقها من «الجلسات» ويكتب ملاحظتها هناك وينشرها من «ملفّ الطفل» | `day-spec.ts` (start في APPOINTMENTS، close/note في SESSIONS، publish في child-profile) | 🟠 | «يومي» يعرض الموعد وجلسته معًا (بدء ← ملاحظة ← إغلاق ← نشر) |
| S-08 | **قبول طلب «تغيير موعد» لا يغيّر الموعد** — القرار في `/requests` والحجز الجديد في `/appointments` بلا رابط (`rescheduled_from_appointment_id` موجود بلا استعمال من الشاشة) | `HBH-043` · `04-ops-api-needs.md:180` | 🔴 | قرار «قبول» يفتح حوار الحجز مسبوق التعبئة بالطفل/الموعد؛ (توصية backend: `PATCH /requests/{id}` يقبل `appointment_id` الجديد) |
| S-09 | **إسناد الحالات CRUD عامّ** تحت «الأخصائيون» (ثلاثة `ref` رقمية) بينما هو قرار على **طفل** | `CASELOAD_SPEC` | 🟠 | فعل «عيّن أخصائيًا» من ملفّ الطفل؛ التبويب يبقى قراءة |
| S-10 | **التقييم بلا شاشة**: 4 جداول و6 دوالّ في القاعدة، صفر مسار، صفر شاشة — خطوة ٦ في الرحلة معدومة في الواجهة | `ux_db` · `HBH-041` | 🔴 | **توصية backend** (مسارات) ثم تبويب «التقييم» في ملفّ الطفل — لا يُبنى في الواجهة قبل مساره |
| S-11 | **الشاشة المشتركة `FamilyMessages` مثبَّتة في بوّابة الأسرة**: بحث في الأسر، إرسال جماعي، قوالب موظّفين — أمام وليّ أمر | `web/shared/src/ui/family-messages.ts` · مسار البوّابة `communications` | 🔴 | مكوّن أسرة: محادثة واحدة مع المركز (نفس النقاط، بلا قائمة أسر) |
| S-12 | **ثلاثة أفعال في البوّابة مبرمجة على الفشل**: تبديل الموافقات (405 محلّيًّا)، «لحظة مهمّة» في البثّ (403 محلّيًّا)، إلغاء تعليم النشاط (لا يُكتب) | `portal-api.ts setConsent/markMoment` · `activities.ts` | 🔴 | إخفاء ما لا مسار له (الموافقات تُدار من المركز؛ اللحظة ليست للأسرة)؛ إلغاء التعليم: توصية backend أو إخفاء |
| S-13 | **«لم تُسدَّد فاتورة الاستشارة» يحيل إلى شاشة بلا دفع** — `/billing` زرّها «طرق الدفع» toast | `consultation.ts` · `billing.html` | 🟠 | حتى تُبنى `FE-07`: الإحالة إلى «طلباتي/مكالمة» بنصّ صادق «تواصل مع المركز للسداد» |
| S-14 | **معالج الالتحاق يسطّح 25 حقلًا في عمودين نصّيين** ويتجاوز الترجمة ويستعمل `<input type=date>` رغم وجود منتقي الأجزاء | `apply.ts` | 🟡 | خارج نطاق التجربة (backend/i18n)؛ يُسجَّل |
| S-15 | **الداشبورد يعرض بطاقات بلا قراءة خلفها** (الغرف/التنبيهات) ولا يميّز «مرفوض» عن «صفر» في بعض المسارات (`metrics` تُرجع `null` لا 403) | `dashboard-tiles.ts` تعليق · `04-app-notes` | 🟡 | إخفاء ما لا مصدر له؛ الشرطة للمرفوض (موجود) |
| S-16 | **`/access` ثلاث وظائف في شاشة بحارس الجميع**: إدارة مستخدمين · ملفّات موظّفين ومستنداتهم · تشخيص الصلاحيات | `features/access` (1072 سطرًا · 3 تبويبات · 9 بطاقات) | 🟠 | «المستخدمون والأدوار» (`USER.MANAGE`) + «ملفّات الموظّفين» (`STAFF.PII`) + تبويب تشخيص |
| S-17 | **لا مكان لإدخال عطلة المركز أو إجازة الأخصائي** (`schedule_blocks` بلا CRUD) — يُحترم عند الحجز ولا يُكتب من شاشة | M-05 | 🟠 | توصية: صفّ `Spec` واحد = شاشة (قاعدة المشروع) |
| S-18 | **نصوص عربية مضمَّنة خارج ملفّ الترجمة** في الأجزاء الأحدث (الداشبورد، التقويم/اللوحات في `DayScreen`، أولياء الأمور، محرّر التقرير، الرسائل، معالج الالتحاق) — يخالف قاعدة الملفّ ويمنع لغة ثانية | `ux_ops` §8.7 · `ux_portal` | 🟡 | خارج نطاق التجربة؛ يُسجَّل للتنفيذ |

## ج · Status Problems — الحالات

**الخلاصة أوّلًا:** الكونسول **لم يحوّل حالات إلى شاشات** — `DayScreen` يجعل الحالة فلتر `?status=`، ولوحة مراحل الالتحاق تجعلها أعمدة. هذا صحيح ويُحافَظ عليه. المشاكل الحقيقية في **المفردات** و**الحالات المشتقّة التي لا اسم لها**:

| # | المشكلة | الدليل | الشدّة | الهدف |
|---|---|---|---|---|
| ST-01 | **مفردات حالة مختلفة بين البوّابة والقاعدة**: الفواتير `DUE/PAID/PARTIAL/VOID` في البوّابة مقابل `ISSUED/PARTIALLY_PAID/PAID/CANCELLED` في القاعدة؛ الطلبات `SUBMITTED/UNDER_REVIEW/ACCEPTED/DECLINED` مقابل `NEW/ACCEPTED/REJECTED` (**`UNDER_REVIEW` لا مصدر له**) | `portal.models.ts` · `0008` · `0015` | 🟠 | مفردة واحدة = مفردة القاعدة؛ التسمية العربية في ملفّ الترجمة |
| ST-02 | **`CHECKED_IN` غائبة من تعداد البوّابة** — موعد حضر يظهر بلا تسمية | `AppointmentStatus` في البوّابة | 🟡 | إضافة القيمة وتسميتها («حضر») |
| ST-03 | **`INACTIVE` للطفل موجودة في القاعدة بلا تسمية في الكونسول** | `status.child.*` في `ar.json` | 🟡 | تسمية |
| ST-04 | **`WITHDRAWN` بمعنيين** (الطفل انسحب / سُحبت موافقة نشر ملفّ الأخصائي) | D-03 | 🟡 | تسميتان عربيتان مختلفتان (موجودتان: «منسحب» / «مسحوبة موافقته») — يُحافَظ على التفريق |
| ST-05 | **حالات مشتقّة بلا اسم ولا فلتر** وهي التي يحتاجها العمل: «تواصلنا ولم نحجز تقييمًا» · «التقييم تمّ ولم نحوّل» · «حضر ولم تبدأ جلسته» · «اكتملت جلسته بلا ملاحظة» · «فاتورة متأخّرة» (`due_date` فات) · «طفل بلا أخصائي» · «باقة توشك على النفاد» · «وليّ أمر بلا حساب» | لا فلتر لأيّ منها في `day-spec.ts` | 🔴 | هذه **مهامّ** لا حالات — تُشتقّ في صندوق المهامّ من حالة + شرط (وتوصية backend: عرض `v_staff_tasks` يجمعها) |
| ST-06 | **حالة الطلب لا تتحرّك بالحجز**: `ASSESSMENT_BOOKED` يُختار من قائمة بيد الموظّف بينما الموعد يُحجز في شاشة أخرى — حالتان لشيء واحد قد تفترقان | `ENROLMENT_NEXT` في `day-spec.ts` | 🟠 | فعل «احجز تقييمًا» من تفصيل الطلب يحجز **ثم** يغيّر الحالة (توصية backend: ربط `assessment_appointment_id`؛ حتى يُبنى: الشاشة تنفّذ النداءين بالترتيب) |

## د · Task Problems — أفعال مطلوبة عُوملت كسجلّات أو ضاعت

| # | الفعل المطلوب | أين يعيش اليوم | المشكلة | الهدف |
|---|---|---|---|---|
| T-01 | مراجعة طلب التحاق جديد | بطاقة عدّ في الداشبورد | عدّ لا قائمة؛ لا CTA | مهمّة |
| T-02 | حجز موعد التقييم للطلب | لا شيء يشير | ضياع بين شاشتين | مهمّة بزرّ يفتح الحجز |
| T-03 | تحويل الطلب إلى ملفّ بعد التقييم | لا شيء يشير | ضياع | مهمّة |
| T-04 | تأكيد مواعيد اليوم | لوحة داخل `/appointments` | مخفيّة في disclosure | مهمّة |
| T-05 | تسجيل الحضور | فلتر يدوي | — | مهمّة (وقتيّة: من وقت الموعد) |
| T-06 | بدء الجلسة | لوحة «يومك» + `/my-day` | موجودة لكن على شاشتين | مهمّة (تبقى أيضًا في «يومي») |
| T-07 | إغلاق الجلسة | `/sessions` | — | مهمّة |
| T-08 | كتابة ملاحظة الجلسة | `/sessions` بلا إشارة | لا يعرف أنه نسي | مهمّة |
| T-09 | نشر ملاحظة/تقرير/تقييم للأسرة | ملفّ الطفل / `/reports` | متفرّقة | مهمّة اختيارية |
| T-10 | البتّ في طلب أسرة | بطاقة عدّ | عدّ | مهمّة (+ حجز مربوط عند القبول) |
| T-11 | إصدار فاتورة مسودّة | فلتر | — | مهمّة |
| T-12 | تحصيل فاتورة متأخّرة / قسط مستحقّ | عدّاد «صادرة» (لا يميّز المتأخّر) | لا «متأخّر» | مهمّة مرتّبة بالتأخّر |
| T-13 | تجديد باقة توشك على النفاد | لا شيء | — | مهمّة |
| T-14 | اعتماد خطّة علاجية | لا زرّ | `LC-09` | مهمّة + توصية backend (مسار اعتماد) |
| T-15 | إسناد أخصائي لطفل جديد | CRUD في `/therapists` | جدول لا مهمّة | مهمّة تُفتح من ملفّ الطفل |
| T-16 | منح حساب بوّابة لأسرة قائمة | لا شاشة | `HBH-011` | مهمّة (بعد المسار/الزرّ) |
| T-17 | تسجيل موافقة ناقصة (صورة/بثّ) | تبويب أولياء الأمور (`LIVE_VIEW` فقط) | `PHOTO_USE` بلا مكان | مهمّة حين يُطلب فعل يحتاجها |
| T-18 | موافقة الأخصائي على نشر ملفّه | بالرابط فقط | لا يعرف | مهمّة للأخصائي |
| T-19 | فضّ تداخل أخصائي/غرفة | لوحة داخل `/appointments` | مخفيّة | مهمّة |
| T-20 | استكمال ملفّ أسرة ناقص (`record_completeness`) | لا شاشة (عمود بلا قارئ) | — | مهمّة (بعد `EN-06`) |
| T-21 | 🔴 تعيين أخصائي لطلب استشارة · مراجعة إيصال · تمديد مهلة | مخطَّط | — | مهامّ حين تُبنى |

**ما ليس مهمّة (ويبقى إشعارًا):** «نُشر تقرير»، «حُجز موعد»، «أُسند إليك طفل» — أحداث وقعت، تُقرأ ولا تُنجَز. راجع `UX-TASK-INBOX.md` للفصل الكامل.

## هـ · Naming Problems — التسمية

| الاسم الحالي | ما تفعله فعلًا | الاسم المقترح |
|---|---|---|
| صندوق الوارد (`/inbox`) | سجلّ إشعارات الشخص | **الإشعارات** |
| — (غير موجود) | ما يحتاج إجراءً منّي | **المهامّ** (جديد) |
| الأطفال (`/children`) | أولياء الأمور + الأطفال | **المستفيدون** (والأولياء شاشة مستقلّة) — يوافق `OD-23` (مستفيد لا طفل) و`BL-45` (تسمية الواجهة فقط) |
| يومي (`/my-day`) | مواعيد الأخصائي اليوم | **مواعيدي** (فلتر داخل المواعيد) |
| الصلاحيات والشاشات (`/access`) | إدارة المستخدمين والأدوار وملفّات الموظّفين | **المستخدمون والأدوار** · **ملفّات الموظّفين** |
| الخدمات والباقات (`/catalog`) | + مكتبة الأنشطة + استبيانات الرضا | **الكتالوج** (الخدمات · الباقات · الأنشطة)؛ الاستبيانات إلى «رضا الأسر» |
| رضا الأسر (`/satisfaction`) | نتائج فقط | **رضا الأسر** (الاستبيانات · النتائج) |
| خدمات الأخصائيين (`/therapist-services`) | مصفوفة أخصائي×خدمة | تبويب **«الخدمات»** في الأخصائيين |
| فريق العمل (`/site/team`) | فريق **الموقع التعريفي** (تسويقي، ليس الموظّفين) | تبويب **«الفريق على الموقع»** في الموقع التعريفي — وإلّا خُلط بالأخصائيين |
| إسناد الحالات (تبويب) | caseload | يبقى، لكن الفعل من ملفّ الطفل «الأخصائي المسؤول» |
| تبويب `activityLog` في ملفّ الطفل | التزام الأسرة بالبرنامج المنزلي | **«البرنامج المنزلي»**؛ و«الخطّ الزمني» اسم محجوز لـ`v_case_timeline` (`HBH-045`) |
| أهلًا بك (`/welcome`) | اختر طفلًا | **اختر المستفيد** (أو مبدّل في الرأس) |
| التقدّم / التقارير والملاحظات (بوّابة) | ثلاث زوايا لمتابعة واحدة | **متابعة طفلي** بتبويبات |
| المحادثات (بوّابة) | شاشة موظّفين | **رسائل المركز** (محادثة واحدة) |

## و · مشاكل خارج نطاق الواجهة — تُسجَّل كتوصيات ولا تُنفَّذ هنا

| # | التوصية للـbackend/DB | لماذا تظهر في تدقيق تجربة | المرجع |
|---|---|---|---|
| R-01 | مسارات التقييم (`assessments`) | خطوة ٦ في الرحلة بلا واجهة ممكنة | `HBH-041` |
| R-02 | مسار اعتماد الخطّة (`approve_plan` بـ`approved_by`) | مهمّة T-14 بلا فعل | `LC-09` |
| R-03 | ربط قبول طلب التغيير بموعد جديد (`PATCH /requests/{id}` يحمل `appointment_id`) | S-08 | `HBH-043` |
| R-04 | ربط الطلب بموعد التقييم (`enrolment_applications.assessment_appointment_id`) — أو الاعتماد على `0144` (الطفل يُنشأ عند الإرسال فيُحجز له) | ST-06 · خطوة ٣ | `HBH-058` |
| R-05 | مسار/زرّ منح حساب البوّابة + قراءة `v_guardian_portal_status` | T-16 | `HBH-011`/`012` · `X-09` |
| R-06 | عرض `v_staff_tasks` (اتحاد الحالات المشتقّة في §ج ST-05 بصلاحية كل مهمّة) — **اختياري**: صندوق المهامّ يعمل أوّلًا بالقراءات القائمة | يقلّل 8–12 نداءً لكل فتح | جديد |
| R-07 | `CRUD` لـ`schedule_blocks` (صفّ `Spec` واحد) | S-17 | M-05 |
| R-08 | المنتِجون الغائبون لخمسة إشعارات (`STAFF_ENROLMENT_NEW`, `STAFF_REQUEST_NEW`, `STAFF_APPOINTMENT_CANCELLED`, `STAFF_PLAN_APPROVED`, `SESSION_STARTED`) | الإشعارات لن تكمل صندوق المهامّ بدونها | C-14 · `HBH-063` |
| R-09 | مسار إلغاء تسجيل النشاط (`done=false`) أو إسقاط الزرّ | S-12 | جديد |
| R-10 | حارس `/settings` و`/satisfaction` و`/access` يسمّي صلاحية القاعدة | N-07 | C-13 |
| R-11 | `REPORT.WRITE` تُمنح في هجرة (`0091`) لا في البذور — تضيع عند إعادة البناء | يفسّر شاشة تقرير «مرفوضة» بلا سبب ظاهر | جديد (مكتشف في الجرد) |
| R-12 | مفردة واحدة لحالات البوّابة (ST-01) — تغيير في طبقة النقل | ST-01 | جديد |

---

## ز · أكبر عشر مشاكل — الترتيب بالأثر

1. **لا صندوق مهامّ.** ما يحتاج إجراءً موزَّع على 4 بطاقات عدّ، لوحتين مخفيّتين، وفلاتر يدوية — والمسمّى «صندوق الوارد» هو سجلّ إشعارات (N-02, T-01…T-20).
2. **قائمة مسطّحة من 22 بندًا** بلا نطاقات ولا شارات ولا ترتيب بدورة العمل (N-01, N-06).
3. **طلب الالتحاق ووليّ الأمر بلا شاشة تفصيل** — أوّل خطوتين في الرحلة تُداران من صفوف وحوارات (S-01, S-02).
4. **ملفّ الطفل بلا أفعال** فالاستقبال يقفز بين 3–5 شاشات لأسرة واحدة (S-03, S-09, S-04).
5. **عمل واحد على شاشتين** بلا رابط: بدء/إغلاق الجلسة، قرار الطلب/الحجز الجديد، حجز التقييم/حالة الطلب (S-07, S-08, ST-06).
6. **الحالات المشتقّة التي يحتاجها العمل بلا اسم ولا فلتر** («لم يُحجز تقييم»، «متأخّرة»، «بلا أخصائي») (ST-05).
7. **بوّابة الأسرة تحمل شاشة موظّفين وثلاثة أفعال مبرمجة على الفشل** (S-11, S-12) و**6 شاشات لا يصل إليها الهاتف** (N-10).
8. **التقييم خطوة معدومة في الواجهة** رغم بنائها في القاعدة — توصية backend قبل أيّ شاشة (S-10).
9. **أسماء لا تقول ما تفعل** («صندوق الوارد»، «الأطفال»، «الصلاحيات والشاشات»، «فريق العمل»، `activityLog`) (§هـ).
10. **حراس يسمّون صلاحية غير التي تفحصها القاعدة ورابط ميّت في الوارد** — أخطاء صغيرة تُقرأ كأعطال (N-07, N-08).

</details>
