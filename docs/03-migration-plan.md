# 03 — خطة الترحيل

<!-- doc-owner -->
> **المالك:** Database Developer — **هو وحده من يكتب في هذه الوثيقة.**
> لاحظت خطأً؟ **راسله ولا تصلحه بنفسك** — راجع [OWNERS.md](OWNERS.md).
<!-- doc-owner -->


قرار المالك في **2026-09-03**: أوراكل تتوقّف، وPostgreSQL يصير مصدر الحقيقة.
الأسباب وما سقط من القواعد في **D-26**. هذا الملف هو الترتيب ونقطة القطع.

**الشرط التشغيلي للقرار، وهو غير قابل للتفاوض:**

> **APEX يصير للقراءة أو يُطفأ — قبل أول كتابة حقيقية من هنا.**
> نظامان يكتبان في نفس البيانات أسوأ من أيٍّ منهما وحده. لا يوجد «فترة انتقالية
> يكتب فيها الاثنان»؛ الفترة الانتقالية يقرأ فيها أحدهما فقط.

---

## أين نحن اليوم

| الطبقة | الحالة | ما ينقص |
|---|---|---|
| الداتابيز | ✅ P1–P8 · **٥٣٥ فحصًا** · كتابة وحذف ناعم مبنيّان (0012، 0013) | جدولان: العملاء المحتملون، التواصل |
| خدمة Go | ✅ ٣ دفعات · **٢٤٣ فحصًا** · الكتابة بدأت في الدفعة ٣ | كتابة العمليات الكاملة |
| بوابة ولي الأمر | 🟨 تعمل · ١٢ شاشة · **على بيانات تجريبية** | التوصيل بالـ API الحقيقي |
| تطبيق المركز | ⬜ `web/ops` فارغ | كل شيء — ١٣ تصميمًا ساكنًا فقط |
| ترحيل البيانات | ⬜ **لا يوجد** | استخراج من أوراكل، تحميل، تسوية |

**أخطر سطر في الجدول هو الأخير.** الشاشات تُبنى وتُختبر؛ البيانات تُنقل مرّة
واحدة وفي ليلة واحدة، ولا يوجد لها اليوم سطر كود.

---

## الترتيب

الترتيب ليس اقتراحًا: كل مرحلة تفتح التي تليها، والقفز يُنتج عملًا يُعاد.

### ١ — سدّ فجوة السكيما  ·  *يسبق كل شيء*

**العملاء المحتملون** و**التواصل** شاشتان من الاثنتي عشرة، وليس لهما جدول.
لا يمكن بناء واجهتهما ولا ترحيل بياناتهما من أوراكل قبل وجودهما.

ويسبقهما سؤال يُجاب من `../hand by hand`: **ما الذي يفعله APEX 115 وليس له
مقابل هنا؟** الجرد يُقرأ من تطبيق أوراكل نفسه لا من الذاكرة — وكل ما يُكتشف
لاحقًا يصير عملًا في ليلة القطع بدل أن يكون عملًا اليوم.

### ٢ — كتابة العمليات في Go

الداتابيز تكتب اليوم عبر دوال (`book_appointment`، `start_session`،
`sell_package`…) وسياسات 0012. الناقص هو تعريضها كنقاط نهاية.

**القاعدة ٢ تحكم هذه المرحلة:** الطبقة تنقل الإجابة ولا تقرّر. كل نقطة كتابة
تنادي دالة PL/pgSQL وتترجم `reason` إلى HTTP — ولا تضيف شرطًا، لأن الشرط في
المعالِج نسخة ثانية من قاعدة، والأضعف هي التي تقرّر.

### ٣ — تطبيق المركز `web/ops`

بعد (٢) لا قبله: بناء شاشة CRUD على نقطة نهاية غير موجودة يعني تخمين عقدها،
ثم إعادة كتابتها. الموجود اليوم يُبنى عليه:

- ١٣ تصميمًا ساكنًا في `html/` — نفس ما بُنيت منه بوابة ولي الأمر
- شاشة دخول باسم مستخدم وكلمة مرور، على عقد `hbh.verify_password`
- قائمة تتبع الصلاحيات، ورموزها من `0002_rbac.sql`

**ويُعاد استعمال ما بُني للبوابة لا يُنسخ:** `PortalApi` المجرّد، طبقة الترجمة،
`FormatService` (UTC، وصيغ الجمع الست، والاشتقاق)، ونظام التصميم. تطبيقان في
مستودع واحد يتقاسمان `web/shared`.

### ٤ — توصيل بوابة ولي الأمر بالـ API

اليوم على بيانات تجريبية، وهو مفتاح واحد (`useFixtures`) — لكنه يحتاج مطابقة
النماذج للعقد الحقيقي: المعرّفات أعداد لا نصوص، و`child_no` لا `caseCode`،
و`birth_date` بلا عمر محسوب. مؤجَّل خلف (٢) لأن نقاط النهاية التي تنقصه تأتي معها.

### ٥ — ترحيل البيانات

**المرحلة الوحيدة التي لا تُجرَّب في الإنتاج.**

استخراج من أوراكل ← تحميل في PostgreSQL ← **تسوية تُثبت التطابق بالعدّ لا
بالنظر**: عدد الأطفال، وأولياء الأمور، والمواعيد، والجلسات، والفواتير، وأرصدة
الباقات، ومجاميع المدفوعات — كلٌّ على الجانبين، والفرق صفر أو لا يُقطع.

وتُجرَّب **ثلاث مرّات على الأقل** على نسخة من بيانات الإنتاج، بقياس زمن كل
تشغيلة. الزمن هو ما يحدّد طول نافذة القطع، ولا يُعرف بالتقدير.

### ٦ — القطع

---

## نقطة القطع

| # | الخطوة | التراجع |
|---|---|---|
| ١ | إعلان النافذة للمركز — يوم عطلة، والجمعة والسبت عطلة المركز | — |
| ٢ | **APEX للقراءة فقط** | يُعاد فتح الكتابة فورًا |
| ٣ | نسخة كاملة من أوراكل، محفوظة ومُختبَرة الاستعادة | — |
| ٤ | تشغيل الترحيل | — |
| ٥ | **التسوية بالعدّ** — فرق واحد يوقف القطع | العودة للخطوة ٢ |
| ٦ | فتح الكتابة في النظام الجديد | — |
| ٧ | مراقبة ٤٨ ساعة وAPEX ما زال قائمًا للقراءة | العودة لأوراكل |
| ٨ | إطفاء APEX | بعدها التراجع يكلّف بيانات |

**النقطة السابعة هي التي لا يجوز اختصارها.** ما دام APEX قائمًا للقراءة، فالعودة
قرار. بعد إطفائه، العودة تعني فقدان كل ما كُتب بعد القطع.

**ولا يُقطع** والفرق في التسوية غير صفر، أو الترحيل لم يُجرَّب ثلاثًا، أو نافذة
القطع أطول من زمن أطول تشغيلة مقيسة مضروبًا في اثنين.

---

## ما تغيّر في القواعد

D-26 أسقط ثلاثًا من القواعد الخمس القديمة، وهذا مقصود لا سهو:

| القاعدة القديمة | لماذا سقطت |
|---|---|
| لا تُعاد كتابة قاعدة عمل موجودة في PL/SQL | لأن PL/SQL لم يعد يعمل |
| لا قاعدة عمل جديدة قبل انتقال الإنتاج | لأن الانتقال هو ما يجري |
| تاريخ إنتاج APEX لا يتحرك | لأن APEX يُطفأ |

**وقاعدة واحدة لم تسقط بل اشتدّت:** مصدر حقيقة واحد. كانت تعني «لا تكتب هنا»،
وصارت تعني **«لا تكتب هناك»** — ونفس الخطر يقف خلفها.

---

## ما يبقى صحيحًا مهما تغيّر النظام

هذه ليست قرارات ستاك، بل قرارات عمل. تنجو من الترحيل كما هي:

- **طفل واحد لكل جلسة.** لا جداول مشاركين.
- **بث مباشر فقط — ولا تسجيل، أبدًا.** لا جدول تسجيلات ولا مؤشّر داخل مقطع.
- **الحذف ناعم**، ولا منحة `DELETE` على أي جدول.
- **كل لحظة UTC** وتُقارَن UTC. التحويل للعرض فقط.
- **مصر بارامترات لا كودًا:** `EGP` · `Africa/Cairo` · الجمعة والسبت · رقم قومي
  ١٤ رقمًا · جوال `01XXXXXXXXX`. مركز في بلد آخر يحتاج صفًّا لا إصدارًا.

---

## 2026-09-10 — الدفتر انكسر، وخطّ الهجرات معه

`db.sh migrate` كان **يموت على `0079`** بـ `relation "family_messages" already exists`،
فلم تكن أي هجرة جديدة قادرة على النزول — لا هذه ولا ما بعدها.

**السبب:** `0079` و`0080` أنشأتا جدوليهما ولم تسجّلا رقميهما. فالجدولان في السكيما
والدفتر لا يعرف عنهما شيئًا، و`migrate` يحاول إعادة تشغيلهما في كل مرة. وهذه هي
**نفس** الحالة المكتوبة في `CLAUDE.md` تحت «المهاجرة التي لا تسجّل رقمها تُعاد إلى
الأبد» — وقعت مرّة أخرى بعد أن كُتبت. الفرق أن تلك المرّة نجت لأن
`ADD COLUMN IF NOT EXISTS` آمنة عند الإعادة؛ و`CREATE TABLE` ليست كذلك، فسقط الخطّ
كلّه هذه المرّة بدل أن يمرّ بصمت.

**الإصلاح على خطوتين، والترتيب مقصود:**

1. **أُثبت أولًا أن كل ما في الملفّين نزل فعلًا** — الجدولان، الفهرس، السياسات الثلاث،
   `FORCE ROW LEVEL SECURITY` على الاثنين، ومنحة التسلسل. تسجيل هجرة في الدفتر إعلانٌ
   بأنها طُبِّقت؛ وإعلان كاذب في الدفتر أسوأ من دفتر ناقص، لأن الناقص يُكتشف عند أول
   تشغيل والكاذب لا يُكتشف أبدًا.
2. ثم أُضيف `INSERT INTO hbh.schema_migrations` إلى آخر كل ملفّ **داخل معاملته**،
   وسُجِّل الرقمان في هذه القاعدة يدويًّا لأن بنيتهما قائمة سلفًا.

**وملاحظة على الملفّين:** كلاهما يفتح `BEGIN` بنفسه بينما `db.sh` يلفّ الملفّ في معاملة
أصلًا، فيظهر `WARNING: there is already a transaction in progress`. غير ضارّ اليوم،
لكنّه يعني أن `COMMIT` في منتصف ملفّ سيُنهي معاملة السكربت لا معاملة الملفّ — فملفّ
بخطوتين بعد `COMMIT` سيُطبَّق نصفه ويُسجَّل كاملًا.

## 0081 — الجدولان يلتحقان بالمعايير

`p00` كان أحمر بثلاثة فحوص على `family_messages` و`family_message_reads`. أُصلح
**اثنان**: أعمدة التدقيق الأربعة، وفهرس خلف كل مفتاح أجنبي.

**والثالث — الحذف الناعم — مُعلَّق عمدًا على `BL-21`**: هل رسالة أُرسلت لأسرة بالخطأ
تُسحَب أصلًا، أم تُصحَّح برسالة تالية كما تُصحَّح ملاحظة سريرية؟ كتابة الأعمدة الآن
تجيب عن السؤال في SQL قبل أن يجيب عنه المالك بكلام، و«تبقى نهائية» قراءة واردة تمامًا.
فـ`p00` أحمر بفحص واحد حتى يُبَتّ فيه — **وهذا هو الوضع الصادق، لا خضرةٌ مزيّفة
باستثناء يقرّر نيابةً عن المالك.**

**ولماذا أعمدة لا استثناء:** الجداول المضافة-فقط في هذه السكيما (`audit_log` و
`consent_events` و`package_ledger`) أخذت استثناء `AUDIT_COLUMNS`، و`family_messages`
بلا منحة `UPDATE` فيبدو أنه منها. الفارق أن الاستثناء **قرار** — يقول إن القاعدة لا
تنطبق هنا للأبد — وأحد خيارات `BL-21` (السحب الناعم) يجعل السحب `UPDATE` يريد بشدّة
أن يسجّل من فعله. الأعمدة صحيحة تحت الخيارات الثلاثة، والاستثناء تحت اثنين منها فقط.

**وفائدة غير متوقّعة:** `family_message_reads` علامةُ قراءة — مفتاحها
`(user_id, guardian_id)` و`message_id` هو آخر ما قرأه ذلك المستخدم في الخيط — **ولم
تكن تسجّل وقتًا إطلاقًا**. «هل رأتها الأسرة؟» كان له جواب، و«منذ متى؟» لا. `updated_at`
هو ذلك الجواب، و`trg_touch` يجعله صادقًا بدل أن يكون زينة.

---

## 0084 — الحذف الناعم، وخلافٌ كان ظاهريًّا

جلستان اختلفتا حول هذه الهجرة، والخلاف كان بين **شيئين يلبسان اسمًا واحدًا**:

- **`Business analysis`:** لا تكتب الحذف الناعم قبل `BL-21` — هل تُسحَب رسالة أُرسلت
  بالخطأ أصلًا، أم تُصحَّح برسالة تالية كما تُصحَّح ملاحظة سريرية؟
- **`project manger`:** القاعدة ٣ في `CLAUDE.md` تقول «الحذف ناعم دائمًا». هذا خرقُ
  قاعدة، وخرق القاعدة يُصلَح بهجرة.

**وكلاهما محقّ، لأن «الحذف الناعم» عمودٌ وقدرةٌ في آنٍ واحد:**

| | ما هو | من يقرّره |
|---|---|---|
| **العمود** `active_flg` | اصطلاح سكيما — كل جدول يحمله | القاعدة ٣، ولا يحتاج قرارًا |
| **القدرة** — منحة `UPDATE`، دالة سحب، زرّ | سلوك يراه ولي الأمر | **`BL-21`، وهو للمالك** |

وإضافة العمود **لا تُنشئ القدرة**. `hbh.family_messages` يحمل `SELECT` و`INSERT` ولا
يحمل `UPDATE`، و`0084` لم تغيّر ذلك ولم تكتب دالة سحب ولم تمنح شيئًا. فإن قرّر المالك
أن الرسالة نهائية، بقي `active_flg` صحيحًا إلى الأبد — خاملًا، كحاله في أي جدول لا
يُعطَّل فيه صفّ. وإن قرّر السحب، فالعمود جاهز، ومشغّل التدقيق الذي أضافته `0082` يسجّل
`UPDATE` الذي يجعله كاذبًا.

**والسياسة تُرشِّح عليه**، لأن عمود حذف ناعم لا تقرؤه أي سياسة زينةٌ — والزينة هي ما
يُوثَق به إلى أن يُحتاج إليه. اليوم لا يتغيّر شيء: كل الصفوف `true` ولا أحد يملك المنحة
التي تجعل صفًّا `false`.

**وجدولٌ من الاثنين أخذ استثناءً بدل العمود، عمدًا.** `family_message_reads` علامةُ
قراءة؛ تعطيل صفّ فيها لا يُخفي شيئًا — بل يجعل رسائل **قرأتها** الأسرة تبدو غير مقروءة.
وللسكيما سابقتان بنفس الشكل: `stream_views` و`auth_sessions`، حالةٌ مشتقّة يكون إخفاء
صفّها **تزويرًا لا إخفاءً**. وكتابة عمود هناك لإرضاء عدّاد ثم عدم الترشيح به هي كيف
تبدأ السكيما في قول أكاذيب صغيرة.

**النتيجة:** `p00` خضراء ٢٩/٠، والاتنتا عشرة مجموعة **٨٣٣ فحصًا · ٠ فشل**.
و`BL-21` **ما زال مفتوحًا وغير مسبوق الحكم**.

---

## مسوّدة قرار للمعماري — لا أملك `01-stack-decisions.md`

طلب `project manger` تسجيل هذا قرارًا مرقَّمًا هناك. الوثيقة ليست لي، فهذه صياغته
جاهزة للنقل بيد مالكها:

> ### D-xx — «الحذف الناعم» عمودٌ وقدرةٌ، ويُفصَل بينهما
>
> **القرار:** اصطلاح `active_flg` يُطبَّق على كل جدول بلا استئذان، لأنه اصطلاح سكيما.
> أمّا منحة `UPDATE` والدالة والزرّ التي تجعل الإخفاء **ممكنًا** فقرار عمل يُبَتّ فيه
> على حدة.
>
> **السبب:** الخلط بينهما يعطّل السكيما بانتظار قرار عمل لا يخصّها، أو — أسوأ — يجعل
> الهجرة تجيب عن سؤال المالك في SQL قبل أن يجيب عنه بكلام. الفصل يسمح للاثنين أن
> يمضيا: البنية تلتزم اليوم، والسلوك ينتظر.
>
> **وحدّه:** حين يكون إخفاء الصفّ **تزويرًا** لا إخفاءً — حالة مشتقّة كعلامة قراءة أو
> سجلّ مشاهدة — فالعمود خطأ والاستثناء المسجَّل هو الصواب.

---

## 0088 · 0089 · 0090 — ثغرات دورة الحالة (LC-04 · 05 · 06 · 09 · 10)

تحليل `Business analysis` لدورة الحالة سمّى أحد عشر نقصًا. **سبعة منها لا تنتظر أحدًا**،
وأربعة محجوبة على المالك (`BL-35` · `BL-37` · `BL-38` · `BL-39`) **ولم تُبدأ** — والأخير
منها بالذات (`PAUSED`) ليس قيمةً تُضاف إلى `CHECK` بل سؤالٌ عن عدّاد صلاحية الباقة.

**`0088` — ثلاثة أسئلة لم تكن السكيما تجيب عنها:**

| | السؤال | العلاج |
|---|---|---|
| `LC-09` | من اعتمد هذه الخطة؟ | `approved_by` · `approved_at` — و`updated_by` **آخر من لمس** لا **من قرّر** |
| `LC-10` | من أي تقييم جاءت؟ | `assessment_id` |
| `LC-06` | من أين نُقل هذا الموعد؟ | `rescheduled_from_appointment_id` |

والختم في **`trg_plan_status`** — الآلة التي ترفض الانتقال غير الشرعي هي مكان تسجيل
الشرعي، ونفس ما فعلته `0026` مع `contacted_at`. ومطالبة المنادي به بدلًا من ذلك تكسر
كل `UPDATE` قائم لحظة نزول الهجرة.

**`0089` — النظام يتعلّم مخاطبة زميل.** كانت كل أنواع الإشعار السبعة موجَّهة لأسرة،
والدالّة الوحيدة `notify_guardians`. أي أن النظام **لا يعرف كيف يُشعر أخصائيًّا إطلاقًا**.
وعلى جانب الأسرة ثغرة أغرب: للموعد `APPOINTMENT_CANCELLED` **وحده** — النظام يُخبر
الأسرة حين يُسحب موعدها ولا يُخبرها حين يُعطى.

و`notify_staff` ليست `notify_guardians` بمعامل مختلف: الأسرة تُبلَّغ **عبر الطفل** وبموافقة
SMS لكل وليّ أمر، والموظّف يُبلَّغ **بالدور أو بالاسم** وبلا موافقة لأن هذا عمله —
و`sms_pending_flg` عنده **`false` دائمًا**، فتمريره خلال دالّة الأسرة كان سيضبطها من
موافقة وليّ أمر.

**`0090` — و`0088` كانت خاطئة في موضعين، أمسكتهما مجموعاتها:**

1. `trg_plan_status` مشغّل `BEFORE UPDATE` وحده، فصفٌّ **يُدرَج** بحالة `ACTIVE` لا يمرّ
   به — فلم يُختَم، ورفضه القيد. الضمانة كانت مفروضة على بابٍ من بابين.
2. و`HB210` رفضت انتقالًا **كان يعمل** قبل الهجرة. وهو فشل «الكود ينتظر / السكيما
   تنتظر» بعينه: السكيما تحرّكت فسقط كل ما يناديها.

**والفشل الثاني أسوأ رغم أنه يبدو أهون:** فحصٌ يؤكّد أن الفهرس الفريد يرفض التكرار صار
يفشل بانتهاك `CHECK` — **فهو أخضر عن لا شيء**. وهو الدرس المسجَّل: أكّد رمز الخطأ نفسه.

**والحلّ لا يخسر شيئًا:** قيد الوجود يسقط، والختم يبقى ويعمل على البابين. كل مسار
**يسلكه التطبيق** يحمل هوية — `hbh_app` يفشل مقفولًا بدونها في كل موضع — فكل خطة
يفعّلها التطبيق تسمّي معتمِدها. وما كان القيد يمنعه فعلًا هو سكربتات المالك والتجهيزات،
وليست هي الفئة التي كُتب من أجلها.

**ورمزا `HB210` و`HB202` متقاعدان ولا يُعاد استعمالهما.**

**و`p9` احتاجت تضييقًا لا إصلاحًا.** كان فيها «الأخصائي لا يرى أي إشعار عن هذا الطفل»،
وكان ذلك صحيحًا **لأن الإشعارات كانت للأسرة وحدها**. بعد `0089` صارت جملتين: «لا يقرأ
إشعارات الأسرة» — وهي الضمانة — و«لا يستقبل إشعارات» وهي لم تعد صحيحة ولا يُراد لها أن
تكون. وأُضيف بجوارها فحصٌ يثبت أن إشعار الموظّف **يصل**، وإلّا لكان التضييق قد أزال
بصمت آخر ما يثبت وصول الأنواع الجديدة.

---

## 2026-09-11 — رقم مزدوج ثانٍ، وعلاج بطء `migrate`

**`migrate` كان يستعلم الدفتر مرّةً لكلّ ملفّ** — كلٌّ منها `docker exec` مستقلّ بنحو
ثانية — فنما مع السكيما حتى صار يستغرق **أكثر من دقيقتين ليقرّر أن لا عمل لديه**.
صار يُقرأ الدفتر **مرّة واحدة**: ١٢ ثانية. وعلى قاعدة يتشاركها ثلاثة، أمرٌ لا يطيقه أحد
هو أمرٌ يكفّ الناس عن تشغيله — ثم لا يعرف أحد ما المطبَّق.

> والمطابقة `grep -qx` مثبَّتة الطرفين عمدًا: `008` يجب ألّا يطابق `0080`.

**وفور تسريعه ظهر رقم ٠٠٨٨ مزدوج** — وهذا ما وُضع الحارس له:

| الملفّ | كُتب | الحالة |
|---|---|---|
| `0088_plan_provenance_and_reschedule_link` | 03:18 | **مطبَّق** ومسجَّل في الدفتر |
| `0088_report_authoring` (جلسة أخرى) | 03:38 | غير مطبَّق |

**والخطّ كان مقفولًا للجلسات الثلاث** حتى يُرقَّم أحدهما. ورُقِّم **ملفّ الجلسة الأخرى**
إلى `0091` — لا أقدميّةً بل لأنه **الوحيد القابل للعكس**: ملفّي مطبَّق ومسجَّل، فنقله
يترك صفًّا في الدفتر بلا ملفّ. ولم يُمسّ من محتواهم شيء سوى السطر الذي يسجّل الرقم —
ولولاه لاصطدم بصفّي عند أوّل تشغيل عندهم.

**ودرسٌ للجدولة لا للكود:** حجز الرقم قبل كتابة الملفّ قاعدة مكتوبة عندنا، وقد سقطت
مرّتين في يومين. الفارق أن الحارس أمسك الثانية **قبل** أن تضيع هجرة، بينما الأولى
(`0079`/`0080`) لم تُكتشف إلّا حين توقّف الخطّ كلّه.

## `p10` و`p9` — فشل عابر لا انحدار

ظهرتا حمراوين في الجولات الكاملة وخضراوين منفردتين. والاختبار الحاسم: **`p10` أربع
تشغيلات متتالية، أربع نجاحات**، وكذلك `p9`.

والدليل على السبب: القاعدة تحمل **أربعة اتصالات**، وسجلّ التدقيق في آخر ساعة يحمل
كتابات باسم `admin` و`anonymous` و`mobile:****0009` — **ليست منّي**. جلسة أخرى تشغّل
مجموعاتها بالتوازي، والجولة الكاملة نافذتها خمس دقائق بينما المنفردة دقيقة: النافذة
الأوسع تلتقط التصادم، والأضيق تفلت منه.

**وهذا ليس تشخيصًا مثبتًا بل أرجح تفسير**، ويظلّ مفتوحًا حتى تُلتقط تشغيلة فاشلة
بتفاصيلها. ما هو مثبت: **لا علاقة له بـ`0088`–`0090`**.

---

## وتصحيح: فشل `p9` لم يكن عابرًا

قلتُ في الفقرة السابقة إن `p9` و`p10` فشلتا فشلًا عابرًا من التزامن. **هذا صحيح في
`p10` وخاطئ في `p9`.**

التقاط التشغيلة الفاشلة بتفاصيلها — بدل قراءة التشغيلة التي تليها — أعطى السبب في سطر:

```
HB029 report 323 has no summary to publish
```

هجرة `0091_report_authoring` (من الجلسة الأخرى، وهي التي رقّمتُها للتوّ وطبّقتُها)
جعلت `publish_report` ترفض تقريرًا بلا ملخّص — **«تقرير بلا ملخّص هو إشعار يتظاهر بأنه
تقرير»**. وتجهيزة `p9` كانت تنشر واحدًا كذلك، ونجحت طوال الوقت **لأن القاعدة لم تكن
موجودة بعد**. القاعدة صحيحة، والتجهيزة كانت تتّكئ على غيابها. أُضيف الملخّص.

**والدرس في الأداة لا في العيب:** «تشغيلة تفشل ثم تنجح» ليست دليل تزامن. قراءة تفاصيل
**التشغيلة التالية** تُظهر جدولًا فارغًا من الأخطاء وتُقرأ كأنها عرَض عابر — بينما
التشغيلة الفاشلة نفسها كانت تحمل رمز الخطأ والسبب. التقط الفاشلة، لا التي بعدها.

و`p5` بـ٢١ فشلًا كانت بقايا تجهيزة من انكسار سابق، لا انحدارًا: نضيفة بمجرّد تنظيفها.

---

## 0093 — آخر بندين غير محجوبين (LC-02 · LC-15)

**`LC-02`** — الاستمارة صار لها نفس مفردات `hbh.waiting_list`: أيام الأسبوع ومدى زمني.
كان عندها `preferred_contact_time` نصًّا حرًّا، فالاستقبال يقرأ «الصبح بعد التاسعة»
ويطابقها **بيده** مع `therapist_working_hours` لكل أسرة، كل مرّة. و`preferred_contact_time`
**باقٍ** لأنه يجيب عن سؤال آخر — متى نتّصل — وحذف عمودٍ يملؤه الناس لأن أفضل منه وصل
هو كيف تضيع سنةٌ من الإجابات.

**`LC-15`** — الخطّ الزمني. كل قطعة فيه كانت موجودة وبجودة عالية؛ الناقص أن أحدًا لم
يجمعها: اثنا عشر عرضًا في السكيما وليس فيها واحد يضع الأحداث في سطر.

**والنقطة الأمنيّة هي البند نفسه.** العرض يقرأ **تسعة** جداول بتسع سياسات مختلفة،
والعرض يعمل بصلاحيات **مالكه** ما لم يُقَل له غير ذلك — ومالكه يتجاوز RLS على كلٍّ
منها. فبدون `security_invoker` يسلّم وليَّ أمرٍ تاريخَ كل طفل في المركز، **بصمت وبلا
خطأ في أي مكان**. وهو أخطر عرض في السكيما من هذه الجهة.

والاختبار في `p4` لأن تجهيزتها هي الصحيحة: طفلان ووليّ أمر مرتبط **بأحدهما**. والفحص
الحاسم ليس أن العرض يرجع صفوفًا، بل أن **الطفل الآخر غائب**. النتيجة: وليّ الأمر يرى
طفله، ولا يرى الثاني، ولا يرى أي طفل خارج أطفاله، وبلا هوية يرى صفرًا.

**ولا نصّ سريريًّا يسافر فيه** — وفحصٌ يؤكّد غياب `body_ar` و`summary_ar` و
`recommendation_ar` بالاسم من أعمدته.

## وانحدار أدخلتُه بنفسي في `db.sh` — وظهر خلال دقائق

تسريع `migrate` يقرأ الدفتر **مرّة واحدة**. وعلى قاعدة يتشاركها ثلاثة، هذا يجعل النسخة
**قديمة**: جلسة أخرى طبّقت `0092` بين قراءتي وبين وصولي إليها، فحاولتُ إعادة تطبيق
هجرة **مطبَّقة ومسجَّلة كما ينبغي**، وماتت التشغيلة على `relation already exists`.

والعلاج ليس التراجع عن التسريع: **الفحص الرخيص للتسعين التي نتخطّاها، والفحص الحقيقي
للواحدة التي نوشك أن نشغّلها.** استعلام واحد حيث يهمّ، ولا استعلام حيث لا يهمّ.

> والدرس أعمّ من السكربت: **التخزين المؤقّت يحوّل «بطيء وصحيح» إلى «سريع وقديم»**،
> والفارق لا يظهر إلّا حين يكتب غيرُك في النافذة التي فتحتَها.

---

## 0128 — الثابت عن البيانات الحيّة، و`NOT VALID` لا يعني ما يبدو أنه يعني

بلاغٌ من جلسة الـAPI: صفّ `hbh.guardians` واحد بجوال خام، و«أي `UPDATE` عليه يوقظ
المشغّل ويُرفض بـ`HB173`». **ثلاثة أخطاء في جملة واحدة، وكلّها تعلّم شيئًا:**

| ما قيل | ما هو |
|---|---|
| `0125283838` — عشرة أرقام | `guardians` 668 فيه **`0100000` — سبعة**. والمُبلَّغ عنه هو `enrolment_applications` 331، ومعطَّل أصلًا |
| المشغّل يرفض | **المشغّل برئ.** حارسه `raw IS NOT DISTINCT FROM OLD->>col` يتخطّى التقييس — وذلك أصل العيب |
| `HB173` | **`HB170`**. والدالّة ترفع الاثنين بفرعين مختلفين |

**والعيب الحقيقي:** `NOT VALID` يعفي الصفوف القائمة من **المسح الأولي** لا من **التعديل
التالي**. فصفٌّ فاسد يصير **خالدًا وغير قابل للاستعمال**: لا يُسمّى، ولا يُربط بحساب،
و**لا يُعطَّل** — والتعطيل `UPDATE` يرفضه نفس الجوال الذي يحاول التقاعد منه. أُثبت قبل
لمس شيء داخل معاملة مُتراجَعة: التسمية `23514`، والتعطيل `23514`، وضبط رقم صحيح يُقبل.

**العلاج:** القيود الأربعة صارت `NOT active_flg OR mobile ~ e164`. **الثابت عن البيانات
الحيّة** — كل صفّ نشط يحمل E.164 كما كان، والمتقاعد يحفظ ما سُجِّل فعلًا. وتجميد التاريخ
لم يكن الغرض يومًا.

**وما لم تفعله الهجرة عمدًا: اختراع رقم هاتف.** لا شيء في السكيما يعرف ما كان `0100000`
يُفترض أن يكون، ورقمٌ معقول يضع **بيانات تواصل ملفَّقة لأسرة** في السجلّ السريري — وذلك
أسوأ من صفّ يراه أحد. تعطيله قرار تشغيلي؛ الهجرة تجعله ممكنًا فقط.

**وكتلة التحقّق غلطت مرّتين، ومسجَّلتان:**

1. عطّلت الصفّ ثم حاولت إرجاعه نشطًا لتترك الأمور كما كانت — **والقيد رفض، وهو الصواب**:
   إعادة تنشيط صفّ بجوال خام هو بالضبط ما يمنعه. **الفحص حاول إلغاء فعله هو**، فصار
   `SAVEPOINT` يلغيه بدلًا منه.
2. والنصف الثاني طابق **صفر صفوف** لأن النصف الأول كان قد عطّل الصفّ (`AND active_flg`)،
   فقُرئ نجاحًا. كل نصف في معاملة مستقلّة الآن — **الحالة المبدئية تُصنع لا تُفترض**،
   وأداة القياس تسري عليها القاعدة كما تسري على التجهيزة.

## وحارس شكل سياسات RLS — حراسةً لا كسحًا

قياس جلسة الـAPI صحيح: شرط RLS **حاجز أمني** لا يُرفع خارج المسح، فتُنادى
`has_permission` **لكل صفّ** لجواب واحد لا علاقة له بالصفّ. ٥٬٢٦٠ مللي على `request_log`،
و٩ مللي بعد رفعه في استعلام فرعي قياسي (`InitPlan`).

**ولم أكسح الـ١٨٧ الباقية، والسبب رقم من قياسهم:** **صفر سياسة غير مرفوعة فوق الألف
صفّ.** توصيتهم منفَّذة بالكامل — `request_log` عندهم، و`audit_log` بلا سياسات وهو الصواب.
والباقي كلّه على جداول أقل من ألف صفّ: إعادة كتابتها اليوم **١٨٧ فرصة لتغيير معنى مقابل
صفر مكسب مقيس**.

فالقاعدة صارت **فحصًا في `p00`**: سياسة على جدول تجاوز ٥٠٠٠ صفّ بالشكل الخام تُفشل
المجموعة. تضرب **لكل جدول حين يكبر**، ولا تُنسى — وذلك عيب الكسح.

**والحارس أُثبت وهو يفشل، وأوّل مسودّة كانت مكسورة:** lookbehind واحد رصد **الشكلين**
وأعطى نفس العدد لكليهما. وبوستجرس يصيّر المرفوع بشكلين: `( SELECT (hbh.…(` بقوس
للمقارنة، و`( SELECT hbh.…(` بلا قوس للبوليان — فاحتاج lookbehind مزدوج. والإثبات ثلاث
حالات: مصاب يُرصَد · مرفوع يُعفى · **والسكيما فيها سياسة واحدة فعلًا فوق الحدّ**، فالفحص
لا يقرأ مجموعة فارغة.

## و`p11` — تسعة وعشرون تقييمًا متروكًا، وإصلاحي السابق لم يُطبَّق قطّ

التنظيف كان مقيَّدًا بـ`fx.asmt` — تقييم التشغيلة الحالية وحده. فكل تشغيلة تفشل قبل
التنظيف تترك تقييمها **إلى الأبد**، وكل تشغيلة بعدها تضيف واحدًا. تراكم **٢٩** قبل أن
يعدّها أحد: كل تشغيلة جديدة ترى ثلاثين تقييمًا منشورًا وتتوقّع واحدًا، فيفشل فحص رؤية
الأسرة وتترك الثلاثين.

**وكنتُ قد «أصلحته» وبلّغته منجَزًا** — والإصلاح لم يُطبَّق قطّ: تعديل بـ`python` سقط
لأن المفسّر غير موجود، والبديل بـ`sed` لم يطابق. **ولم أتحقّق.** الآن مقيَّد بالأداة
(`PB-LANG`) لا بالتشغيلة، وبشكل «واحد على الأقل، ثم لم يبقَ شيء» — فالعدد المضبوط هو ما
جعل الكومة غير مرئية: كان يمرّ على الصفّ الذي يعرفه ويتجاهل الثمانية والعشرين.

---

## تصحيح على فقرة 0128 — الرمزان قائمان، والدالّة **ترجع NULL**

جلسة الـAPI صحّحت لي شيئين، وقِستُ الاثنين بنفسي فوجدتهما صحيحين:

**١ · جدولي قرأ كأن `HB170` حلّت مكان `HB173`، وليس كذلك.** `canonical_mobile` ترفع
الاثنين، ولكلٍّ فرعه: **`HB173`** حين **لا تستطيع قراءة الرقم أصلًا** (لا يبدأ بـ`+`/`00`/`0`
· لا دولة تُقرأ عليها · دولة ليست في `country_dial_codes` · لا دولة تطابق البادئة)،
و**`HB170`** حين **قرأته وما طابق نمط جوال تلك الدولة**. و`0128` تسمّي الثلاثة
(`23514` · `HB170` · `HB173`) فسلوكها صحيح؛ الخطأ كان في السرد لا في الكود.

**٢ · وأخطر من ذلك: `canonical_mobile` ترجع NULL فعلًا.** قياسًا:
`(NULL,'EG')` و`('abc','EG')` و`('','EG')` → **`NULL`** ثلاثتها.

فالسطر في `CLAUDE.md` (٢٢٧) الذي يقول «ترفع ولا تُرجع NULL، فلا `coalesce` يُكتب فوقها
— و`coalesce(hbh.canonical_mobile(x), x)` **كود ميت**» **غير صحيح، وضرره أنه يدلّ على
حذف حارس**. السطر `IF canon IS NULL THEN RETURN NEW` في `trg_canonical_mobile` **حامل**:
لو حُذف، يكتب `jsonb_populate_record` قيمة NULL فوق الجوال —
`guardians`/`enrolment_applications` يصرخان `23502` لأن العمود `NOT NULL`، أمّا
**`users` و`therapists` فعمودهما يقبل NULL، فيُمحى جوال موظّف بصمت.** قِستُ العمودين:
`nullable=YES` كلاهما.

و`CLAUDE.md` ملفّ المالك، ولا أعدّله بطلب جلسة — مرفوع له بالقياس.

## 0129 — من أي باب جاء هذا السجلّ (BE-01 · BE-02)

أربعة أعمدة: `guardians.registration_source` و`record_completeness`،
و`children.origin_source` و`origin_reference_id`. وثلاث قيم:
`CENTER` · `ENROLMENT_REQUEST` · `ONLINE_CONSULTATION`.

**ولم تنتظر قرار المالك، والحجّة حجّة محلّل الأعمال:** قيمتان من الثلاث **تصفان الجدول
كما هو اليوم** — صفٌّ أنشأه موظّف، وصفٌّ أنتجه `convert_enrolment`. فالعمود وصفٌ صادق
لما سبق أن حدث، وغيابه هو النقص.

**ثلاثة قرارات في الشكل:**

1. **التعبئة الرجعيّة مشتقّة من دليل موجب لا من غياب دليل.** الظهور في
   `enrolment_applications.converted_guardian_id` → `ENROLMENT_REQUEST`؛ و`created_by`
   يسمّي مستخدمًا حقيقيًّا → `CENTER`؛ **ولا شيء → NULL، معدودًا ومذكورًا**.
   `NOT NULL DEFAULT 'CENTER'` كان سيختم كذبًا على كل وليّ أمر جاء من طلب التحاق، ولا
   أحد يعود يسأل عن افتراضيّة بعد نزولها. النتيجة: ١٣ وليّ أمر وطفلان فارغون بالقصد.
2. **ليست `profile_status`.** الاسم مأخوذ: `therapists.profile_status` منذ `0033` يعني
   **النشر على الموقع التعريفي** — محورٌ آخر تمامًا. `record_completeness` يُقرأ بلا مرجع.
3. **والتهجئة `ENROLMENT` بلام واحدة.** الطلب كتبها بلامين، والسكيما بريطانية بالكامل —
   وقيمة مهجّأة على غير ذلك هي الموضع الوحيد الذي تختلف فيه، وهكذا يصير `WHERE` لا يطابق
   شيئًا وهو يبدو صحيحًا.

**والثبات مشغّل لا اتّفاق** — `HB240`. و`NULL → قيمة` مسموح **مرّة**: ذلك **تسجيل** أصل
لا **تغييره**، وهو كيف تُصحَّح الصفوف التي لم تُشتقّ بيد من يعرف. و`قيمة → غيرها` مرفوض.

**وثلاث مرّات عضّني شيء أثناء كتابتها، وكلها نفس العائلة:**

- فحص الرفض اختار صفًّا عشوائيًّا بمصدر فارغ، **فوقع على الوصيّ ٦٦٨ المجمَّد**، ومات على
  `ck_guardians_mobile_e164` وهو يختبر `HB240`. الفحص يختار صفًّا **يقدر أن يكتبه**.
- ثم **التعبئة نفسها** وقعت على الصفّ ذاته: `created_by` بتاعه `dev_admin`، مستخدم
  حقيقي، فطابقه فرع `CENTER` وقتل الهجرة **على عمود لا علاقة له بالجوالات**.
  **الصفّ المجمَّد يجمّد كل عمود يُضاف بعده.** فصارت التعبئة تكتب ما تستطيع وتعدّ ما لا
  تستطيع وتطبع العدد.
- و«لم أستطع الاشتقاق» و«لم أستطع الكتابة» عدّان **منفصلان**، لأن واحدًا منهما فقط عن
  الدليل.

---

## تصحيحان على ما سبق — أحدهما في تشخيصي والآخر في رأس `0129`

**١ · شخّصتُ الجدول الخطأ، والذي أقنعني أن الأرقام طابقت.**

بلاغٌ بفحص أحمر «expected 4, got 5» على «أزواج الأخصائي/الطفل». قرأتُ «أزواج» فذهبتُ
إلى `hbh.caseload`، وقِستُه فوجدتُ **أربعة نشطة وخمسة مميَّزة** — وهو نفس الرقمين
حرفيًّا. فبنيتُ تفسيرًا متّسقًا تمامًا: زوجٌ انتهى تبقى صفوفه، وعدٌّ لا يرشّح
`active_flg` يرى تاريخًا لا حالة.

**والتفسير صحيح في نفسه وخاطئ هنا.** الفحص الواقع على `hbh.therapist_services` —
جدولٌ آخر — **وهو يرشّح `active_flg` على الجداول الثلاثة أصلًا**، فالآلية التي وصفتُها
لا تستطيع أن تنتج الفرق. والسبب الحقيقي أن صفوفًا كانت تُنشأ وتُحذف كل ثلاث ثوانٍ بيد
جلسة أخرى، فالصفّ الخامس عاش عشرين ثانية داخل نافذة القراءة.

**والدرس ليس «اقرأ الفحص» وحده** — بل أن **مطابقة الأرقام ليست تأكيدًا**. وجدتُ جدولًا
يُنتج فيه تفسيري الرقمين المرصودين، فتوقّفتُ عن البحث. وصدفةٌ من رقمين أقوى إقناعًا من
حجّة، ولا تحمل من الدليل شيئًا: السؤال ليس «هل يفسّر هذا الأرقام؟» بل **«هل هذا هو
الشيء الذي فشل؟»**.

**٢ · ورأس `0129` كان يقتبس عدًّا حيًّا.**

كان مكتوبًا «يشتقّ ٤ من ١٨ وليّ أمر و٨ من ١١ طفلًا» — صحيحًا لحظة كتابته، وكاذبًا خلال
ساعة: معظم الصفوف المشتقّة كانت تجهيزات جلسات أخرى فنُظِّفت. والقاعدة تقول الآن أرقامًا
أخرى.

**عدٌّ حيّ مقتبس في رأس مهاجرة قياسٌ يتعتّق كذبًا**، ويقرؤه التالي وصفًا للسكيما. أُزيل
وأُحيل إلى كتلة التحقّق في ذيل الملفّ، فهي **تطبع** العدّين حين تعمل — نفس الحقيقة ومعها
ختمٌ زمني. (التعديل على التعليق وحده؛ لم يتغيّر سطر SQL واحد.)

---

## رحلة العميل الموحَّدة — المرحلة ٠ و١ (`10-FEAT` §9)

| الهجرة | البند | ماذا |
|---|---|---|
| `0130` | `EN-D6` | `HB240` كان بمعنيين والثاني بيدي — صار `HB241` |
| بذور | `RB-D1` | ستّ صلاحيات لـ`CENTER_ADMIN` |
| `0131` | `EN-D2`·`01`·`02` | `mobile_verifications` + طلب الرمز والتحقّق |
| `0132` | `EN-03` | المطابقة قبل الإنشاء |
| `0133` | `EN-D4`·`06` | قواعد الاكتمال وإعادة حسابه |

**لم يُبنَ بعد:** `PR-01`…`PR-03` (المرحلة ١'، الأسعار) — في `11`.

### `0130` — وحارس صار في كل هجرة بعدها

`0129` رفعت `HB240` لـ«الأصل لا يتغيّر»، و`available_slots` ترفعه لـ«لا يجوز قراءة دفتر
الحجز»، و`ops_handlers.go:975` تترجمه `403`. فاستقبال يغيّر أصلًا كان سيُقال له «لا
صلاحية» — **403 لقاعدة عمل**. اخترتُ الرمز بلا `grep`.

**والدرس يوجع:** «معنى ثالث لرمز واحد ليس اختصارًا بل خسارة» مكتوبة في `CLAUDE.md` —
وهذا معنى ثانٍ **بيد من كتب الجملة**. فكل هجرة من `0131` تبدأ بحارس يرفض إن كان الرمز
الجديد مرفوعًا من أي دالّة. **القاعدة تُنفَّذ لا تُتذكَّر.**

### `0131` — رمز التحقّق ليس رمز دخول

بلا `user_id` (قد لا يوجد حساب بعد)، ولا يفتح جلسة، و**`request_otp` لم يُلمس** — قرار
الكشف في 09-05 يخصّ باب الدخول. ولا يبحث عن شيء، فلا يكشف شيئًا ويُرسل للجميع.

**مقيس في معاملة متراجَعة:** الفارغ كلّف محاولة (٥←٤) والـNULL (٤←٣) — درس `0097`؛
و**العدّاد وصل فعلًا إلى ٢** — لو رفعت الدالّة بعد العدّ لرجع صفرًا (D-1). والرمز غير
مخزَّن نصًّا، والصندوق وسّع `ck_sms_body` ليشمل `OTP_VERIFY` **بدل قيد ثانٍ قد ينحرف**.

### `0132` — والمواصفة كما كُتبت كانت ستبني أداة كشف تسجيل

`match_guardian(center, mobile)` ممنوحة لـ`hbh_app` تجيب **كل من يحمل توكنًا**: هل هذا
الرقم مسجَّل هنا؟ — **بالضبط ما بُني باب الدخول كي لا يكشفه.** وليّ أمر في البوّابة كان
سيقدر أن يمشي على قائمة أرقام ويعرف أيّها أسر في المركز.

فالمطابقة طبقتان: **`find_*_matches` داخليتان بلا منحة** (كـ`notify_guardians`)،
يناديهما `submit_enrolment` v2 **بعد أن يُثبت المتّصل حيازة الرقم** — وهي الحالة
الوحيدة التي لا يكون فيها الجواب كشفًا. و**`match_*` محكومتان** للاستقبال بـ
`GUARDIAN.MANAGE`، والمركز **من المتّصل لا معامل**.

**مقيس:** وليّ أمر مسجَّل → `HB255`؛ الداخلية → `42501`؛ الاستقبال → `EXACT`. و«الاسم
بلا التاريخ» → **`AMBIGUOUS` لا `EXACT`** (OD-07: لا دمج آليًّا). والطيّ العربي يطابق
«احمد محمد» مع «أحمد مُحمّد».

### `0133` — والرأس وصف حارسًا لم يكن في الكود

فخّان رأيتُهما قبل الكتابة: **الدالّة تكتب الجدول الذي يناديها مشغّله** (حلقة)، و**الوصيّ
٦٦٨ المجمَّد يقتل أي إعادة حساب على مستوى المركز** — مديرٌ عاجز عن تعديل قواعد الاكتمال
بسبب رقم هاتف من سبعة أرقام.

**وكتبتُ الرأس يصف حارس الحلقة، ثم وضعتُ في المشغّل `WHEN (… OR TRUE)` — صحيح دائمًا.**
الكود كان سينتهي رغم ذلك، بدورة ضائعة، لأن الدالّة لا تكتب إلا حين تختلف القيمة. لكن
**رأسٌ يَعِد بحارس غير موجود هو بالضبط كيف يُحذف الحارس الموجود.** صار مشغّلين: `INSERT`
دائمًا، و`UPDATE` حين **لم** يتغيّر `record_completeness`.

وقاعدة لحقل غير موجود → `HB256`: قاعدة لا يستطيع أحد استيفاءها **تجعل كل أسرة ناقصة
للأبد، ولا شيء فيها يبدو خطأ**.

**مقيس:** قاعدة مطلوبة لحقل فارغ → `PARTIAL`؛ ملؤه → `COMPLETE` **بلا حلقة**؛ الصفّ
المجمَّد **تُخطّي (١) ولم يقتل التعديل (١٤ أُعيد حسابهم)**.

**وما لم يُقَس، ويُقال:** عزل المراكز — القاعدة فيها مركز واحد، فـ«تغيير قاعدة لا يمسّ مركزًا
آخر» صحيح في الكود (`WHERE center_id = p_center_id`) **ولم يُثبَت بمركز ثانٍ**. ولا حالة
`MINIMAL` بتجهيزة مخصّصة.

---

## `db.sh verify` يثبّت أرضه — والأداة التي قاسته كذبت أوّلًا

محلّل الأعمال طلب الحكم **مع الأرض التي قِيس عليها**. و`db.sh verify` لم يكن يثبّت شيئًا
(`grep` على `RUN VOID` = صفر) — فأيّ حكم منه لا يُوثَق به إلّا بفحص يدوي قبل وبعد. وهو
الصنف الذي أصلحه `api.sh verify` بالفعل.

**كشفٌ لا منع.** ثلاث جلسات تطبّق هجرات على القاعدة؛ جولةٌ تتقاطع مع إحداها تُعطي رقمًا
واحدًا عن سكيمتين. **المنع قفل، والقفل قرار المالك** — هذا يرفض فقط أن يعطي حكمًا لا
يستطيع أن يكفله. يُثبَّت `count|max` (لا الأقصى وحده: رقم أقلّ قد ينزل بعد أعلى)، ويُفحص
قبل كل مجموعة **ومرّة بعد الأخيرة**.

### والدرس المكتوب في `CLAUDE.md` حدث حرفيًّا

قِستُ الحارس بدالّتين مزيّفتين في مجلّد مؤقّت — لا بتحريك دفتر حقيقي على قاعدة مشتركة.
**فقال القياس الأوّل إن الحارس لا يعضّ في أي حالة**، حتى التي تحرّك فيها الدفتر.

**وكان الحارس سليمًا.** الأداة كانت تعدّ النداءات بمتغيّر صدفة، و`$(ledger_pin)` **استبدال
أوامر يعمل في صدفة فرعية** — فكل زيادة تقع على نسخة تُرمى، وكل نداء يرى صفرًا ويُرجع
القيمة الأولى. الدفتر «لم يتحرّك» في الأداة أبدًا. **وكنتُ على وشك إصلاح حارسٍ غير
مكسور.** عدّاد في ملفّ ينجو من الصدفة الفرعية، فعضّ الحارس فورًا.

**وقياس الحارس هو ما كشف ثغرته الوحيدة الحقيقية:** دفترٌ **لا يُقرأ إطلاقًا** (مصادقة
فاشلة، قاعدة خاطئة) يقرأ `""` في كل فحص، و`""` يساوي `""`، فتقول الجولة «ثبتت» وتخرج
بصفر — **تُقرأ نجاحًا**. أُغلقت: أرضٌ لا تُقرأ ليست أرضًا.

| الحالة | الخروج |
|---|---|
| نظيفة | `0` · «ثبتت» |
| تحرّكت قبل مجموعة | `2` · `RUN VOID` |
| تحرّكت أثناء الأخيرة | `2` · `RUN VOID` |
| رقم أقلّ نزل بعد أعلى (العدد تحرّك، الأقصى ثابت) | `2` · `RUN VOID` |
| القراءة الأولى فارغة | `2` · `RUN VOID` |
| **كل القراءات فارغة** | `2` · `RUN VOID` ← **كان `0`** |

## 2026-09-12 — Decision Log: قرارات المالك على الأسعار والرموز والانحدار

قرارات **المالك**، مسجَّلة هنا بما نُفِّذ منها فعلًا وما ينتظر. معرّفات القرار في
`10-FEAT` §3 ملك تحليل الأعمال.

| # | القرار | الحال |
|---|---|---|
| D-a9 | تعديل في الاختبار وحده بلا تغيير في القاعدة أو الخادم **يُتراجَع عنه**؛ لا يُغيَّر Expected Result ليمرّ الاختبار | **نُفِّذ.** كتلة `PAIRS` في `tests/api/a9_verify.sh` (تعديل 13:40) كانت اختبارًا فقط — أُعيدت حرفيًّا إلى فحص `and matches what the schema holds`. إن احمرّ من حركة جلسات أخرى يُبلَّغ مشكلةَ تجهيزة، لا يُعاد تعريف المتوقَّع |
| D-257 | `HB257` = `NO_EFFECTIVE_CATALOGUE_PRICE` ← `409` | الرمز حيّ؛ الاسم عند الـAPI |
| D-258 | `HB258` = `PRICE_OVERRIDE_FORBIDDEN` ← `403` **وحده** | في هجرة محجوزة |
| D-262 | تجاوز على سطر بلا `service_id` ← رمز مستقل `422` | `HB262` — أول رمز حرّ، مقيس: لا دالّة حيّة ترفعه ولا هجرة. في الهجرة المحجوزة |
| D-259 | `HB259` = `BILLING_MODEL_DISALLOWS_CHARGE` ← `409` | الرمز حيّ؛ الاسم عند الـAPI |
| D-260 | `HB260` = `PRICE_MANAGEMENT_FORBIDDEN` ← `403` **وحده** | في هجرة محجوزة |
| D-051 | خدمة مركز آخر في `set_service_price` **لا** `HB260` ولا رمز خاصّ: `HB051` بنصّ «غير موجود» نفسه | في هجرة محجوزة. **مُثبَت** في معاملة مُتراجَع عنها بمركز ثانٍ: معرّف غير موجود ومعرّف غريب يرجعان `HB051` **بنصّ متطابق**. `HB263` أُسقط ولا يُحجز |
| D-241 | `HB241` باسمه ومعناه | بلا تغيير |
| D-strict | `INVOICE_REQUIRE_CATALOGUE_PRICE = false` | مقيس `false` (صفّ عامّ). لا يُقلب إلا بعد: الطبقة ترسل `NULL` لا `0` لسعر غير مُدخَل · كل الأسعار محمَّلة · انحدار منفصل |
| D-227 | `CLAUDE.md:227` يعكس العقد بعد `0135`/`0136` | **نُفِّذ** (السطر صار ~266): الترتيب **صلاحية ← الصفّ ← موجود ولي (جواب واحد) ← الحالة ← الكتابة**، مع سطر صريح أن الفصل ما زال محجوزًا |
| D-reg | انحدار كامل على `136\|0136` | **الأرض لم تعد موجودة**: `0137` و`0138` طُبِّقتا فوقها و`reset` ممنوع على قاعدة مشتركة. الجولة على الأرض الحالية، مسمّاة |
| D-lock | قفل `db.sh migrate` بعد جولة بصفر فشل | القفل **كان قائمًا قبل الجولة** (`scripts/lock.sh`، جلسة أخرى). لم يُنزَع |

**الهجرة المحجوزة** (`split_add_invoice_line` · `split_set_service_price` · `fix_grant_portal_access`
← `HB261`) خارج `db/migrations/` حتى يرسل الـAPI «الصورة بُنيت» بالخريطة أعلاه. الرقم يُحجز
لحظة النقل (الحرّ الآن `0139`).

### الانحدار — ثلاث جولات، والأوليان لم تكونا معزولتين

| الجولة | الأرض والنافذة (UTC) | النتيجة |
|---|---|---|
| ١ | `138\|0138` ثابتة · ~20:05–20:07:57 · **بلا قفل** | **1195 فحصًا، 2 فشل — في `p10`**. لم تُلتقط تفاصيلهما: فلترتُ المخرَج بـ`grep` فأسقط جدول الفشل **وابتلع كود الخروج** |
| — | — | `p10` وحدها بعدها: 94، صفر |
| ٢ | `138\|0138` ثابتة · 20:08:44–20:09:21 · **بلا قفل** | 17 مجموعة، 1195 فحصًا، صفر فشل، `EXIT=0` |
| ٣ | `138\|0138` ثابتة · 20:12:10–20:12:47 · **داخل القفل** | **17 مجموعة، 1195 فحصًا، صفر فشل، `EXIT=0`**. في النافذة لا مستخدم API (`a*_`) ولا `hbh_app`؛ الكتّاب خارج بادئات `p*` هم `admin`/`dev_admin`/`dev_reception`/`anonymous` — هويّات تستعملها المجموعات نفسها (13 ملفًّا)، بالبصمة ذاتها في كل جولة DB سابقة. القفل حُرِّر بعدها. **هذه هي الجولة المعتمدة.** |

**لماذا جولة ثالثة.** `v_audit_trail` يُظهر جولة API كاملة لجلسة الـback end من 20:02 إلى ما
بعد 20:09 (`a10_*` ثم `a1_*`…`a9_*`، ومعها `hbh_app`) — تحمل القفل،
وتتقاطع مع **الجولتين الأوليين كلتيهما**. متّسق مع فشلَي `p10` وليس برهانًا عليهما. (نسبتُهما
أوّلًا إلى نافذة 19:52 — تلك جولة سابقة لا علاقة لها؛ ساعة القاعدة هي ما صحّحها.)
**والثغرة حقيقية:** `api.sh verify` و`db.sh migrate` يأخذان القفل، و**`db.sh verify` لا
يأخذه** — تثبيت الدفتر يكشف هجرة تنزل ولا يرى جولة تكتب في الجداول نفسها. الجولة الثالثة
شُغِّلت **داخل القفل** بغلاف خارجي، دون تعديل `db.sh`. جعلُ ذلك افتراضيًّا قرار المالك.

**وخطأ مني في النافذة نفسها:** تراجع `a9` كُتب 20:02:12 UTC والقفل بيد جولة الـback end —
لم أسأل `lock-status` قبل الكتابة. جرت `a9` عندهم ~20:09 على الملفّ بعد التراجع كاملًا (لا
ملفّ مختلط)، لكنه ليس الملفّ الذي بدأت به جولتهم. أُبلغوا بالتوقيت والكاتب.

**والـback end لم يعتمد الخريطة الجديدة ولا تراجع `a9` بعد:** تعليمة المالك في محادثته كانت
«افصل HB258 وHB260»، ورسالة نظير لا تغيّر قرارًا تلقّاه مباشرةً — فعرض الصيغتين على المالك
وينتظر. صورته `5eddcbb6` بُنيت بخريطته هو، **فليست «الصورة بُنيت» للهجرتين المحجوزتين**.

**ولا تغطية مقبولة بعدُ لـ`0129`–`0138`:** كل برهانها داخل الهجرات نفسها وفي معاملات
مُتراجَع عنها. مجموعة `p17` هي الخطوة التالية.

### تكملة — `0140` نزل (2026-09-12 · 20:21 UTC)

المالك أكّد للـback end في محادثته نفسها: «اعتمد تسمية جلسة قاعدة البيانات، وأبقِ `a9` على
الفحص القديم». الصورة `613bec8a` تحمل الخريطة (`HB263` مُزال منها)، فنزلت الهجرة المحجوزة:
`0140_one_code_one_meaning` — الفصل الثلاثي و`HB261` في ملفّ واحد، والأجسام الثلاثة مقيسة
مطابقة للحيّ قبل الكتابة، وتجربة جافّة في معاملة متراجَع عنها، ثم `migrate` تحت القفل.

| البرهان بعد النزول (معاملة متراجَع عنها بمركز ثانٍ، `dev_admin`/`hbh_app`) | الجواب |
|---|---|
| خدمة غير موجودة · خدمة مركز آخر (`set_service_price`) | `HB051` · `HB051` **بنصّ متطابق** |
| نوع سعر مجهول | `HB264` |
| تجاوز على سطر بلا خدمة | `HB262` |
| سطر فاتورة بخدمة غريبة | `HB051` |
| وليّ أمر ثانٍ على هاتف مشترك وحسابه لآخر | `HB261` |

**الجولة داخل القفل على `140\|0140`** (20:21:58–20:22:34): 17 مجموعة، 1195 فحصًا، صفر فشل،
`EXIT=0`. والـback end يشغّل جولة API كاملة على الأرض نفسها.

**الترقيم:** `0139` لجلسة `HBH-010` (نسبة الحضور)، `0140` هذه، **وطبقة أ لخطط الدفع `0141`**.

## `0141` — خطط الدفع، الطبقة أ (`OD-33` · `11` §15 و§17.4) — **مبنيّة، محجوزة للـAPI**

**النطاق بعد حكم المعماري:** القوالب · أقساطها · `invoice_installments` بآلتها وتاريخها ·
`issue_invoice` v2 · مشغّل المجموع · السداد بالترتيب · `override_installment_schedule` **داخل
جدول الأقساط** (`SUPERSEDED` + ختم على الفاتورة) · `BILLING.SCHEDULE_OVERRIDE`. **لا**
`policy_overrides` ولا `limit_overrides` (اقتراح `01-proposals` ١.٧ ينتظر المالك).
**خارجها:** مشغّل `AFTER_SESSIONS` والتفعيل عند `seq 1` (الطبقة ب مع الباقات) · تعليم
`DUE`/`OVERDUE` الزمني وإشعاراته (PP-03/05 — المهلة سؤال مفتوح).

**تسعة رموز جديدة، `HB265`–`HB273`**، أُرسلت للـback end بمعانيها. الملفّات في مجلّد الجلسة
المؤقّت لا في `db/migrations/` — والبذور معها (`0004_payment_plans` + ذيل `0002_rbac`)،
لأن البذور تُشغَّل مع كل `migrate` فتضع صلاحية لا يفحصها شيء.

| البرهان (كلّه في معاملات متراجَع عنها على `140\|0140`) | النتيجة |
|---|---|
| 37 مجسًّا على PP-AC-01/02/04/05/07 وحدود الرفض — بالرمز، وبـ`CONSTRAINT_NAME` لـ`23505`/`23514` | 37 / 0 |
| `p00` والهجرة داخل المعاملة | 37 / 0 |
| صعود ثم نزول: الدوالّ (بصمة `pg_get_functiondef`) والأعمدة | 0 فرق · منحة `issue_invoice(integer)` تعود |
| البذور مرّتين | صفّ واحد لكل خطّة · منحة واحدة |

**وفشلان في الجولة الأولى من المجسّات كانا في توقّعاتي لا في الكود، وصُحِّحا بسبب مكتوب:**
عدد صفوف التاريخ 7 لا 6 (3 إصدار + سداد `seq 1` + 2 `SUPERSEDED` + 1 جديد — أخطأتُ العدّ)،
وقسط `FULL` بحالة `PENDING` لأن `create_invoice` تضع `due_date` بعد 14 يومًا
(`INVOICE_DUE_DAYS`) والقسط يتبع تاريخ الفاتورة — وأُضيف تأكيد على المساواة بدل الحالة وحدها.

**قرارات مُبلَّغة لتحليل الأعمال (قابلة للنقض قبل النزول):** `CANCELLED` و`SUPERSEDED` حالتان ·
القسط الأخير يحمل الباقي، والزيادة تُرفض `HB268` · إعادة الإصدار `HB050` · لا جدول للفواتير
السابقة · «اليوم» تاريخ المركز المحلّي · `FULL` يُستحقّ في تاريخ الفاتورة · `DEPOSIT_50` مسوّدة.

### تكملة `0141` — §17.5 محسومة، و`OD-38`

تحليل الأعمال حسم الستّ في `11` §17.5: خمسة بافتراضي، وتعديل واحد — **تعليم التقويم في طبقة أ**.
فأُضيف إلى `0141`، **بلا رمز جديد**:
- `mark_installment_dues()`: القسط `DUE` يوم استحقاقه، و`OVERDUE` حين اليوم > `due_date + INSTALLMENT_GRACE_DAYS` (بارامتر عامّ `0`). مهمّة في `run_maintenance`، ومعالجها الخاصّ.
- ثلاثة أنواع إشعار: `INSTALLMENT_DUE` و`INSTALLMENT_OVERDUE` للأسرة، و`STAFF_INSTALLMENT_OVERDUE` لموظّفي **مركز القسط** الذين تحمل أدوارهم `BILLING.MANAGE`.
  **لا `notify_role`:** بلا هوية (والصيانة بلا هوية) يخاطب الدور في **كل** المراكز.
  والإشعار مرّة لكل قسط، والقفز `PENDING`→`OVERDUE` في تشغيلة واحدة يُبلِغ «متأخّر» وحده.
- تاريخ القسط صار **ثابتًا كمبلغه** (`HB269`)، والتغيير يمرّ عبر التجاوز.

و`OD-38`: `DEPOSIT_50` تُبذر `ACTIVE` بقسطها الثاني ٥٠٪ بعد ٦ جلسات. تُنشأ فقط إن غابت عن المركز،
حتى لا يعيد `migrate` تفعيل خطّة سحبها المركز. و`HB269` صار **500** بقرار الـback end
(عيب منّا لا خطأ متّصل). و`23514 ck_invoice_installments_total` **غير قابل للوصول بالبناء**، فيبقى تأكيدًا.

**والنزول لا يضيّق `ck_ntf_kind`:** القيد يسري على كل صفّ محذوفًا أو لا، والإشعارات المرسَلة رسائل
وصلت فعلًا، لا تُمحى ليمرّ ملفّ نزول.

| البرهان على `140\|0140`، في معاملات متراجَع عنها | النتيجة |
|---|---|
| مجسّات: PP-AC-01..07 + الحدود + الساعة والإشعارات + البذور الحقيقية | **53 / 0** |
| `p00` مع الهجرة | 37 / 0 |
| صعود ثم نزول | 0 دالّة، 0 عمود |
| البذور مرّتين | صفّ واحد لكل خطّة، ومنحة واحدة |

**وخطأ أداة في الطريق:** `ALTER TABLE ... DISABLE TRIGGER` داخل المجسّ رُفض لأن فحص المجموع
المؤجَّل ترك أحداثًا معلّقة. أُطلقت الفحوص المؤجّلة أوّلًا (`SET CONSTRAINTS ALL IMMEDIATE`)،
وهي تأكيد إضافي أن كل ما كُتب قبلها يجمع صحيحًا.

**الترتيب المتّفق عليه:** `0150` (الواجهة، يرفع `HB290`) ← صورة الـAPI (أسماء `0141` + نداء
`update_report` بسبعة) ← `0141` تحت القفل ← `0151` بعد تشغيل الصورة. أرقام الواجهة
`0150`/`0151`، وأنا من `0142` صعودًا عند الحاجة.

### إعادة ترقيم — خطط الدفع صارت `0142` (2026-09-13)

`p00` يفشل على أي فجوة في الدفتر (`p00_verify.sql:129`)، وملفّا الواجهة كانا `0150`/`0151` —
أوّل `migrate` كان سيسجّل `0150` بعد `0140` فيحمرّ الحارس على الفجوة لا على عيب. **الأرقام تتبع
ترتيب التطبيق:** `0141` تزامن مسوّدة التقرير (الواجهة، `HB290`) ← الصورة ← **`0142` خطط الدفع** ←
`0143` حذف `update_report` السداسية. حارس `0142` يطلب `0141` مطبَّقة.

الملفّات المحجوزة انتقلت من مجلّد الجلسة المؤقّت إلى **`db/held/0142_payment_plans/`** ليقرأها
المعماري — لا شيء في `scripts/` يمسح `db/held`. وأُعيد البرهان على الملف المرقَّم من مكانه: 53/53،
والدفتر `140|0140`.

**ودرس صغير من الطريق:** حجز الواجهة كان ملفًّا كاملًا داخل `db/migrations/` — أي `migrate`
كان سيطبّق `0151` (يكسر كل حفظ مسوّدة على الصورة الجارية). نُقل بعد التنبيه. **الحجز رسالة
ومجلّد خارج `db/migrations/`، لا ملفّ فيه.**

## `0141` و`0142` نزلتا — والتنظيف كسر مرّتين قبل أن يصير الرقم أخضر (2026-09-13)

| الخطوة (UTC) | ما حدث |
|---|---|
| `0141` (الواجهة) | قُرئ كاملًا · تجربة جافّة مع `p00` (37/0) · `migrate` تحت القفل ← `141\|0141` |
| الصورة `d7c8c40a` | الـback end: الأسماء التسعة داخل الثنائيّة، و`a5` 188/0 على `141` |
| `0142` + بذورها | إعادة البرهان على `141` (53/53) ← `migrate` تحت القفل ← `142\|0142` · `FULL` افتراضية · `DEPOSIT_50` نشطة · الصلاحية ممنوحة مرّة |
| 09:13–09:14 جولة DB | **2 فشل في تنظيف `p6`**: إصدار الفاتورة صار يكتب أقساطًا، فحذف فواتير الفيكسترة اصطدم بالمفتاح الأجنبي، وبقيت صفوفها على القاعدة |
| 09:17–09:26 | **العيب نفسه في `a3`/`a5` عند الـback end**: التنظيف عطّل خمسة مشغّلات مضاف-فقط ثم مات على الحذف، و`psql` وقف قبل إعادتها، والتعطيل كان قد التزم — **فبقي سجلّ المواعيد والجلسات ودفتر الباقات قابلًا للتعديل تسع دقائق**. أُعيدت 09:26:16، ولم ألمسها |
| 09:29 | بقايا `p6` أُزيلت بالهوية (سجلّ التدقيق: أُدرجت 09:14:06 داخل جولتي) **في معاملة واحدة** — فشلٌ فيها يتراجع عن التعطيل معه |
| 09:31:01–09:31:59 | **18 مجموعة · 1294 فحصًا · 0 فشل · `EXIT=0` · `142\|0142` ثابتة · لا مشغّل معطَّل** |

**الدروس الثلاثة، وكلها عن التنظيف لا عن الهجرة:**
1. **هجرة تضيف ابنًا بمفتاح أجنبي إلى جدول تحذف منه الفيكسترات تكسر كل تنظيف يحذف الأب** — في مجموعات لا تملكها. قبل تطبيق هجرة كهذه: `grep` على `DELETE FROM hbh.<الأب>` في `tests/`.
2. **تعطيل مشغّل في ملفّ `psql` ليس محميًّا بمعاملة.** مع `ON_ERROR_STOP on` يقف الملفّ قبل `ENABLE` والتعطيل ملتزم؛ مع `off` يُكمل. التعطيل والحذف والإعادة **في معاملة واحدة**، وإعادة التأكيد بالاسم.
3. **عدّة CTEs تحذف لا ترتيب بينها** (والدرس مكتوب في `CLAUDE.md`) — ووقع مجدّدًا: نفس الجملة مرّت في `p17` وفشلت على بقايا `p6` لأن `trg_line_guard` يسأل عن فاتورة قد تكون حُذفت أو لم تُحذف بعد.

**`p17_verify.sql` — مجموعة قبول دائمة لـ`0142`:** 91 فحصًا · PP-AC-01..07 · القيود باسمها (`CONSTRAINT_NAME`، D-40) · الساعة داخل كتلة تتراجع عن نفسها وتحمل نتائجها في رسالة الاستثناء، لأن `mark_installment_dues` تمشي على أقساط **كل** المراكز في قاعدة مشتركة. وقبل أن تمرّ فشلت مرّتين **بخطئي فيها**: دالّة مساعدة لم تُعرَّف، وتجاوز بمجموع 9000 على فاتورة يُدان منها 10000 — ورفضتها الدالّة بـ`HB272` كما يجب.
