# Hand By Hand (new) — PostgreSQL · Go · Angular

## ما هذا
منصّة إدارة مركز مهارات وعلاج للأطفال. بدأت ستاكًا جديدًا **بجوار** النظام العامل
في `../hand by hand` (Oracle 21c XE + APEX 26.1)، ومنذ **2026-09-03** صارت **بديله**.

**PostgreSQL هو الإنتاج ومصدر الحقيقة.** أوراكل تتوقّف — راجع D-26 قبل أي كود،
ففيه ما سقط من القواعد القديمة ولماذا. ووثائق المجال في `../hand by hand/docs/`
تبقى مرجعًا يُقرأ في مكانه ولا يُنسَخ.

> نصّ هذه الفقرة كان يقول «النظام الحالي هو الإنتاج» حتى بعد القرار، وهو ينقض
> القسم الذي يليه مباشرةً. أي جلسة تقرأ من أعلى كانت تصطدم بالجملة القديمة أولًا
> وتتصرّف بها. صُحِّحت 2026-09-03.

## النطاق الحالي
**بوابة ولي الأمر + تطبيق المركز.** منذ **2026-09-03** صار PostgreSQL مصدر الحقيقة
وتوقّفت أوراكل — قرار المالك، مسجَّل بأسبابه وبما سقط من القواعد في **D-26**.

> **الشرط التشغيلي للقرار:** APEX يصير للقراءة أو يُطفأ. نظامان يكتبان أسوأ من أيٍّ
> منهما وحده، وهو الخطر الذي وُضعت القاعدة ١ أصلًا لمنعه.

## خمس قواعد تحكم كل شيء هنا
1. **مصدر حقيقة واحد: PostgreSQL.** لا كتابة مزدوجة، ولا قاعدة ثانية تحمل بيانات حقيقية.
2. **قواعد العمل تُكتب في PL/pgSQL** — لا في Go ولا في Angular. الطبقة تنقل الإجابة ولا تقرّر.
3. **الحذف ناعم دائمًا** (`active_flg` + تاريخ حذف). **لا منحة `DELETE` على أي جدول**،
   وبيانات الطفل السريرية لا تُحذف فعليًا دون إجراء امتثال موثَّق.
4. **كل وصول — قراءة وكتابة — يمرّ عبر RLS**، والصلاحية تُفحص **داخل السياسة** لا في
   الكود. شرط في المعالِج يصير نسخة ثانية من قاعدة، والأضعف هي التي تقرّر.
5. **الحالات آلات حالة** مقيَّدة بجدول تاريخ — لا تحديث حرّ لعمود حالة، ولا من مدير المركز.

## الستاك — مثبَّت
- PostgreSQL · Go · Angular · PL/pgSQL
- **لا React ولا Vue.** Angular وحده في هذا المجلد.
- الرسوم البيانية: مكتبة واحدة تُختار مرة وتُوثَّق. لا خلط.

## القرارات المقفلة المنقولة من المشروع الحالي
هذه قرارات **عمل**، لا تخصّ محرّك قاعدة البيانات، وتنتقل كما هي:

- **مصر.** `EGP` · `Africa/Cairo` · عطلة الجمعة والسبت · تقويم ميلادي · رقم قومي 14 رقمًا ·
  جوال `01XXXXXXXXX`. **ولا شيء منها مكتوب في الكود** — كلها بارامترات.
- **طفل واحد لكل جلسة.** لا جلسات جماعية، ولا جداول مشاركين.
- **بث مباشر فقط — ولا تسجيل، أبدًا.** لا جدول تسجيلات، ولا إشارة إلى مقطع.
  "لحظة مهمّة" ملاحظة سريرية بختم زمني، لا مؤشّر داخل تسجيل.
  هذه قاعدة دائمة ولا علاقة لها بالمساحة.
- **الحذف ناعم** (`active` + تاريخ حذف). بيانات الطفل السريرية لا تُحذف فعليًا
  دون إجراء امتثال موثَّق.
- **الحالات آلات حالة مقيَّدة بجدول تاريخ** — لا تحديث حرّ لعمود حالة.

## الوقت
**كل لحظة تُخزَّن UTC وتُقارَن UTC.** الخادم على توقيت والمستخدمون في مصر على آخر،
وعمود بلا منطقة زمنية يجعل الطرفين يفترقان بساعتين بصمت. حدث هذا فعلًا في المشروع
الحالي: عطّل قفل الحساب، وأبطل كل رمز تحقّق لحظة إصداره. التحويل للعرض فقط.

## التسمية
- **Go**: اصطلاح Go. لا بادئة `hbh_` على الأنواع.
- **قاعدة البيانات**: `snake_case`، جمع للجداول. **البادئة `hbh_` ليست مطلوبة هنا** —
  كانت ضرورية في أوراكل لأن السكيما مشتركة مع تطبيق آخر ليس لنا. هذه القاعدة لا تنتقل.
- **Angular**: اصطلاح Angular.

## الأمان
- التخويل يُفرض في الخادم. إخفاء زر ليس ضابطًا.
- ولي أمر لا يصل لبيانات طفل آخر بتعديل رابط. تُفرض بـ RLS، لا بشرط في الصفحة.
- **الهوية تفشل مقفولة**: لا هوية = صفر صفوف، لا كل الصفوف.
- توكنات البث: معتِمة · ≤15 دقيقة · مُجزّأة عند التخزين · مربوطة بجلسة وكاميرا ومستخدم.
- لا رابط كاميرا ولا عنوان IP ولا بيانات اعتماد في أي مكان يبلغه عميل.
- كل قراءة حسّاسة تُسجَّل صراحةً — المشغّلات لا تلتقط القراءات.
- لا بيانات شخصية في رابط أو معامل استعلام.

## نصوص
تعليقات الكود بالإنجليزية. نصوص الواجهة بالعربية، عبر ملفات الترجمة لا داخل القوالب.
**ولا رمز تعبيري في كود يُشحن** — سبقت أن جعلت حزمة غير قابلة للترجمة برسالة خطأ
تشير إلى مكان آخر تمامًا.

---

# دروس مدفوعة الثمن

كل سطر هنا كلّف دورة تشغيل. تُقرأ قبل الكتابة، لا بعد الفشل.
القرارات المعمارية وراءها في [`docs/01-stack-decisions.md`](docs/01-stack-decisions.md).

## PostgreSQL

- **RLS يُتجاوَز لمالك الجدول وللـ superuser افتراضيًا.** API يتصل بالمالك يجعل كل سياسة
  في السكيما **ديكورًا** — بلا رسالة خطأ وبلا أثر في أي لوج. لذلك يوجد دور `hbh_app`
  منفصل: `NOSUPERUSER`, `NOBYPASSRLS`, ولا يملك جدولًا. ومجموعة القبول تتأكّد من
  الخصائص الثلاث **بالاسم**، لأن ضياع أي واحدة يبطل كل ما بعدها بصمت.
- **سياسة تسمح بصفوف `center_id IS NULL` تسرّبها لاتصال غير مصادَق.** الصفوف العامّة —
  بارامتر نظام، قيمة بحث — تحقّق الشرط بصرف النظر عمّن يسأل. كل سياسة تبدأ بـ
  `hbh.current_center_id() IS NOT NULL` **قبل** أي شرط آخر. فحص المركز وحده لا يكفي؛
  فحص الهوية هو ما يقفل الباب.
- **`SET LOCAL` وليس `SET` لهوية الطلب.** مسبح اتصالات Go يعيد استخدام الجلسات، فهوية
  مضبوطة على مستوى الجلسة تتسرّب من طلب إلى الطلب التالي. هي نفس بقّة `HBH_PKG_UI`
  التي استعملت `BOOLEAN` عامًّا كأنه حالة لكل صفحة.
- **`set_config(..., true)` معاملة-محلّية، وكل `CALL` في psql معاملة مستقلّة.** فالهوية
  تضيع قبل الفحص التالي، ويقرأ الفحص كفشل سياسة بينما السياسة سليمة تمامًا. مجموعة
  الاختبار تضبط الهوية على مستوى الجلسة؛ الـ API يفعل العكس عن قصد.
- **CTE يُعدِّل البيانات غير مرئي لبقيّة جملته.** الطرفان يقرآن اللقطة نفسها المأخوذة عند
  بدء الجملة، فعدّ سجل التدقيق بجوار الإدراج الذي يفترض أنه كبّره يقارن رقمًا بنفسه —
  ويفشل دائمًا. الإدراج والتأكيد جملتان منفصلتان.
- **والصفّ لا يُحدَّث إلا مرة واحدة في الجملة الواحدة.** `UPDATE` ثانٍ لنفس الصفّ داخل
  نفس الجملة **يُهمَل بصمت** — لا خطأ ولا تحذير. جملة تنقل موعدًا من `BOOKED` إلى
  `CONFIRMED` ثم إلى `CHECKED_IN` تتركه عند `CONFIRMED`، فيُرفض الاختبار التالي بـ
  `HB024` بدل `HB023` — رفض للسبب الخطأ لا يثبت شيئًا. كل انتقال حالة جملة مستقلّة.
- **وCTE قرائي لا يشير إليه أحد لا يُنفَّذ إطلاقًا.**
  `WITH p AS (SELECT some_function()) SELECT ...` لا تستدعي الدالة إذا لم تُذكر `p` في
  الاستعلام الخارجي — بلا خطأ وبلا تحذير. النشر «نجح» ولم يحدث، وثلاثة فحوص بعده فشلت
  بلا أي إشارة إلى السبب. (CTE يُعدِّل البيانات يُنفَّذ دائمًا، مُشار إليه أو لا — وهذا ما
  يجعل الفخّ محيّرًا.) القاعدة: **إمّا أن يُشار إلى الـ CTE، وإمّا أن يكون نداءً مباشرًا.**
- **وجملة تُعدِّل البيانات شرعية داخل `WITH` فقط، لا داخل استعلام فرعي عادي.**
  `SELECT ... FROM (INSERT ... RETURNING) x` خطأ نحوي — ومع `ON_ERROR_STOP off` تمرّ
  المجموعةُ فوقه ويبقى مفتاح التجهيزة `NULL`، فتفشل ثلاثة عشر فحصًا بعده لسبب لا يذكره
  أيٌّ منها.
- **العرض يعمل بصلاحيات مالكه ما لم يُقَل له غير ذلك.** والمالك هنا يتجاوز RLS على كل
  جدول، فعرضٌ فوق جدول محميّ بسياسة **يسلّم وليَّ الأمر كل أطفال المركز** — بصمت وبلا
  خطأ في أي مكان. كل عرض يُنشأ بـ `WITH (security_invoker = true)`، ومجموعة المعايير
  العامة تفشل إن ظهر عرض واحد بدونها.
- **فحص يعدّ صفوفًا في جدول append-only ينجح مرة ويفشل للأبد بعدها.** الجدول يحتفظ بصفوف
  التشغيلة السابقة — وهذا هو الغرض منه. كل عدّ يُقيَّد بوقت بدء التشغيلة.
- **`UNIQUE` عادي يعتبر كل NULL مختلفًا عن غيره — نفس فخّ أوراكل.** لكن العلاج يعتمد على
  **معنى** الـ NULL، وخلطهما يصنع العيب بدل أن يمنعه:
  - NULL **قيمة** (`sys_params.center_id`: NULL تعني «عام») ← `UNIQUE NULLS NOT DISTINCT`.
    صفّان عامّان بنفس الكود تكرار حقيقي.
  - NULL **غياب** (`children.national_id`: NULL تعني «غير معروف بعد») ← **فهرس جزئي**
    `WHERE national_id IS NOT NULL`. مجهولان ليسا تكرارًا، و`NULLS NOT DISTINCT` هنا
    ترفض **ثاني** طفل بلا رقم قومي — وهو بالضبط العيب الذي وصل للإنتاج في أوراكل، في
    مركز لأطفال صغار في مصر كثير منهم بلا رقم قومي.

  **واختباره بصفّين على نفس جانب القاعدة** — صفّ بمركز وصفّ بدونه لا يتصادمان أبدًا،
  وهكذا نجا العيب من ٧٣ اختبارًا.
- **`WHEN OTHERS` يلفّ جسم الدالة كله يبتلع أخطاءك أنت.** `audit_attempt` كانت ترفع
  `HB012` عند استعمال خاطئ، ثم يبتلعها معالجها ويحوّلها إلى تحذير — فالمنادي يمرّر فعلًا
  غير مسموح ويرجع له «نجاح». المعالج يلفّ **الشيء الوحيد الذي قد يفشل فعلًا** ولا شيء
  غيره. الاختبار وحده هو ما كشفها.
- **المقارنة الثلاثية القيم تفشل مفتوحةً، والمصادقة أسوأ مكان لذلك.**
  `public.crypt(NULL, hash)` تساوي NULL، و`hash <> NULL` ليست **false** بل **UNKNOWN**،
  و`IF UNKNOWN THEN` لا تُنفَّذ — فيُتخطّى فرع «الرمز خاطئ» ويسقط التنفيذ إلى السطور التي
  تستهلك الرمز وترجّع النجاح. في `verify_otp` كان ذلك يعني أن **رمزًا فارغًا (NULL) يُسجِّل
  الدخول باسم صاحب الرقم** — والحماية الوحيدة كانت شرطًا في معالِج HTTP، أي النسخة الأضعف
  من القاعدة (القاعدة ٤). كل مقارنة تقرّر سماحًا أو منعًا تُكتب `IS DISTINCT FROM`،
  والمدخل الفارغ يُرفض صراحةً **مع احتساب المحاولة** — رفضه كخطأ تحقّق يمنح المهاجم
  محاولات مجانية بلا حدّ. (المهاجرة 0097.)
- **المسار الذي يسبق المصادقة لا يستطيع قراءة جدول محميّ بـ RLS.** رمز الدخول يُصدَر قبل
  أن توجد جلسة، فالاتصال بلا هوية، و`current_center_id()` تساوي NULL، فالسياسة على
  `hbh.users` لا تطابق شيئًا. أول محاولة لقراءة `center_id` من هناك رجّعت **صفر صفوف في كل
  مرة، بصمت**: مسار التسليم لم يفعل شيئًا وأبلغ عن 202. والهوية تفشل مقفولة بالضبط كما
  صُمّمت — فالعلاج ليس ثقبًا في السياسة بل أن **ترجّع الدالّةُ `SECURITY DEFINER` التي
  وجدت المستخدم أصلًا ما تعرفه** (المهاجرة 0095). ودالّة بحث جديدة كانت ستصنع قدرة تجيب
  «هل هذا الرقم مسجَّل؟» لكل من يناديها.
- **`dblink` بلا `user=` يتصل بمستخدم نظام الخادم `postgres`.** والدور ده مش موجود في هذه
  الحاوية — المالك اسمه `hbh_owner`. الفشل يرجع `08001 could not establish connection`،
  وهي رسالة تقرأ كمشكلة شبكة وليست كذلك.
- **`down` يحذف الجداول قبل الدوال.** السياسة تعتمد على الدالة التي تناديها، فحذف
  `can_access_child` وسياساتها قائمة يفشل — والمهاجرة كلها في معاملة واحدة، فلا يُحذف
  **شيء**، ويعيد البناءُ استعمالَ التعريفات القديمة بصمت، ويبدو التصحيح الذي كُتب للتوّ
  وكأنه لا يعمل. حذف الجدول يأخذ سياساته معه.
- **قيد `CHECK` قد يمنع اختبارًا من افتعال حالة.** `expires_at > issued_at` يمنع سحب
  الانتهاء إلى الماضي. الحل تقليص النافذة إلى ميلي ثانية — الصفّ يبقى شرعيًا ويصير
  منتهيًا — لا إضعاف القيد ليمرّ الاختبار.
- **القاعدة الشاملة للسكيما لا تعيش في مجموعة مرحلة.** فحوص «كل جدول له أعمدة تدقيق»
  و«كل مفتاح أجنبي له فهرس» كانت في مجموعة P1، فكسرتها المهاجرة 0002 وظهر الانحدار
  باسم المرحلة الأولى. مكانها `tests/db/p00_verify.sql` — مجموعة تنمو مع السكيما.
  **واستثناءات القاعدة تُسجَّل في `hbh.convention_exemptions` بسبب مكتوب**، لا في ملف
  الاختبار: قاعدة استثناءاتها داخل اختبارها هي قاعدة تعدّلها كل مرحلة بصمت.
- **لا معاملات مستقلّة، والقاعدة العامة: الدالة إمّا تغيّر حالة وإمّا ترفض — لا الاثنين.**
  أي `UPDATE` يليه `RAISE` في نفس الدالة **يضيع**، لأن الاستثناء يفكّ المعاملة ويأخذ
  التغيير معه. وقعت مرتين في هذا المشروع:
  - `verify_otp` كانت سترفع بعد زيادة عدّاد المحاولات — أي **تجربة رموز بلا حدّ**؛
  - و`consume_package_session` كانت تعلّم الباقة `EXPIRED` ثم ترفع، فلا تُعلَّم أبدًا.

  العلاج واحد: الدالة التي تعدّ أو تعلّم **ترجع حالة**، والتعليم يصير نداءً مستقلًّا لا
  يرفع شيئًا (`hbh.expire_packages`). راجع D-1.
- **تحديث صفّ ثم نداء دالة تحدّث نفس الصفّ في **جملة واحدة** يرفع `27000`** —
  «tuple to be updated was already modified by an operation triggered by the current
  command». حدّ حقيقي في المحرك، لا عيب في القاعدة المُختبَرة. جملتان.
- **`timestamptz` دائمًا.** `timestamp` بلا منطقة هو العيب الذي عطّل قفل الحساب وأبطل كل
  رمز تحقّق لحظة إصداره. اختبار القبول يفشل إن ظهر عمود واحد بلا منطقة.
- **`SECURITY DEFINER` بلا `search_path` مثبَّت هو تصعيد صلاحيات ينتظر.** كل دالة كهذه
  تحمل `SET search_path = hbh, pg_catalog`.
- **دالة تُنادى من داخل سياسة على جدول تقرأه هي recursion.** `current_center_id` تقرأ
  `users`، والسياسة على `users` تناديها. `SECURITY DEFINER` هو ما يكسر الحلقة.
- **الـ `CAST` في فهرس الأسماء العربية لا ينتقل.** كان في أوراكل علاجًا لـ ORA-01450،
  ولا وجود للمرض هنا. نقله نقلُ علاج لمرض غير موجود.
- **نمط `LIKE` بلا `ESCAPE` يُطيع ما كتبه المستخدم بدل أن يبحث عنه.** المصطلح بارامتر
  مربوط، فهذه **ليست** ثغرة حقن — هي العيب الأهدأ تحته: `%` تعني «كل شيء»، و`_` تعني
  «أي حرف». استقبالٌ يكتب `%` وحده يُسلَّم **كل أطفال المركز**، واسمٌ فيه `_` يطابق
  الأسرة الخطأ. وأسوأ موضع له `hbh.users`، لأن **كل اسم مستخدم في هذه السكيما فيه
  شرطة سفلية**، فبحثٌ يبدو سليمًا يرجّع الزميلة الخطأ.

  و`crud.go` كان قد حلّها يوم كُتب — `likeEscape` يضاعف الشرطة المائلة ثم يهرّب `%`
  و`_`، وكل نمط ينتهي بـ`ESCAPE '\'`. لكن `portal.go` و`identity.go` كُتبا بعده ولم
  يأخذا العلاج، **ولا شيء كان يربط الثلاثة**. العلاج نصفان لا ينفع أحدهما وحده:
  التهريب عند الربط، و`ESCAPE` على كل نمط — و`ESCAPE` فوق مصطلح لم يهرّبه أحد **زينة**
  تسمّي حرفًا غير موجود.

  ومع الطيّ العربي **يسبق التهريبُ الطيَّ**: `likeEscape` في Go ثم `normalize_arabic`
  فوق نتيجته. وهو الترتيب الصحيح لأن الدالة لا تمسّ الشرطة المائلة — تُصغّر الحروف،
  وتحذف التشكيل والتطويل، وتطوي بدائل الألف والياء والتاء المربوطة، وتضغط المسافات
  وتقصّ الأطراف؛ والشرطة المائلة ليست في أيٍّ من مجموعات `translate` وليست مسافة.
  `hbh.normalize_arabic('أ\%حمد')` ترجّع `ا\%حمد` — الألف انطوت والشرطة باقية.

  **واختباره بمصطلح يفرّق بين السلوكين، لا بمصطلح لا يطابق شيئًا في الحالتين.** «`_`
  لا يرجّع شيئًا» تأكيد صحيح على الأطفال وخاطئ على المستخدمين — بعد العلاج تطابق `_`
  كل اسم مستخدم فيه شرطة سفلية، أي كلّهم. الشكل الذي يفرّق: خُذ اسمًا حقيقيًّا وبدّل
  حرفًا واحدًا منه بـ`_` (`a7_ther_pist`) — كان يجده يوم كانت `_` بدلًا، ولا يجد شيئًا
  يوم صارت حرفًا. **ويتبعه فحص قبول**: الاسم الحقيقي بشرطته السفلية **ما زال يُوجد** —
  فـ`ESCAPE` خاطئة ترفض كل اسم مستخدم في السكيما، وفحص الرفض وحده يبقى أخضر وهي تفعل.
  وقبل كل ذلك: **القائمة غير المفلترة ليست فارغة**، وإلا فـ«صفر صفوف» لا يثبت شيئًا.
  والفحوص في `a4` (الأطفال) و`a7` (المستخدمون)، والحارس الآلي في `scripts/api.sh lint`.

## الصلاحيات

- **تجاوز إداري مبنيّ على صلاحيات وحدها يُعطي الدور الذي يملكها لعمله سلطةً على عمل
  الجميع.** `can_close_session` سمحت بإغلاق الجلسة لمن يملك `CHILD.VIEW_ALL` و
  `SESSION.COMPLETE` — ودور `THERAPIST` يملك الاثنتين (الأولى ليغطّي زميلًا، والثانية
  ليُغلق جلساته هو)، فصار **أي أخصائي يغلق جلسة أي أخصائي آخر**. التجاوز الإداري يُقيَّد
  بـ `user_type = 'STAFF'`. الخط: **الإكلينيكي يُغلق عمله هو، والإداري يُغلق عمل أي أحد** —
  تبريران مختلفان، ففرعان مختلفان. مجموعة القبول هي التي وجدتها.
- **وحقّان لهما رمزان منفصلان يحتاجان بوّابتين منفصلتين.** إغلاق الجلسة `SESSION.COMPLETE`،
  وكتابة ملاحظتها `SESSION.NOTES.EDIT`. بوّابة واحدة تخدم الاثنين تعيد تعريف أيّ المنحتين
  تتجاهلها بصمت.
- **الصلاحية تقول «ماذا»، ولا تقول «أيّ صفّ» أبدًا.** RLS هي التي تجيب النصف الثاني —
  و`SECURITY DEFINER` هي بالضبط المكان الذي لا تُطبَّق فيه. فدالّة تأخذ معرّفًا خامًّا
  وتسأل `has_permission` وحدها تكتب في أي مركز يُعطى لها. **ثماني دوالّ كانت كذلك
  دفعةً واحدة**، وأُثبت سبع منها: `issue_invoice` و`decide_request` (الأخيرة أرسلت
  **إشعارًا بنصّ المهاجم إلى أسرة مركز آخر**)، ثم `sell_package` و`publish_session_note`
  و`grant_consent` (وهي التي تقرّر **من يشاهد طفلًا أثناء جلسته**) و`withdraw_consent`
  و`set_password` — وهذه الأخيرة **استيلاء على حساب موظّف في مركز آخر**.
  الترتيب داخل كل دالّة: **اقرأ الصفّ ← هل هو موجود ← هل هو لي ← هل يحقّ لي ← هل
  الحالة تسمح ← ثم اكتب**. والفحص الآلي في `p00_verify.sql` يمنع عودة الصنف كلّه.
- **و«لا مسار يصل إليها» خاصيّة الموجّه لا الدالّة.** الموجّه يتغيّر أسرع من السكيما
  بكثير. `publish_assessment` — التي **تُشعر الأسرة** — لم يكن لها مسار، وكانت ستسلّم
  رسالة عن طفل مركز آخر في اليوم الذي يُضاف فيه واحد.

## الهجرات

- **المهاجرة تُنشئ البنية. وأي `INSERT` يقرأ جدولًا تملؤه البذور مكانه ملفّ البذور.**
  `scripts/db.sh` يطبّق **كل** المهاجرات أولًا ثم ملفات البذور، فوقت تشغيل المهاجرة تكون
  `hbh.roles` و`hbh.centers` **فارغة** على قاعدة معاد بناؤها. الـ `INSERT ... SELECT`
  الذي يقرأ منها لا يطابق شيئًا، ولا يُدرج شيئًا، **ويُبلغ عن النجاح**. المنح لم تحدث،
  وسلسلة الترقيم لم تُنشأ، وثلاثة عشر فحصًا في مجموعة P4 فشلت بلا أي إشارة إلى السبب.
  ومجموعة المعايير العامة صارت تفحص أن البذور نزلت فعلًا.
- **ملفّان برقم واحد = هجرة تضيع بصمت.** الأول يُطبَّق ويسجّل رقمه، والثاني يصير
  «مطبَّقًا سلفًا» ويُتخطّى بلا كلمة. حصل فعلًا: شخصان رقّما هجرة `0003`، ودالة الفحص
  التي يحتاجها الـ API عند الإقلاع لم تصل القاعدة أبدًا، والقاعدة تبدو سليمة تمامًا.
  `db.sh migrate` صار **يرفض ويقف** عند أي رقم مكرّر قبل أن يشغّل شيئًا. **وحين يعمل
  أكثر من شخص على السكيما، احجز رقمك قبل أن تكتب الملف.**

- **والمهاجرة التي لا تسجّل رقمها تُعاد إلى الأبد.** ملفّان كُتبا بلا
  `INSERT INTO hbh.schema_migrations` في آخرهما، فبقيت أعمدتهما في الجداول والدفتر لا
  يعرف عنهما شيئًا — و`migrate` يعيد تشغيلهما كل مرة. ولم يقع شيء، لأن
  `ADD COLUMN IF NOT EXISTS` آمنة؛ وهذا بالضبط ما يجعلها خطرة، فأول سطر غير idempotent
  يُضاف هناك يسقط بلا سبب ظاهر. **وسكيما فيها عمود لا يفسّره الدفتر أسوأ من دفتر يُظهر
  قرارًا اتُّخذ ثم نُقض.**

- **السكيما والكود لا يتحرّكان في نفس الخطوة، والذي ينتظر هو الذي يقع إن تحرّك أولًا.**
  الاتجاهان وقعا في يوم واحد:
  - **الكود ينتظر:** هجرة رفعت `SQLSTATE` جديدًا وطُبِّقت قبل بناء الخدمة، فسقط الرمز
    في `default` وصار **500** — والشاشة قالت «حدث خطأ غير متوقَّع» عن قاعدة عمل رفضت
    بالقصد. لذلك تحمل رؤوس `0053` و`0058` و`0059` سطر «الـAPI ينزل أولًا».
  - **السكيما تنتظر:** هجرة حذفت `children.photo_url` وجملة `SELECT` حيّة ما زالت
    تقرؤه، فصارت **كل** قراءة طفل `42703` — البوّابة وكونسول المركز معًا، وواحد
    وأربعون فحصًا في مجموعات الـ API. **العمود الذي ما زال يقرؤه أحد يُحذف على
    خطوتين — القارئ أولًا — أو لا يُحذف.**

- **`for f in $(ls ...)` يقسم المسار على المسافة.** المشروع تحت «سطح المكتب» — بمسافة —
  فالسطر يموت على مسارين غير موجودين. مصفوفة ومؤشّر، لا `$(ls)` ولا `$(find)` بلا `-print0`.
- **heredoc كبير فيه عربي يُكسر أحيانًا في هذه البيئة.** الملفات الكبيرة تُكتب بأداة
  الكتابة المباشرة، لا بـ `cat > f <<EOF`.
- **`local out rc=0` يبتلع كود الخروج.** المتغيّر مضبوط فعلًا، فـ `${rc:-1}` يرجع صفرًا
  ويقرأ الفشل كنجاح. الفشل يرجّع `1` صراحةً.
- **Git Bash يترجم المسار الذي داخل الحاوية كأنه مسار ويندوز.** `-v "$src:/etc/nginx/conf.d/x"`
  يصير وجهته `C:/Program Files/Git/etc/nginx/conf.d/x`، فيُنشئ دوكر المسار الغريب **بلا خطأ**
  ويقلع الحاوية سليمة — والصورة تعمل بملفّها الافتراضي. الأعراض تقرأ كعيب في الكونفيج
  المُختبَر لا في التجربة: `404` على كل مسار، وصفحة ترحيب nginx بـ 615 بايت تبدو صفحة
  التطبيق لمن لا يعدّها. `docker inspect --format '{{range .Mounts}}{{.Destination}}'` هو ما
  يكشفها في ثانية. العلاج `MSYS_NO_PATHCONV=1` قبل أي `docker run -v`.
- **`cut -d: -f1` على مخرَج `grep -rn` يقطع عند حرف القرص.** فحص لينت جديد أخذ اسم
  الملف بهذا الشكل، وعلى مسار `C:/Users/...` صار الاسم `C` — فأبلغ أن **كل** ملف ينقصه
  `likeEscape`، برسالة لا تذكر الملف ولا القرص. ونجا العيب لأن المشروع يُشغَّل من
  Git Bash بمسارات `/c/...`: الفحص كان أخضر طوال ما كان مكسورًا، ولم يظهر إلا على
  شجرة اختبار مكتوبة بمسار ويندوز. الاسم يُؤخذ بقصّ الذيل `:رقم:محتوى`
  (`sed -E 's/:[0-9]+:.*$//'`)، لا بالقطع عند أول نقطتين. وهي عائلة
  `MSYS_NO_PATHCONV` نفسها: مسار ويندوز يمرّ في أداة تفترض غيره.
- **وفحصُ لينت لا يُصدَّق حتى يُرى وهو يفشل.** الثلاثة أعلاه كُتبت ومرّت خضراء من أول
  تشغيلة — وواحدة منها كانت مكسورة. البرهان شجرةٌ صغيرة لكل حالة: نظيفة تمرّ، ومصابة
  تُرفض، **وفارغة تُرفض أيضًا** لأن فحصًا قرأ صفر أنماط لم يثبت شيئًا. وتُبنى الشجرة في
  مجلّد مؤقّت، لا بكسر ملف في الشجرة الحيّة: قد تكون جلسة أخرى تبني منها الآن.
- **النصّ العربي في سطر أوامر Git Bash لا يصل إلى البرنامج عربيًّا.** على ويندوز يمرّ
  الوسيط بصفحة ترميز قديمة، فكل حرف عربي يصير `?` **قبل** أن يخرج الطلب. جُرِّب رمز الدخول
  بأرقام عربية هكذا: `curl ... -d '{"mobile":"٠١٢٢٥٢٨٣٨٣٨"}'`، فخرج الجسم حرفيًّا
  `{"mobile":"???????????"}`، وردّ الـ API `VALIDATION` / `FORMAT` — **وهو محقّ**: رفض
  علامات استفهام. وقُرئ الرفض عيبًا في المنتج، وأُبلِغ المالك بأن الأرقام العربية
  «تُرفَض منذ ما قبل التصليح». ولم يكن فيها عيب: النمط يحمل `U+0660`–`U+0669` فعلًا،
  و`canonical_mobile` تقرؤها `+201225283838`، والرسالة تصل `SENT`.
  **الجسم الذي فيه عربي يُكتب في ملف ويُرسَل منه:** `printf '%s' '...' > body.json`
  ثم `curl --data-binary @body.json`. و**`--trace-ascii` هو الحكم** — يطبع ما خرج على
  السلك فعلًا، فإن رأيت فيه `?` مكان الحروف فالتجربة لم تُجرَ. والطريق نفسه يسلكه أي
  نصّ عربي يُمرَّر وسيطًا — `psql -c` داخل `docker exec` مثلًا — فالاستعلام الذي فيه
  عربي يُمرَّر من ملف على stdin.
  وهي عائلة «أداة القياس تُصنع لها حالتها» و«ما الذي رفضه؟» معًا: **رفضٌ لمدخل لم يصل
  لا يثبت شيئًا عن المدخل**، وأخطر ما فيه أنه يبدو برهانًا على عيب لا وجود له.

## الاختبار

- **مجموعة تموت قبل حكمها تُقرأ كنجاح.** كل فحص داخل معالج استثناء خاص به يسجّل الفشل
  بدل أن يُسقط التشغيلة. والحكم يُطبع دائمًا، ثم — وبعده فقط — يُرفع الاستثناء الذي يجعل
  كود الخروج غير صفري.
- **أكّد رمز الخطأ نفسه.** فحص ينتظر رفضًا ويأتيه رفض لسبب آخر لم يُثبت شيئًا ويبدو أخضر.
  **والثمن دُفع مرّتين.** أداة إثبات كانت تستنتج «مرفوض» من *غياب* صفّ نجاح داخل
  savepoint، فأبلغت أن ستّ دوالّ مغلقة على قاعدة `sell_package` فيها مفتوحة بالبرهان:
  `convert_enrolment` كانت تُردّ بقاعدة عمل عن حالة الطلب، و`publish_session_note`
  نجحت فعلًا — والشكلان متطابقان لمن يقرأ «هل رُفع استثناء؟». أُعيدت كتابتها لتسجّل
  `SQLSTATE`، فأجابت القاعدة نفسها: **خمس كتابات عابرة للمراكز نجحت**. السؤال ليس
  «هل فشل؟» بل **«ما الذي رفضه؟»**.
- **والرفض لا يكفي: الحالة المبدئية تُصنع.** نفس الأداة رأت `HB010 number series
  CHILD is not defined` وسجّلتها رفضًا أمنيًّا — بينما الدالّة كانت قد تجاوزت كل فحوص
  التخويل ووقفت عند تسلسل ناقص في تجهيزة الاختبار. مركز ثانٍ في الاختبار يحتاج
  **سلاسل ترقيمه** كما يحتاج صفوفه.
- **وكود الخروج وحده لا يقول إن شيئًا شُغِّل.** `ng test` ردّ **صفرًا** ثلاث مرات
  والمتصفّح لم يقم أصلًا: `Cannot start ChromeHeadless — running as root without
  --no-sandbox`. صفر اختبار نُفِّذ، والأمر يقول «نجح». **اقرأ عدد ما نُفِّذ، لا كود
  الخروج** — وهي نفس عائلة البذور التي «نجحت» ولم تُدرج شيئًا، والتنظيف الذي حذف صفر
  صفوف فمرّ. (الحلّ هنا `CHROME_BIN` يشير إلى غلاف يمرّر `--no-sandbox`، وداخل
  `hbh-web-portal` وحدها: `node_modules` على القرص مبنيّ لويندوز، فـ`esbuild` يموت في
  أي حاوية لينكس أخرى.)
- **أكّد التجهيزة بالاسم قبل الاختبارات، لا من خلالها.** قطعة منسيّة تظهر بعد خمسين فحصًا
  كرفض محيّر من كود سليم.
- **`cmd | grep -q ... && echo A || echo B` يقلب جوابه تحت `pipefail`.** حالة الأنبوب هي
  حالة أي عضو فشل فيه، لا حالة `grep`. فدالة مثل `psqlapp` ترجّع 3 عند خطأ SQL، ويصير
  الفحص «المنحة مفتوحة» بينما `permission denied` مطبوع أمامه حرفيًّا. الشكل الصحيح:
  **التقط المخرَج في متغيّر أولًا، ثم افحصه** — وهي نفس عائلة `local out rc=0`.
- **تنظيف يبتلع فشله أسوأ من غياب التنظيف.** التنظيف فحص مسجَّل مثل غيره.
- **التجهيزة تبني الحالة، والدالة تختبرها.** وتجهيزة تنادي دالة المنتج للوصول إلى حالة
  تترك خلفها تاريخًا لم يحدث — **والأثر يبقى حين يكون السجلّ مضاف-فقط**. تجهيزة أرادت
  «لا موافقة هنا» فكادت تنادي `withdraw_consent`، وهي تكتب حدث سحب في
  `hbh.consent_events`: صفّ لا يُحذف، مع كل تشغيلة، على طفل بعينه — فيصير السجلّ يقول
  إن أهله سحبوا موافقتهم عشرين مرة. `UPDATE` مباشر يبني الحالة ولا يخلّف شهادة كاذبة
  في المكان الذي يُفترض أنه أصدق ما في السكيما.

## قاعدة بيانات مشتركة بين جلستين

- **الفحص يعدّ تجهيزته هو، لا الجدول كله.** `count(*) = 5 FROM therapist_working_hours`
  انكسر لأن جلسة أخرى تركت خمسة صفوف في نفس القاعدة — ففشل الفحص على بيانات لا علاقة لها
  بالقاعدة المُختبَرة. **كل عدّ يُقيَّد بمفتاح من `hbh_test.fx`.**
- **والتقييد بالمفتاح لا يكفي وحده: عددٌ بعينه ينكسر يوم يُعاد استعمال رقم.** المعرّفات
  تُعاد بعد حذف صلب، فيجرّ رقم الطفل معه صفوفًا من حياة سابقة — والعدّ مقيَّد بالمفتاح
  الصحيح ويفشل مع ذلك. الشكل الذي يصمد شكلان في واحد: **«واحد على الأقل، ثم لم يبقَ
  شيء»**. الخاصية المقصودة أنه لم يبقَ لهذا الصفّ أثر، لا أن حساب تشغيلة بعينها ضبط.
- **وكل تأكيد على صفّ يسمّي الصفّ.** فحوص `maintenance_runs` كانت تقرأ «آخر تشغيلة»،
  وحاوية الصيانة تعمل بجدولها الخاص — فأول تقاطع بينهما يفشل ويبدو عيبًا وليس كذلك.
  التشغيلة تُلتقط بمعرّفها ويُؤكَّد عليها بالاسم.
- **مبادرة `DECLARE` تعمل قبل أول سطر في الجسم.** بناء `tstzrange` هناك حدث **قبل** فحص
  النافذة المقلوبة، فرُفعت `22000` بدل إرجاع `BAD_WINDOW` — والحارس يجب أن يسبق، فالبناء
  يأتي بعده.

## تعديل ملفات SQL من الصدفة

- **`perl -0pi -e` و`sed` على ملف فيه `$q$` يأكلها.** الصدفة تفسّر `$q` كمتغيّر، فيتحوّل
  `$q$ WITH ...` إلى `\ ...` و`$q$);` إلى رقم المستخدم — والملف يمرّ بلا خطأ نحوي ظاهر
  ثم يموت عند التشغيل بـ `invalid command \`. حدث في أربع مجموعات في وقت واحد.
  **الكتل التي تحوي `$q$` تُعدَّل بأداة الكتابة المباشرة، لا بالصدفة.**

## قاعدة مشتركة بين جلستين — تكملة

- **`db.sh reset` ممنوع على قاعدة يشاركك فيها أحد.** الهدم سحب السكيما من تحت عملية أخرى
  كانت تطبّق مهاجراتها في نفس اللحظة، فقرأ `migrate` جدول المهاجرات وهو يُملأ بالتوازي
  وتخطّى ما لم يُطبَّق. على قاعدة مشتركة: `migrate` فقط.
- **مشغّل `BEFORE` يعمل قبل فحص `WITH CHECK` في RLS.** فقاعدة العمل تُجيب أولًا وتحجب رفض
  الصلاحية تمامًا: دفعة على فاتورة مسوّدة تُرفض بـ `HB052` لأي أحد، والاستقبال لا يصل إلى
  السياسة أصلًا. اختبار الصلاحية يحتاج صفًّا **تقبله قاعدة العمل** أولًا.
- **فتح الكتابة يغيّر شكل الرفض لا وجوده.** قبل المنحة كان الجواب `42501`؛ بعدها المنحة
  موجودة والسياسة هي التي تقرّر، فالتحديث الممنوع **يطابق صفر صفوف ويُبلغ عن النجاح**.
  التأكيد يعدّ الصفوف، لا يلتقط استثناءً — وإلا نجح الاختبار يوم يوسّع أحدهم السياسة
  ليمرّ الجميع.
- **عدّة CTEs تُعدِّل البيانات لا ترتيب بينها.** حذف الأب والابن في جملة واحدة ينجح أيامًا
  ثم يفشل حين تتغيّر الخطة — والفشل يبدو متقلّبًا وهو في الحقيقة **غير معرَّف**. جملة لكل
  مستوى، والآباء آخرًا.

- **تنظيف يحذف بالتشابه يمدّ يده إلى بيانات غيره.** كانت مجموعة P5 تنهي عملها بـ
  `DELETE FROM hbh.guardians WHERE mobile LIKE '015000000%'` — والنمط طابَق
  `01500000062`، وهو صفّ من فيكسترة **سيشن الـ API** في نفس القاعدة المشتركة. النتيجة
  ثلاثية: الحذف مات على مفتاح أجنبي، فتراجعت جملة التنظيف كلها **تاركة صفوف P5 نفسها**،
  فانفجرت الجولة التالية بواحد وعشرين فشلًا لا علاقة لأيّ منها بالسبب. **بادئة في نمط
  ليست نطاق أسماء:** `015000000%` و`01500000062` يتشاركان تسعة أحرف. التنظيف يحذف
  **بالهوية** — معرّفات مسجَّلة في `hbh_test.fx`، أو قيد يربط الصفّ بمستخدمي المجموعة
  نفسها — لا بتشابه رقمي يستطيع جار أن يتعثّر فيه.
- **وبادئة التنظيف لا بد أن تكون البادئة التي استعملتها الفيكسترة فعلًا.** الإصلاح
  الميكانيكي أعلاه كتب `username LIKE 'p10.%'` و`'p11.%'` — بينما المجموعتان تسمّيان
  مستخدميهما `pa.` و`pb.`. النمط لم يطابق شيئًا، فلم يُحذف شيء، **ولم يُبلَّغ عن خطأ**:
  حذف صفر صفوف نجاح. اقرأ الفيكسترة قبل أن تكتب التنظيف.
- **مشغّل التدقيق ينسخ الصفّ كاملًا — بما فيه الملفّ.** `to_jsonb(NEW)` على جدول مرفقات
  يضع كل بايت من كل ملف داخل `audit_log`، مرّتين عند التعديل. المرفقات لها مشغّل تدقيق
  خاصّ يطرح `content`. أي جدول يحمل `bytea` كبيرًا يحتاج نفس المعالجة قبل أن يُربط
  بالتدقيق العامّ.
- **`$$` داخل نصّ مزدوج الاقتباس في الصدفة هو رقم العملية.** `$$$file$$` في
  `backup.sh` توسّع إلى `237hbh-....dump`، وPostgres ردّ بـ «trailing junk after
  numeric literal» — رسالة لا تشير إلى الصدفة إطلاقًا. الاقتباس الدولاري لا يُكتب داخل
  `"..."`؛ استعمل اقتباسًا مفردًا مع مضاعفة الفواصل.
- **الأرشفة نقل، والحذف حذف.** أي «سياسة احتفاظ» تُقترح على `hbh.audit_log` تُرفض:
  الصفوف تنتقل إلى `audit_log_archive` وتبقى قابلة للسؤال عبر `hbh.v_audit_trail`.
  ودالة تعطّل مشغّل حماية لتفعل ذلك **تتحقّق من إعادته قبل أن تعود** — وإلا فقد تركت
  سجلّ التدقيق قابلًا للكتابة وسكتت.
- **فحص يقرأ معرّفًا من قائمة مفهرسة بالمركز يأخذ صفّ غيره عاجلًا أو آجلًا.** القائمة
  تحتوي صفوف الآخرين بحكم تصميمها — وهي تعمل بالضبط كما يجب. مجموعة a5 قرأت
  `request_id` من قائمة الطلبات وبتّت فيه، فوقع الاختيار على صفّ **بيانات عرض** لسيشن
  ثالث وختمته بـ`decided_by = a5_admin`. لا تنظيف يصلح هذا، لأن الخطأ ليس بقايا:
  **كل معرّف يدخل فحصًا يُقرأ بمفتاح الفيكسترة نفسها** — بالطفل، بالرقم المرجعي — لا من
  ترتيب قائمة مشتركة.
- **وقبل حذف صفّ لا تعرفه، اسأل سجلّ التدقيق.** الصفّ الذي طُلب مني حذفه بوصفه «من
  فيكسترتك» لم يكن كذلك: `v_audit_trail` أظهر أنه أُدرج في **نفس معاملة** تقرير تقدّم
  برقم عرض `RPT-2026-9001` بيد `dev_admin` — بيانات عرض لطرف ثالث. العلاج لم يكن الحذف
  بل تصحيح النسبة الكاذبة إلى من قرّر فعلًا، فانفكّ الحاجز والصفّ باقٍ. **حذف صفّ لفكّ
  حاجز اختبار يوسّع الضرر ولا ينهيه.**
- **فحص «الباب مقفول» يُشغَّل بدور المالك يثبت أن المالك يمرّ منه.** كتبت فحصًا يؤكّد أن
  إدراجًا مباشرًا في `session_notes` يُرفض بـ`42501` — ومرّ الإدراج، لأن المجموعة في تلك
  النقطة تعمل بـ`hbh_owner`، والمنح وRLS **كلاهما** يُتجاوَز للمالك. ثم فشل فحصان بعده
  على الصفّ الذي تركه. القاعدة المكتوبة في هذا الملف منذ اليوم الأول، ووقعت فيها في
  اختبارها هي: **كل فحص لرفض يسبقه `SET ROLE hbh_app` صراحةً**، وإلّا فهو يختبر المالك.
- **وتجهيزة لا تستطيع إنتاج الرفضين لا تستطيع التفريق بينهما.** مجموعة فيها أخصائية
  واحدة — وهي صاحبة الجلسة — تُمرّر أي رمز خطأ يرجّعه الكود لأن حالة «يملك الصلاحية ولا
  يملك الجلسة» **غير قابلة للتكوين فيها أصلًا**. تحتاج زميلة تملك `SESSION.NOTES.EDIT`
  ولا تملك جلسة، **وتُؤكَّد صلاحيتها بالاسم قبل الاختبارات**: بغير ذلك يظلّ الفحص أخضر
  وهو يثبت رفض صلاحية عادي، يوم يضيّق أحدهم دور `THERAPIST`.
- **وفحص رفض بلا فحص قبول بعده لا يثبت أن البوّابة تفتح — يثبت أنها مغلقة.** بوّابة
  موافقة الصورة كانت تسأل عن `consent_type = 'PHOTO'`، ولا وجود لهذا النوع: السكيما
  تحمل `PHOTO_USE` منذ بُنيت الموافقات. فكانت **بوّابة بلا مفتاح** — كل صورة تُرفَض،
  والرسالة تطلب من الاستقبال تسجيل موافقة لا تكتبها أي دالة في السكيما. والفحص الأول،
  «بلا موافقة يُرفض»، **مرّ أخضر** وبدا أن البوّابة تعمل. ما كشفها هو الخطوة التالية:
  سجّل الموافقة وتوقّع أن يمرّ الرفع. الرفض كان مطلقًا لا مشروطًا.
- **والحالة المبدئية تُصنَع لا تُفترَض.** «لا توجد موافقة بعدُ» كان افتراضًا في تجهيزة،
  وثلاثة أشياء تكسره: مجموعة أخرى تسجّل موافقات من النوع نفسه، ومعرّفات الأطفال تُعاد
  بعد حذف صلب، ومجموعة تترك صفوفًا وراءها. وأيٌّ منها يجعل فحص الرفض الذي يليه
  **يختبر لا شيء**: يقول «الجملة نجحت» فيُقرأ كبوّابة سقطت، وهي أصلًا لم تُسلَّح.
  التجهيزة تسحب الحالة صراحةً قبل أن تختبر غيابها.
- **الواجهة التي تُنشر ليست هي الواجهة التي كُتب لها الخادم.** خادم ثابت واحد
  (`deploy/server/web.native.mjs`) يخدم ثلاثة أصول: البوّابة والكونسول والموقع
  التعريفي. كُتب للتطبيقين، فكان يمرّر `/api/` و`/healthz` **لكل** أصل يخدمه — بما
  فيها الموقع، وهو الوحيد المنشور على دومين حقيقي. فصار
  `POST https://hbhskills.com/api/v1/auth/otp/request` يردّ `200` ومعه `dev_code`:
  **رمز دخول أي وليّ أمر، لأي غريب يعرف رقم موبايله، على الإنترنت المفتوح.** ظلّ
  كذلك إحدى عشرة دقيقة.
  وثلاثة أشياء صحيحة لم تمنعه: البوّابة نفسها لم تُنشر قطّ، والمنافذ كلها على
  `127.0.0.1`، وتوجيه النفق يسمّي الموقع وحده. **المرور جاء من داخل الأصل المسموح
  به**، ولا شيء في طبقة النشر يستطيع رؤيته. و`hbh.nginx.conf` كان يقول هذا صراحةً
  في رأسه — «بلوك الموقع بلا `proxy_pass`، ولا تُضِف مسار API عشان فورم التسجيل» —
  لكن القاعدة كانت مكتوبة في ملفّ nginx الذي **لا يعمل**، لا في الخادم الذي يعمل.
  **القاعدة تُكتب حيث تُنفَّذ.** واختبار النشر يسأل عمّا يجب ألّا يُجاب، لا عمّا يجب
  أن يُجاب: `/` بـ`200` لا يثبت شيئًا، و`/api/v1/...` بغير `404` يثبت كل شيء.

- **والمرّة التالية لم يكن المكشوف هو المسار، بل التعليمات.** نفس الأصل، وقد صار
  بلا أي طريق إلى الخدمة — ومع ذلك كان يسلّم ثلاثة ملفّات: `/README.md` و
  `/CONTENT-AUDIT.md` بـ`200` لأن الخادم الثابت يخدم **أي** ملفّ تحت جذره ويردّ
  `application/octet-stream` لامتداد لا يعرفه؛ وبينهما كل جدول خلف الموقع،
  وصلاحية النشر، وأرقام المهاجرات، وأي شهادات أسرة محجوبة لغياب موافقتها.
  وأثقلها الثالث: `config.js` — يُخدَم **بالضرورة** لأنه يحمل عنوان البوّابة — كان في
  تعليقاته ستّ إشارات إلى `OTP_ECHO` وجملة صريحة أن
  `POST /api/v1/auth/otp/request` يعيد رمز الدخول في جسم الردّ، وأن من يعرف رقم
  موبايل وليّ أمر يدخل بحسابه. أي: **شرح الثغرة منشور على الصفحة التي تدعو الأسر
  إلى التسجيل**، بعد أن أُغلقت الثغرة نفسها على هذا الأصل.
  ورأس الملفّ كان يقول حرفيًّا «لا يحمل سرًّا أبدًا؛ كل ما فيه يُخدَم للإنترنت
  المفتوح» — والتعليق تحته ينقضه بعشرين سطرًا. **قاعدة مكتوبة في نفس الملفّ الذي
  تُخرَق فيه لا تمنع شيئًا.**
  ثلاثة دروس، وكلها تعمّم:
  - **الشرح ليس سرًّا بذاته؛ هو خريطة.** يوفّر على قارئه السؤال الوحيد الذي يفصل
    بين من يعرف وبين من لا يعرف. **السبب يُكتب حيث لا يُقرأ من الخارج** — انتقل
    إلى `deploy/server/PORTAL-LINK.md`، وبقي في الملفّ المنشور ما يحتاجه من ينشر.
  - **قائمة السماح أقوى من قائمة المنع.** لا شيء كان يربط إلى الملفّين، و«اسم لا
    يخمّنه أحد» ليس ضابطًا: `README.md`. فصار جدول `TYPES` في `site.native.mjs`
    هو قائمة السماح نفسها — وما ليس فيه `404`. إضافة نوع سطر واحد، ونسيانه `404`
    لا نشرًا صامتًا.
  - **وقائمة السماح تُشتقّ من السكيما لا من الصفحة.** `.mp4` لم يكن فيها، وفي
    `hbh.site_team_media` صفّ `VIDEO` حقيقي ينتظر النشر: أول نشر من الشاشة كان
    سيردّ `404` على الشيء الوحيد المعروض. لا شيء في الصفحة اليوم يطلبه — وهكذا
    تصير قائمة السماح فخًّا مؤجَّلًا.

  **والفحص يُثبَت بأن يسقط قبل الإصلاح.** `check_no_docs` شُغِّل على الخادم القائم
  أولًا فسقط بالملفّين بالاسم، ثم مرّ بعده. فحص أخضر من أول مرّة لم يُثبت أنه يقرأ
  شيئًا — وهي نفس عائلة `ng test` الذي ردّ صفرًا بلا متصفّح.
- **مكان المشروع يقرّر أين تذهب مخرجاته ما لم تقرّر أنت.** كانت النسخ الاحتياطية تنزل
  في `backups/` داخل المشروع، والمشروع تحت «سطح المكتب» المُعاد توجيهه إلى OneDrive —
  فكل dump فيه السجلّ السريري الكامل لكل طفل كان يُرفَع إلى حساب سحابي شخصي تلقائيًا.
  لا خطأ، ولا سؤال، ولا سطر في أي لوج. **أي ملف يكتبه سكربت داخل شجرة المشروع يرث
  مصير المجلّد الأب** — والمصير هنا كان المزامنة. الوجهة الآن `~/hbh-backups`،
  و`backup.sh` **يقف** عند أي مجلّد مزامنة بدل أن يحذّر: التحذير يمرّ، والفشل يُكتشف
  حين تسأل عن بيانات وصلت مكانًا لم يقصده أحد.
