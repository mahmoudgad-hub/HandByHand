# جرد الشاشات — إطار واحد في Figma لكل سطر

مستخرَج يوم 2026-09-09 ومُراجَع **2026-09-10** من `web/portal/src/app/app.routes.ts`
و`web/ops/src/app/app.routes.ts` و`site/`. **اسم الإطار في Figma هو المسار نفسه** —
لا اسمًا وصفيًّا. المسار لا يلتبس، والاسم الوصفي يلتبس بعد شهر.

المجموع **٤٤ إطارًا**: ١٦ في البوّابة، ٢٦ في تطبيق المركز، ٢ في الموقع العام.

> **مراجعة 2026-09-10:** كان المجموع ٤٢، وثلاث ملاحظات في هذا الملفّ صارت غير صحيحة.
> السبب واحد: **الهجرات بلغت ٨٠**، وآخرها `0079_family_messages` و`0080_family_message_reads`
> نزلت بعد كتابة الجرد. التفصيل في آخر الملفّ تحت «ما صُحِّح».

---

## بوّابة وليّ الأمر — `web/portal` · موبايل أوّلًا · ٣٩٠×٨٤٤

مسارات هذا التطبيق **لا تحمل معرّف طفل** إطلاقًا. الطفل المختار يعيش في
`ChildContextService`، لأنّ وضعه في المسار يوحي بأنّ تعديل شريط العنوان يغيّر ما يُرى —
والخادم هو من يقرّر ذلك. الاستثناء الوحيد `:therapistId`، وهو عضو طاقم يجوز لكل أسرة
أن تبحث عنه، والخدمة تُخفي على الصفّ نفسه ما لا يجوز للأسرة رؤيته.

| # | المسار | الحارس | الهيكل | ملاحظة تصميمية |
|---|---|---|---|---|
| 1 | `/apply` | **لا حارس** | مستقلّ | الطريق الوحيد بلا مصادقة في التطبيق كلّه، وخلفه الكتابة الوحيدة بلا مصادقة في الخدمة. صمّمه ليُملأ بيد واحدة على الهاتف. |
| 2 | `/login` | `guestOnlyGuard` | مستقلّ | مخرج واحد مقصود، فلا ملاحة حوله. |
| 3 | `/login/otp` | `guestOnlyGuard` | مستقلّ | ستّ خانات. الرقم غير المسجَّل والمسجَّل الذي لم يُرسَل له بعد **لهما ردّ واحد** — قرار D-11. |
| 4 | `/welcome` | `authGuard` | مستقلّ | اختيار الطفل. أسرة بطفل واحد ترى بطاقة واحدة، ولا يجوز أن تبدو ناقصة. |
| 5 | `/home` | `+ childSelectedGuard` | تبويب | الواجهة. منها `/live` و`/reports`. |
| 6 | `/schedule` | `+ childSelectedGuard` | تبويب | نقر اسم الأخصائي يفتح `/therapists/:id`. |
| 7 | `/progress` | `+ childSelectedGuard` | تبويب | أهداف وقياسات — لا رسوم بلا أرقام. |
| 8 | `/activities` | `+ childSelectedGuard` | تبويب | البرنامج المنزلي، ومنه يُرسل الطلب. |
| 9 | `/live` | `+ childSelectedGuard` | رجوع إلى `/home` | بثّ حيّ فقط: **لا تسجيل، فلا شريط تقديم**. وتحتاج موافقة مسجَّلة — D-24. |
| 10 | `/reports` | `+ childSelectedGuard` | تبويب | التقارير المنشورة فقط. |
| 11 | `/reports/:reportId` | `+ childSelectedGuard` | رجوع إلى `/reports` | من فتح تقريرًا من قائمة خمسة يريد الأربعة الباقية لا الصفحة الأولى. |
| 12 | `/billing` | `authGuard` | تبويب | **لا يحتاج طفلًا مختارًا** — المال على الأسرة لا على الطفل. |
| 13 | `/requests` | `authGuard` | تبويب | كذلك. |
| 14 | `/therapists/:therapistId` | `authGuard` | رجوع | المعرّف في المسار عمدًا — عضو طاقم لا بيانات طفل. |
| 15 | `/profile` | `authGuard` | تبويب | |
| 16 | `/communications` | `authGuard` | تبويب (`tab:'requests'`) | **أُضيفت 2026-09-10.** محادثة الأسرة مع المركز — `@hbh/shared/ui/family-messages`، وهو **مكوّن واحد يخدم التطبيقين**. خلفها `hbh.family_messages` و`family_message_reads` (الهجرتان 0079 و0080). **لا يحتاج طفلًا مختارًا** — المحادثة على وليّ الأمر لا على الطفل. |

**تبويبات الشريط السفلي:** home · schedule · progress · activities · reports · billing · requests · profile
(الشريط لا يعرضها كلّها دفعة واحدة — راجع `web/portal/src/app/layout/shell/`).

---

## تطبيق المركز — `web/ops` · سطح مكتب · ١٤٤٠×٩٠٠

كل شاشة تعلن الصلاحية التي تحتاجها. **الحارس ليس هو الرفض** — الرفض في سياسة RLS تحت
الاستعلام؛ الحارس يمنع فقط رسم شاشة سيرفض الخادم ملأها. وإخفاء زرّ ليس ضبطًا للصلاحية.

الشاشات نوعان مدفوعان بوصفين لا بكود مكرّر:

- **`ResourceScreen`** على الموارد التي تُنشأ وتُعدَّل وتُؤرشَف — الوصف في `core/resource/resource-spec`.
- **`DayScreen`** على القراءات المفهرسة بالمركز — صفوف **تتحرّك** عبر آلات الحالة، لا بتعديل عمود. الوصف في `core/ops/day-spec`.

في Figma: صمّم **قالبين** لا خمسًا وعشرين شاشة. الباقي مثيلات (instances) بمحتوى مختلف.

| # | المسار | الصلاحية | النوع | ملاحظة |
|---|---|---|---|---|
| 1 | `/login` | `opsGuestGuard` | مستقلّ | |
| 2 | `/dashboard` | `PORTAL.VIEW` | خاصّ | |
| 3 | `/children` | `CHILD.VIEW_ALL` | Resource | تبويبان: أولياء الأمور، الأطفال |
| 4 | `/children/:childId` | `CHILD.VIEW_ALL` | خاصّ | ملفّ الطفل. قبله كان النقر على صفّ لا يفعل شيئًا، فيفتح موظّف الاستقبال خمس شاشات ويرشّح كلًّا منها بيده. |
| 5 | `/children/:childId/card` | `CHILD.VIEW_ALL` | طباعة | **يخرج مع الأسرة**: بلا رقم قومي وبلا عنوان، وحبر داكن على أبيض لأنّ المتصفّحات لا تطبع الخلفيات. |
| 6 | `/therapists` | `STAFF.MANAGE` | Resource | أخصائيون · ساعات عمل · caseload |
| 7 | `/therapists/:therapistId/profile` | **بلا حارس صلاحية** | خاصّ | عمدًا: السؤال عن **الصفّ** («ملفّك أنت، أو أنت تدير الطاقم») لا عن الشخص، وحارس المسار لا يسأل إلّا عن الشخص. تقييده بـ`STAFF.MANAGE` أقفل الشاشة في وجه الأخصائيّة وحدها — وهي الوحيدة التي تسجّل الموافقة. |
| 8 | `/therapist-services` | `STAFF.MANAGE` | خاصّ | بلا صفّ هنا **لا يُحجز أيّ موعد** — `validate_slot` يردّ `THERAPIST_SERVICE_MISMATCH`. |
| 9 | `/rooms` | `CATALOG.MANAGE` | Resource | غرف · كاميرات |
| 10 | `/plans` | `PLAN.MANAGE` | Resource | خطط · أهداف · قياسات · أنشطة الطفل |
| 11 | `/catalog` | `CATALOG.MANAGE` | Resource | خدمات · باقات · مكتبة أنشطة · استبيانات |
| 12 | `/site` | `SITE.EDIT` | Resource | **مسار واحد لا أربعة**: من يفتحه يحرّر «الموقع» لا «جدول الأسئلة». والنشر يحتاج `SITE.PUBLISH` وترفضه القاعدة مهما عرضت الشاشة. |
| 13 | `/site/team` | `SITE.EDIT` | خاصّ | رئيس وتفصيل: المؤهّل لا معنى له بمعزل عن صاحبه. |
| 14 | `/appointments` | `APPOINTMENT.BOOK` | Day | |
| 15 | `/sessions` | `SESSION.START` | Day | |
| 16 | `/sessions/:sessionId/live` | `LIVE.VIEW` | خاصّ | حيّ فقط، فلا مقطع يُفتح لاحقًا ولا شريط تقديم. |
| 17 | `/reports` | `REPORT.VIEW` | Day | |
| 18 | `/billing` | `BILLING.VIEW` | Day | |
| 19 | `/requests` | `REQUEST.MANAGE` | Day | |
| 20 | `/enrolments` | `ENROLMENT.MANAGE` | Day | ما كانت التصاميم الثابتة تسمّيه *leads*. لم يكن له مسار حتى أعطته الهجرة 0018 جدولًا. |
| 21 | `/access` | `PORTAL.VIEW` | خاصّ | أيّ شاشة تحتاج أيّ صلاحية، وهل تملكها. **ليست إدارة مستخدمين** — وهذه تفرقة تصميمية لا نقص: إدارة المستخدمين والأدوار لها **تسعة مسارات API** (`/users` بـCRUD كامل · `PUT /users/{id}/roles` · `PUT /roles/{code}/permissions` · `/permissions`) **بلا شاشة تستهلكها بعد**. <br>⚠️ **صُحِّح 2026-09-10:** كان مكتوبًا «خمسة جداول بلا مسار من أيّ نوع» — المسارات موجودة، والغائب هو الشاشة. |
| 22 | `/satisfaction` | `CATALOG.MANAGE` | خاصّ | ما أجابت به الأسر. كان المركز يُنشئ الاستبيان ولا يقرأ منه ردًّا واحدًا. |
| 23 | `/ops-log` | `OPS.VIEW` | خاصّ | صفّ لكل طلب منتهٍ. الشاشة الوحيدة عن النظام لا عن طفل. |
| 24 | `/settings` | `CATALOG.MANAGE` | خاصّ | ⚠️ **صُحِّح 2026-09-10:** كان مكتوبًا «قراءة فقط لأنّ غير ذلك غير ممكن: لا مسار يقرأ `sys_params` ولا يكتبها». **المسارات موجودة:** `GET /settings/params` · `PATCH /settings/params/{code}` · `DELETE /settings/params/{code}` · `PATCH /settings/center` · `GET /settings/time-zones`، ويحرسها `guard_center_settings`. **فالشاشة تُصمَّم قابلة للتحرير.** |
| 25 | `/denied` | — | مستقلّ | |
| 26 | `/communications` | — (خلف `opsAuthGuard` وحده) | تبويب | **أُضيفت 2026-09-10.** الطرف الآخر من محادثة الأسرة — **نفس مكوّن البوّابة** `@hbh/shared/ui/family-messages`. المسارات: `GET /family-contacts` · `GET|POST /family-messages/{guardian_id}` · `POST /family-messages/{guardian_id}/read`. |

**غائبة واحدة من التصاميم الثابتة:** `leads` عادت باسم `/enrolments`.

> ⚠️ **صُحِّح 2026-09-10:** كان هنا أن `communications` «ما زالت بلا جدول، فبلا مسار».
> **صارت لها جداولها ومساراتها وشاشتاها** — الهجرتان `0079` و`0080`. والجملة التي
> تلتها — *«مدخل في القائمة يقود إلى لا شيء أسوأ من غيابه»* — **صحيحة وتبقى**، لكنها
> لم تعد تنطبق على هذه الشاشة.

---

## الموقع التعريفي العامّ — `site/` · ١٤٤٠ و٣٩٠

HTML/CSS/JS خام: **بلا بناء، وبلا مصادقة، وبلا API**. وهو السطح الوحيد هنا المقصود أن
يكون على الإنترنت المفتوح، والذي يجعل نشره آمنًا أنّه **لا يمرّر شيئًا إلى الخدمة** —
لا `proxy_pass` في `deploy/server/site.native.mjs` ولا في كتلة nginx الخاصّة به.

| # | الملفّ | ملاحظة |
|---|---|---|
| 1 | `index.html` | صفحة واحدة طويلة. **لا نموذج التحاق فيها** — أزرارها تشير إلى `/apply` في البوّابة عبر `portalBaseUrl` في `site/config.js`. |
| 2 | `privacy.html` | |

---

## الحالات التي يجب أن يحملها كل إطار

إطار «الشاشة السعيدة» وحده يخفي ثلثي العمل. لكل شاشة تعرض بيانات، ارسم أربع حالات
كـ variants على مكوّن واحد:

| الحالة | ما تعرضه | المكوّن في الكود |
|---|---|---|
| تحميل | هيكل عظمي، لا دوّامة تملأ الشاشة | `shared/src/ui/skeleton.ts` |
| فارغة | سبب الفراغ وما العمل — لا «لا توجد بيانات» | `shared/src/ui/empty-state.ts` |
| خطأ | ما فشل، وزرّ إعادة محاولة | `shared/src/ui/error-note.ts` |
| ممتلئة | صفّان على الأقلّ، وواحد بنصّ طويل يلتفّ | — |

وفي تطبيق المركز حالة خامسة: **ممنوعة** (`/denied`) — لأنّ الحارس يمنع الرسم، والخادم
يردّ بالرفض، والمستخدم يجب أن يرى أيّهما حدث.
